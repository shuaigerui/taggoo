
import Foundation

import UIKit

struct CSProfileSetting: Equatable {
    
    let productId: String
    let gems: Int
    
    let displayPrice: String

    var price: String {
       var dirI: String! = String(cString: [101,110,115,117,114,101,0], encoding: .utf8)!
   if dirI == dirI {
      dirI = "\(2 >> (Swift.min(3, dirI.count)))"
   }
     return displayPrice }

    
    static let catalog: [CSProfileSetting] = [
        CSProfileSetting(productId: "ujpruorrlkbqffzn", gems: 63_700, displayPrice: "$99.99"),
        CSProfileSetting(productId: "zzcjkacefpbiibug", gems: 29_400, displayPrice: "$49.99"),
        CSProfileSetting(productId: "hrzfuikywexdnuxk", gems: 10_800, displayPrice: "$19.99"),
        CSProfileSetting(productId: "woqhjfsmlmeyjqov", gems: 5_150, displayPrice: "$9.99"),
        CSProfileSetting(productId: "lyekhqwnlnxjwaet", gems: 2_450, displayPrice: "$4.99"),
        CSProfileSetting(productId: "jcnxubmhvhgnhtyw", gems: 800, displayPrice: "$1.99"),
        CSProfileSetting(productId: "qjubzksscotwhmqr", gems: 400, displayPrice: "$0.99")
    ]


    static func package(productId: String) -> CSProfileSetting? {
       var tempg: Double = 4.0
   withUnsafeMutablePointer(to: &tempg) { pointer in
    
   }
   repeat {
      tempg += (Double(3 & Int(tempg > 356847132.0 || tempg < -356847132.0 ? 5.0 : tempg)))
      if tempg == 900787.0 {
         break
      }
   } while (tempg == 900787.0) && (tempg <= tempg)

return         catalog.first { $0.productId == productId }
    }

    static var productIds: [String] {
       var window_so: String! = String(cString: [99,120,100,97,116,97,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &window_so) { pointer in
    
   }
       var star1: [Any]! = [74, 57]
       _ = star1
       var cachen: Double = 3.0
       _ = cachen
         star1 = [(star1.count % (Swift.max(8, Int(cachen > 380620254.0 || cachen < -380620254.0 ? 5.0 : cachen))))]
          var k_layerf: String! = String(cString: [99,97,108,108,105,115,116,111,0], encoding: .utf8)!
          var rechargek: String! = String(cString: [103,101,116,115,111,99,107,97,100,100,114,0], encoding: .utf8)!
          var cacheu: Double = 5.0
         star1 = [(2 + Int(cachen > 261284100.0 || cachen < -261284100.0 ? 38.0 : cachen))]
         k_layerf.append("\((Int(cachen > 83023502.0 || cachen < -83023502.0 ? 52.0 : cachen)))")
         rechargek.append("\((k_layerf.count << (Swift.min(4, labs(Int(cachen > 62060979.0 || cachen < -62060979.0 ? 47.0 : cachen))))))")
         cacheu /= Swift.max(Double(3 - rechargek.count), 3)
      if (star1.count - 4) < 2 {
          var menue: String! = String(cString: [112,108,117,114,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
         cachen /= Swift.max(1, Double(3 % (Swift.max(4, menue.count))))
      }
      repeat {
          var wrapl: String! = String(cString: [114,103,98,116,101,115,116,0], encoding: .utf8)!
         star1 = [(Int(cachen > 241468507.0 || cachen < -241468507.0 ? 37.0 : cachen) | 3)]
         wrapl.append("\(wrapl.count)")
         if star1.count == 4524951 {
            break
         }
      } while (star1.contains { $0 as? Double == cachen }) && (star1.count == 4524951)
         star1 = [star1.count]
         cachen -= Double(3)
      window_so.append("\((Int(cachen > 151799999.0 || cachen < -151799999.0 ? 11.0 : cachen)))")

            return catalog.map(\.productId)
    }
}

final class CSPushInfoCell: UICollectionViewCell {

    static let reuseID = "CSPushInfoCell"

    private let cardView: UIView = {
       var diamondD: Float = 3.0
    var topY: [Any]! = [82, 50, 46]
    var relativeL: String! = String(cString: [114,101,110,100,101,114,101,114,0], encoding: .utf8)!
   repeat {
      topY.append(3 ^ topY.count)
      if 2810738 == topY.count {
         break
      }
   } while (2810738 == topY.count) && (1 == (4 >> (Swift.min(2, topY.count))))

        let v = UIView()
      relativeL.append("\(1 | topY.count)")
        v.backgroundColor = .white.withAlphaComponent(0.3)
      relativeL.append("\((2 >> (Swift.min(labs(Int(diamondD > 351288342.0 || diamondD < -351288342.0 ? 77.0 : diamondD)), 2))))")
        v.layer.cornerRadius = 16
      diamondD /= Swift.max(5, (Float(Int(diamondD > 219559911.0 || diamondD < -219559911.0 ? 8.0 : diamondD) - Int(diamondD > 114809836.0 || diamondD < -114809836.0 ? 13.0 : diamondD))))
        v.clipsToBounds = true
        return v
    }()

    private let diamondView: UIImageView = {
       var configz: String! = String(cString: [114,101,116,0], encoding: .utf8)!
    var random6: [Any]! = [String(cString: [99,102,104,100,0], encoding: .utf8)!, String(cString: [100,112,99,109,0], encoding: .utf8)!]
    var group4: Double = 2.0
       var detailX: String! = String(cString: [115,111,105,115,99,111,110,110,101,99,116,105,110,103,0], encoding: .utf8)!
       _ = detailX
       var bubblel: [String: Any]! = [String(cString: [99,104,114,111,109,97,116,105,99,0], encoding: .utf8)!:56.0]
       var success_: String! = String(cString: [112,114,101,101,109,112,116,105,118,101,0], encoding: .utf8)!
       _ = success_
      if (4 + success_.count) >= 2 {
         bubblel[success_] = 2 - success_.count
      }
         detailX = "\(detailX.count - bubblel.count)"
      repeat {
         bubblel = ["\(bubblel.keys.count)": ((String(cString:[74,0], encoding: .utf8)!) == success_ ? success_.count : bubblel.values.count)]
         if bubblel.count == 3539366 {
            break
         }
      } while (bubblel.count == 3539366) && (bubblel.count <= 1)
          var postm: String! = String(cString: [109,98,98,121,0], encoding: .utf8)!
          var lighty: [String: Any]! = [String(cString: [105,112,97,100,100,0], encoding: .utf8)!:String(cString: [101,120,116,101,110,115,105,111,110,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &lighty) { pointer in
    
         }
          var sideY: String! = String(cString: [111,98,106,99,0], encoding: .utf8)!
         success_ = "\(postm.count ^ 1)"
         lighty[success_] = (success_ == (String(cString:[110,0], encoding: .utf8)!) ? detailX.count : success_.count)
         sideY = "\(bubblel.keys.count * 3)"
       var likedd: [Any]! = [88, 66]
      for _ in 0 ..< 1 {
          var photo_: Float = 4.0
          var fallbackQ: String! = String(cString: [100,97,116,97,104,97,115,104,0], encoding: .utf8)!
         likedd = [likedd.count ^ 3]
         photo_ -= (Float((String(cString:[48,0], encoding: .utf8)!) == fallbackQ ? fallbackQ.count : success_.count))
      }
      if 4 > (2 >> (Swift.min(2, detailX.count))) {
         detailX = "\(((String(cString:[69,0], encoding: .utf8)!) == success_ ? likedd.count : success_.count))"
      }
       var hostp: Double = 2.0
      if 3 < (success_.count / 2) && 5 < (2 / (Swift.max(10, success_.count))) {
         bubblel = ["\(likedd.count)": (success_ == (String(cString:[87,0], encoding: .utf8)!) ? likedd.count : success_.count)]
      }
         hostp /= Swift.max(2, (Double(Int(hostp > 196425630.0 || hostp < -196425630.0 ? 38.0 : hostp) + 1)))
      random6.append(1)
      group4 /= Swift.max(1, Double(random6.count))

        let v = UIImageView(image: "profile_diamond".toImage)
      group4 /= Swift.max(Double(2), 4)
       var savedo: [Any]! = [10, 42, 73]
       var createdi: String! = String(cString: [101,120,112,97,110,115,105,111,110,0], encoding: .utf8)!
         savedo.append(savedo.count << (Swift.min(labs(1), 5)))
      repeat {
         createdi = "\(createdi.count >> (Swift.min(4, savedo.count)))"
         if 2388567 == createdi.count {
            break
         }
      } while ((5 & savedo.count) > 3) && (2388567 == createdi.count)
         createdi.append("\(createdi.count | 2)")
      for _ in 0 ..< 2 {
         createdi.append("\(2 << (Swift.min(3, savedo.count)))")
      }
         createdi = "\(savedo.count << (Swift.min(createdi.count, 2)))"
      for _ in 0 ..< 3 {
         createdi = "\(savedo.count)"
      }
      configz.append("\(configz.count ^ createdi.count)")
        v.contentMode = .scaleAspectFit
        return v
    }()

    private let gemsLabel: UILabel = {
       var n_productsP: String! = String(cString: [105,115,108,101,97,112,0], encoding: .utf8)!
    var delete_jhj: Double = 4.0
      n_productsP = "\((Int(delete_jhj > 90214383.0 || delete_jhj < -90214383.0 ? 19.0 : delete_jhj)))"

        let v = UILabel()
   repeat {
      n_productsP.append("\(n_productsP.count)")
      if n_productsP == (String(cString:[49,119,112,98,111,0], encoding: .utf8)!) {
         break
      }
   } while (delete_jhj <= Double(n_productsP.count)) && (n_productsP == (String(cString:[49,119,112,98,111,0], encoding: .utf8)!))
        v.font = .systemFont(ofSize: 16, weight: .semibold)
        v.textColor = UIColor(hex: "#4A3F35")
        v.textAlignment = .center
        return v
    }()

    private let priceContainer: UIView = {
       var c_managerS: Float = 1.0
    var closeX: String! = String(cString: [99,105,112,104,101,114,115,0], encoding: .utf8)!
       var pathsH: String! = String(cString: [105,110,105,116,105,97,108,105,122,101,114,115,0], encoding: .utf8)!
       _ = pathsH
       var changeh: Bool = true
         changeh = pathsH.count > 27
         pathsH = "\(((String(cString:[120,0], encoding: .utf8)!) == pathsH ? pathsH.count : (changeh ? 5 : 4)))"
      while (!pathsH.hasPrefix("\(changeh)")) {
          var visiblen: Double = 1.0
         changeh = !pathsH.hasPrefix("\(changeh)")
         visiblen += (Double(Int(visiblen > 18785925.0 || visiblen < -18785925.0 ? 98.0 : visiblen) / (Swift.max(8, (changeh ? 3 : 2)))))
         break
      }
      for _ in 0 ..< 1 {
         changeh = pathsH.count == 71
      }
          var foundn: Double = 2.0
          var applyV: String! = String(cString: [106,115,101,112,0], encoding: .utf8)!
         pathsH = "\((Int(foundn > 255139972.0 || foundn < -255139972.0 ? 11.0 : foundn) >> (Swift.min(5, labs(3)))))"
         applyV.append("\(applyV.count - 3)")
      while (4 >= pathsH.count) {
         changeh = pathsH.count <= 15
         break
      }
      closeX = "\((Int(c_managerS > 294561382.0 || c_managerS < -294561382.0 ? 99.0 : c_managerS) - 3))"

        let v = UIView()
      c_managerS /= Swift.max((Float(Int(c_managerS > 312871576.0 || c_managerS < -312871576.0 ? 17.0 : c_managerS) - closeX.count)), 5)
        v.backgroundColor = .white.withAlphaComponent(0.5)
        v.layer.cornerRadius = 14
        v.clipsToBounds = true
        return v
    }()

    private let priceLabel: UILabel = {
       var rawt: Float = 2.0
   for _ in 0 ..< 2 {
       var actionsU: String! = String(cString: [118,97,108,105,100,0], encoding: .utf8)!
       var dismissr: String! = String(cString: [101,116,97,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &dismissr) { pointer in
    
      }
       var unread9: String! = String(cString: [108,106,112,101,103,0], encoding: .utf8)!
       var movieC: Float = 3.0
       var iconi: [Any]! = [30.0]
       _ = iconi
         actionsU = "\(((String(cString:[70,0], encoding: .utf8)!) == dismissr ? iconi.count : dismissr.count))"
         actionsU.append("\(dismissr.count * 3)")
      while (3.57 <= (movieC / 5.89) && 5.89 <= (movieC / (Swift.max(9, Float(dismissr.count))))) {
         movieC -= Float(unread9.count << (Swift.min(labs(3), 2)))
         break
      }
         iconi = [dismissr.count]
      repeat {
         movieC /= Swift.max(3, Float(dismissr.count))
         if movieC == 920126.0 {
            break
         }
      } while (2.31 > (Float(iconi.count) / (Swift.max(4, movieC))) || 5.78 > (movieC / 2.31)) && (movieC == 920126.0)
      repeat {
         movieC /= Swift.max(Float(2), 3)
         if 1798397.0 == movieC {
            break
         }
      } while (1798397.0 == movieC) && (!dismissr.contains("\(movieC)"))
      while (3 >= (iconi.count * 3) || 1 >= (3 * iconi.count)) {
         unread9 = "\(dismissr.count)"
         break
      }
      for _ in 0 ..< 2 {
         movieC /= Swift.max((Float(Int(movieC > 161075244.0 || movieC < -161075244.0 ? 56.0 : movieC) * 3)), 1)
      }
          var hnewsc: String! = String(cString: [115,116,114,108,0], encoding: .utf8)!
          var w_badgee: Bool = false
         withUnsafeMutablePointer(to: &w_badgee) { pointer in
    
         }
          var sessionf: [Any]! = [String(cString: [99,97,112,116,117,114,101,114,0], encoding: .utf8)!]
         actionsU = "\((unread9.count % (Swift.max(3, (w_badgee ? 1 : 1)))))"
         hnewsc = "\(1)"
         sessionf.append(sessionf.count)
       var reusen: String! = String(cString: [108,97,112,110,100,122,0], encoding: .utf8)!
       var arrowO: String! = String(cString: [99,105,110,116,0], encoding: .utf8)!
       _ = arrowO
          var modelc: String! = String(cString: [98,105,110,100,101,114,0], encoding: .utf8)!
         iconi.append((actionsU == (String(cString:[66,0], encoding: .utf8)!) ? reusen.count : actionsU.count))
         modelc.append("\(arrowO.count)")
      for _ in 0 ..< 1 {
         reusen.append("\(reusen.count)")
      }
         actionsU.append("\(reusen.count / 1)")
      while ((unread9.count - 1) > 2 && 2 > (1 - iconi.count)) {
          var ownL: String! = String(cString: [114,101,115,112,111,110,115,101,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &ownL) { pointer in
                _ = pointer.pointee
         }
          var configZ: String! = String(cString: [104,119,116,105,109,101,0], encoding: .utf8)!
          var providerz: Int = 4
         unread9.append("\((dismissr == (String(cString:[100,0], encoding: .utf8)!) ? dismissr.count : providerz))")
         ownL.append("\(3)")
         configZ = "\(dismissr.count)"
         break
      }
      if (reusen.count % 3) > 1 {
         reusen.append("\(reusen.count >> (Swift.min(labs(1), 1)))")
      }
      rawt /= Swift.max(5, Float(dismissr.count))
   }

        let v = UILabel()
        v.font = .systemFont(ofSize: 13, weight: .medium)
        v.textColor = UIColor(hex: "#666666")
        v.textAlignment = .center
        return v
    }()

    override init(frame: CGRect) {
        super.init(frame: frame)
        setupUI()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    private static func formatGems(_ count: Int) -> String {
       var itemK: Double = 1.0
   for _ in 0 ..< 2 {
       var followersF: [Any]! = [52, 47]
       var bannerX: Bool = true
       _ = bannerX
       var detaile: Bool = true
      withUnsafeMutablePointer(to: &detaile) { pointer in
    
      }
       var skyB: Double = 3.0
       var applyS: Float = 5.0
          var delete_1M: String! = String(cString: [112,114,101,118,118,101,99,0], encoding: .utf8)!
          var extraS: String! = String(cString: [99,104,97,114,99,111,110,118,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &extraS) { pointer in
    
         }
          var celli: Double = 0.0
         detaile = delete_1M.count <= 24
         extraS.append("\(followersF.count % 3)")
         celli -= (Double((bannerX ? 1 : 2) * Int(celli > 149162077.0 || celli < -149162077.0 ? 9.0 : celli)))
         followersF = [2]
      while (applyS >= 2.73) {
         followersF.append((Int(applyS > 67977679.0 || applyS < -67977679.0 ? 8.0 : applyS)))
         break
      }
      repeat {
         skyB /= Swift.max(Double(2 + followersF.count), 4)
         if 3273333.0 == skyB {
            break
         }
      } while ((skyB - 4.95) == 1.5 || 4.95 == skyB) && (3273333.0 == skyB)
      while (detaile) {
         followersF = [(Int(applyS > 350550870.0 || applyS < -350550870.0 ? 67.0 : applyS) & (bannerX ? 5 : 3))]
         break
      }
      if detaile || 1 >= (4 * followersF.count) {
         detaile = followersF.count == 1 && bannerX
      }
         skyB -= (Double(Int(applyS > 145896639.0 || applyS < -145896639.0 ? 22.0 : applyS) | Int(skyB > 23642797.0 || skyB < -23642797.0 ? 1.0 : skyB)))
         detaile = applyS <= 64.66
      repeat {
         bannerX = (((bannerX ? 7 : followersF.count) % (Swift.max(10, followersF.count))) <= 7)
         if bannerX ? !bannerX : bannerX {
            break
         }
      } while (bannerX ? !bannerX : bannerX) && (!bannerX)
         skyB += (Double(Int(applyS > 187768477.0 || applyS < -187768477.0 ? 10.0 : applyS) - (detaile ? 2 : 2)))
         followersF.append(((detaile ? 1 : 1) >> (Swift.min(labs(Int(applyS > 393390129.0 || applyS < -393390129.0 ? 55.0 : applyS)), 1))))
          var modelsY: [String: Any]! = [String(cString: [98,97,115,105,115,0], encoding: .utf8)!:58, String(cString: [99,111,100,101,102,0], encoding: .utf8)!:26]
          _ = modelsY
         bannerX = !bannerX
         modelsY = ["\(applyS)": (Int(applyS > 317903840.0 || applyS < -317903840.0 ? 41.0 : applyS) % 2)]
      for _ in 0 ..< 2 {
          var appendb: [Any]! = [30, 72, 53]
          var layout4: String! = String(cString: [110,101,119,101,115,116,0], encoding: .utf8)!
          var legacyG: Bool = false
          var welcomez: Float = 0.0
         applyS += (Float(3 << (Swift.min(labs(Int(skyB > 194410003.0 || skyB < -194410003.0 ? 87.0 : skyB)), 1))))
         appendb = [(Int(skyB > 201195451.0 || skyB < -201195451.0 ? 71.0 : skyB))]
         layout4 = "\((appendb.count >> (Swift.min(4, labs(Int(welcomez > 24235362.0 || welcomez < -24235362.0 ? 25.0 : welcomez))))))"
         legacyG = welcomez == 21.90
      }
       var x_imagei: Bool = false
      withUnsafeMutablePointer(to: &x_imagei) { pointer in
             _ = pointer.pointee
      }
      repeat {
         applyS -= Float(followersF.count % 2)
         if 3553030.0 == applyS {
            break
         }
      } while (3553030.0 == applyS) && (3.22 <= applyS || (applyS * 3.22) <= 5.88)
      itemK -= (Double(2 + (detaile ? 4 : 1)))
   }

        let mode = NumberFormatter()
        mode.numberStyle = .decimal
        return mode.string(from: NSNumber(value: count)) ?? "\(count)"
    }


    private func setupUI() {
       var gradientr: Int = 4
    var prefix_7p: [Any]! = [90, 75, 22]
   while ((prefix_7p.count * 1) < 3) {
       var basel: [String: Any]! = [String(cString: [108,117,116,100,0], encoding: .utf8)!:50.0]
      withUnsafeMutablePointer(to: &basel) { pointer in
    
      }
      for _ in 0 ..< 3 {
         basel = ["\(basel.values.count)": 1]
      }
      repeat {
         basel = ["\(basel.count)": basel.count]
         if basel.count == 108383 {
            break
         }
      } while (basel.keys.contains("\(basel.keys.count)")) && (basel.count == 108383)
          var finishedO: String! = String(cString: [109,105,110,113,0], encoding: .utf8)!
         basel = ["\(basel.keys.count)": 3 * basel.count]
         finishedO = "\((finishedO == (String(cString:[114,0], encoding: .utf8)!) ? finishedO.count : basel.count))"
      prefix_7p.append(prefix_7p.count / (Swift.max(1, gradientr)))
      break
   }

   for _ in 0 ..< 2 {
      prefix_7p.append(gradientr)
   }
        contentView.addSubview(cardView)
        cardView.addSubview(diamondView)
        cardView.addSubview(gemsLabel)
        cardView.addSubview(priceContainer)
        priceContainer.addSubview(priceLabel)

        cardView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        diamondView.snp.makeConstraints { make in
            make.top.equalToSuperview().offset(14)
            make.centerX.equalToSuperview()
            make.width.height.equalTo(35)
        }

        gemsLabel.snp.makeConstraints { make in
            make.top.equalTo(diamondView.snp.bottom).offset(6)
            make.centerX.equalToSuperview()
        }

        priceContainer.snp.makeConstraints { make in
            make.left.right.equalToSuperview().inset(10)
            make.bottom.equalToSuperview().offset(-10)
            make.height.equalTo(28)
        }

        priceLabel.snp.makeConstraints { make in
            make.center.equalToSuperview()
        }
    }


    func configure(with package: CSProfileSetting, priceText: String? = nil) {
       var n_heightx: String! = String(cString: [116,114,116,97,98,108,101,0], encoding: .utf8)!
    var sessionu: String! = String(cString: [97,116,116,114,105,98,117,116,101,100,0], encoding: .utf8)!
   while (4 > sessionu.count) {
       var videot: String! = String(cString: [117,110,105,113,117,101,0], encoding: .utf8)!
      repeat {
         videot = "\(videot.count)"
         if 1665631 == videot.count {
            break
         }
      } while (videot.count >= 1) && (1665631 == videot.count)
      if 1 <= videot.count && videot != String(cString:[48,0], encoding: .utf8)! {
         videot.append("\(videot.count)")
      }
          var conversationM: String! = String(cString: [102,105,108,101,115,121,115,116,101,109,0], encoding: .utf8)!
         videot.append("\(2 - conversationM.count)")
      n_heightx = "\(3)"
      break
   }

      sessionu = "\(sessionu.count << (Swift.min(5, sessionu.count)))"
        gemsLabel.text = Self.formatGems(package.gems)
        priceLabel.text = priceText ?? package.displayPrice
    }
}
