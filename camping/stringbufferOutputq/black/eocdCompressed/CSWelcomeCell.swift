
import Foundation

import UIKit

final class CSWelcomeCell: UITableViewCell {

    static let reuseID = "CSWelcomeCell"

    var onActionTapped: (() -> Void)?

    private let cardView: UIView = {
       var collecte: Double = 2.0
    _ = collecte
   for _ in 0 ..< 2 {
      collecte -= (Double(2 / (Swift.max(6, Int(collecte > 92636756.0 || collecte < -92636756.0 ? 40.0 : collecte)))))
   }

        let v = UIView()
        v.backgroundColor = UIColor(hex: "#C8E6A0").withAlphaComponent(0.88)
        v.layer.cornerRadius = 18
        v.clipsToBounds = true
        return v
    }()

    private let avatarView: UIImageView = {
       var packagesf: String! = String(cString: [120,118,105,100,105,100,99,116,0], encoding: .utf8)!
       var commente: Bool = true
         commente = (commente ? !commente : !commente)
          var priceV: Bool = true
         withUnsafeMutablePointer(to: &priceV) { pointer in
    
         }
         commente = priceV
          var b_centery: Int = 3
          var cell7: Bool = true
          var submittedI: String! = String(cString: [114,108,105,110,101,0], encoding: .utf8)!
         commente = (b_centery & submittedI.count) <= 2
         cell7 = 81 > b_centery
      packagesf = "\((packagesf.count & (commente ? 2 : 5)))"

        let v = UIImageView()
        v.contentMode = .scaleAspectFill
        v.clipsToBounds = true
        v.layer.cornerRadius = 26
        v.backgroundColor = UIColor(hex: "#D4C4A8")
        return v
    }()

    private let nameLabel: UILabel = {
       var lighto: String! = String(cString: [104,97,100,111,119,115,0], encoding: .utf8)!
      lighto = "\(2)"

        let v = UILabel()
        v.font = .systemFont(ofSize: 16, weight: .semibold)
        v.textColor = UIColor(hex: "#2D2D2D")
        return v
    }()

    private lazy var imageActionButton: UIButton = {
       var primaryb: [String: Any]! = [String(cString: [115,119,97,98,0], encoding: .utf8)!:87, String(cString: [114,116,112,119,0], encoding: .utf8)!:21]
   for _ in 0 ..< 3 {
      primaryb = ["\(primaryb.values.count)": 2 - primaryb.count]
   }

        let photoBtn = UIButton(type: .custom)
        photoBtn.contentHorizontalAlignment = .fill
        photoBtn.contentVerticalAlignment = .fill
        photoBtn.imageView?.contentMode = .scaleAspectFit
        photoBtn.addTarget(self, action: #selector(actionTapped), for: .touchUpInside)
        return photoBtn
    }()

    private lazy var textActionButton: UIButton = {
       var tappedB: String! = String(cString: [110,97,110,111,98,101,110,99,104,109,97,114,107,0], encoding: .utf8)!
   while (tappedB.count >= tappedB.count) {
      tappedB.append("\((tappedB == (String(cString:[114,0], encoding: .utf8)!) ? tappedB.count : tappedB.count))")
      break
   }

        let photoBtn = UIButton(type: .custom)
        photoBtn.titleLabel?.font = .systemFont(ofSize: 14, weight: .semibold)
        photoBtn.setTitleColor(UIColor(hex: "#2D2D2D"), for: .normal)
        photoBtn.layer.cornerRadius = 18
        photoBtn.clipsToBounds = true
        photoBtn.contentEdgeInsets = UIEdgeInsets(top: 8, left: 16, bottom: 8, right: 16)
        photoBtn.addTarget(self, action: #selector(actionTapped), for: .touchUpInside)
        photoBtn.isHidden = true
        return photoBtn
    }()

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        setupUI()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    private func setupUI() {
       var showsx: Float = 4.0
    var local_zd: String! = String(cString: [104,113,120,100,115,112,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &local_zd) { pointer in
    
   }
      local_zd.append("\(local_zd.count)")

       var panel2: String! = String(cString: [115,105,109,117,108,99,97,115,116,0], encoding: .utf8)!
       _ = panel2
       var followO: Int = 5
      withUnsafeMutablePointer(to: &followO) { pointer in
             _ = pointer.pointee
      }
         panel2.append("\(panel2.count)")
          var coverj: String! = String(cString: [102,105,108,116,101,114,101,100,0], encoding: .utf8)!
          var suffixZ: String! = String(cString: [102,114,97,110,100,111,109,0], encoding: .utf8)!
          var sendera: [String: Any]! = [String(cString: [100,97,116,97,98,97,115,101,115,0], encoding: .utf8)!:String(cString: [115,108,105,99,101,97,110,103,108,101,0], encoding: .utf8)!]
         followO /= Swift.max(5, panel2.count)
         coverj = "\(1)"
         suffixZ.append("\(sendera.count & followO)")
         sendera = [suffixZ: suffixZ.count]
          var nowr: String! = String(cString: [122,101,114,111,101,115,0], encoding: .utf8)!
          _ = nowr
          var current8: String! = String(cString: [99,104,101,98,121,115,104,101,118,0], encoding: .utf8)!
         panel2 = "\(panel2.count + current8.count)"
         nowr.append("\(followO >> (Swift.min(labs(3), 4)))")
      while (3 >= (panel2.count / (Swift.max(7, followO)))) {
         panel2 = "\((panel2 == (String(cString:[110,0], encoding: .utf8)!) ? panel2.count : followO))"
         break
      }
      repeat {
         panel2.append("\(followO | panel2.count)")
         if panel2 == (String(cString:[107,57,50,0], encoding: .utf8)!) {
            break
         }
      } while (followO == 5) && (panel2 == (String(cString:[107,57,50,0], encoding: .utf8)!))
       var repliesn: [Any]! = [8, 57, 43]
       var valuek: [Any]! = [88, 6]
         repliesn.append(3)
         valuek.append(2 + repliesn.count)
      local_zd.append("\(local_zd.count << (Swift.min(labs(2), 2)))")
        selectionStyle = .none
      showsx += (Float(Int(showsx > 53647735.0 || showsx < -53647735.0 ? 22.0 : showsx) % (Swift.max(1, Int(showsx > 144335898.0 || showsx < -144335898.0 ? 5.0 : showsx)))))
        backgroundColor = .clear
        contentView.backgroundColor = .clear

        contentView.addSubview(cardView)
        cardView.addSubview(avatarView)
        cardView.addSubview(nameLabel)
        cardView.addSubview(imageActionButton)
        cardView.addSubview(textActionButton)

        cardView.snp.makeConstraints { make in
            make.edges.equalToSuperview().inset(
                UIEdgeInsets(top: 6, left: 16, bottom: 6, right: 16)
            )
        }

        avatarView.snp.makeConstraints { make in
            make.left.equalToSuperview().offset(14)
            make.centerY.equalToSuperview()
            make.width.height.equalTo(52)
        }

        imageActionButton.snp.makeConstraints { make in
            make.right.equalToSuperview().offset(-14)
            make.centerY.equalToSuperview()
            make.width.equalTo(88)
            make.height.equalTo(36)
        }

        textActionButton.snp.makeConstraints { make in
            make.right.equalToSuperview().offset(-14)
            make.centerY.equalToSuperview()
            make.height.equalTo(36)
        }

        nameLabel.snp.makeConstraints { make in
            make.left.equalTo(avatarView.snp.right).offset(12)
            make.centerY.equalToSuperview()
            make.right.lessThanOrEqualTo(imageActionButton.snp.left).offset(-8)
        }
    }


    func configure(user: CSResource, actionStyle: CS_UserListActionStyle) {
       var textb: Float = 5.0
    var value6: String! = String(cString: [110,116,111,108,111,103,121,0], encoding: .utf8)!
    _ = value6
       var coverP: Double = 5.0
       var playo: Float = 5.0
      if Float(coverP) >= playo {
         coverP += (Double(Int(playo > 271163985.0 || playo < -271163985.0 ? 84.0 : playo) << (Swift.min(2, labs(2)))))
      }
          var shouldU: [Any]! = [41, 91]
          var scriptsE: String! = String(cString: [109,101,116,97,100,97,116,97,0], encoding: .utf8)!
         playo -= Float(shouldU.count)
         scriptsE = "\(scriptsE.count & shouldU.count)"
      for _ in 0 ..< 2 {
          var secondsu: Double = 3.0
          var accessedB: String! = String(cString: [110,111,119,0], encoding: .utf8)!
         playo -= (Float(Int(coverP > 12836027.0 || coverP < -12836027.0 ? 12.0 : coverP) - 1))
         secondsu -= Double(2)
         accessedB.append("\((3 % (Swift.max(4, Int(coverP > 114559371.0 || coverP < -114559371.0 ? 79.0 : coverP)))))")
      }
         playo -= (Float(3 + Int(playo > 148342500.0 || playo < -148342500.0 ? 57.0 : playo)))
          var audioE: String! = String(cString: [114,111,119,105,100,0], encoding: .utf8)!
          var previewk: String! = String(cString: [102,114,97,99,116,105,111,110,97,108,0], encoding: .utf8)!
         playo += Float(audioE.count)
         previewk = "\((previewk == (String(cString:[110,0], encoding: .utf8)!) ? Int(playo > 97230135.0 || playo < -97230135.0 ? 91.0 : playo) : previewk.count))"
      while (2.64 <= (playo + 5.76)) {
         playo /= Swift.max(Float(1), 4)
         break
      }
      textb += (Float((String(cString:[55,0], encoding: .utf8)!) == value6 ? value6.count : Int(textb > 288719104.0 || textb < -288719104.0 ? 73.0 : textb)))

        nameLabel.text = user.userName
        if let path = user.avatarURL, !path.isEmpty {
            avatarView.image = path.resourceFileImage ?? path.toImage
            avatarView.backgroundColor = avatarView.image == nil
                ? UIColor(hex: "#D4C4A8") : .clear
        } else {
            avatarView.image = "info_avatar".toImage
            avatarView.backgroundColor = .clear
        }

        switch actionStyle {
        case .image(let name):
            imageActionButton.isHidden = false
            textActionButton.isHidden = true
            imageActionButton.setImage(name.toImage, for: .normal)
            nameLabel.snp.remakeConstraints { make in
                make.left.equalTo(avatarView.snp.right).offset(12)
                make.centerY.equalToSuperview()
                make.right.lessThanOrEqualTo(imageActionButton.snp.left).offset(-8)
            }
        case .text(let title, let color):
            imageActionButton.isHidden = true
            textActionButton.isHidden = false
            textActionButton.setTitle(title, for: .normal)
            textActionButton.backgroundColor = color
            nameLabel.snp.remakeConstraints { make in
                make.left.equalTo(avatarView.snp.right).offset(12)
                make.centerY.equalToSuperview()
                make.right.lessThanOrEqualTo(textActionButton.snp.left).offset(-8)
            }
        }
    }


    @objc private func actionTapped() {
       var peoplej: [String: Any]! = [String(cString: [104,97,112,116,105,99,0], encoding: .utf8)!:47, String(cString: [119,109,97,118,111,105,99,101,0], encoding: .utf8)!:30, String(cString: [108,97,117,110,99,104,0], encoding: .utf8)!:81]
    var n_products5: Bool = false
       var email_: Bool = false
       var recordst: String! = String(cString: [99,111,117,112,108,105,110,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &recordst) { pointer in
    
      }
       var capturerH: Double = 2.0
         email_ = recordst == (String(cString:[67,0], encoding: .utf8)!)
      repeat {
         capturerH /= Swift.max(1, Double(recordst.count))
         if 572266.0 == capturerH {
            break
         }
      } while (572266.0 == capturerH) && ((capturerH * 2.2) > 1.18 || 2.2 > capturerH)
         recordst = "\((recordst.count * Int(capturerH > 334146829.0 || capturerH < -334146829.0 ? 12.0 : capturerH)))"
         recordst.append("\(((email_ ? 1 : 3)))")
         recordst = "\(((email_ ? 1 : 4) % (Swift.max(Int(capturerH > 33501449.0 || capturerH < -33501449.0 ? 23.0 : capturerH), 4))))"
         capturerH += (Double((email_ ? 4 : 3)))
      repeat {
         email_ = recordst.hasSuffix("\(capturerH)")
         if email_ ? !email_ : email_ {
            break
         }
      } while (email_ ? !email_ : email_) && (!email_)
      repeat {
         capturerH -= Double(3 - recordst.count)
         if capturerH == 4134544.0 {
            break
         }
      } while ((capturerH + 5.3) > 1.31) && (capturerH == 4134544.0)
       var collectionC: Bool = true
         collectionC = capturerH <= 54.11
      peoplej = ["\(capturerH)": (Int(capturerH > 28624229.0 || capturerH < -28624229.0 ? 2.0 : capturerH) + (n_products5 ? 4 : 3))]
      peoplej["\(n_products5)"] = ((n_products5 ? 2 : 2) - 1)

        onActionTapped?()
    }
}
