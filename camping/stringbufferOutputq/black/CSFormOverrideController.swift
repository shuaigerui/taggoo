
import Foundation

import UIKit
import Toast_Swift

class CSFormOverrideController: CSConversationController {

    private let kind: CS_UserListKind
    private var users: [CSResource] = []

    private lazy var backButton: UIButton = {
       var playingE: [Any]! = [44, 84, 66]
    var starrym: String! = String(cString: [112,111,105,110,116,0], encoding: .utf8)!
       var catalogx: Int = 5
      withUnsafeMutablePointer(to: &catalogx) { pointer in
    
      }
       var colorl: Bool = true
       var starw: Bool = false
      while (starw) {
         starw = !colorl
         break
      }
         starw = (starw ? !colorl : !starw)
      playingE = [catalogx]

        let photoBtn = UIButton(type: .custom)
   for _ in 0 ..< 2 {
      playingE = [1 / (Swift.max(6, starrym.count))]
   }
        photoBtn.setImage("common_back".toImage, for: .normal)
       var extN: String! = String(cString: [107,101,101,112,105,110,103,0], encoding: .utf8)!
       var pagee: [String: Any]! = [String(cString: [109,109,105,117,116,105,108,115,0], encoding: .utf8)!:45.0]
       _ = pagee
         pagee = ["\(pagee.count)": pagee.count & 1]
         extN = "\(pagee.values.count)"
         pagee = ["\(pagee.count)": 3]
      repeat {
         pagee = ["\(pagee.keys.count)": 2]
         if pagee.count == 3505187 {
            break
         }
      } while (pagee.count == 3505187) && ((5 ^ extN.count) < 2 && 1 < (pagee.keys.count ^ 5))
      for _ in 0 ..< 2 {
          var bottomH: Double = 2.0
         withUnsafeMutablePointer(to: &bottomH) { pointer in
                _ = pointer.pointee
         }
          var setupb: String! = String(cString: [119,101,108,115,101,110,99,100,101,109,111,0], encoding: .utf8)!
          var tempt: String! = String(cString: [108,97,110,103,117,97,103,101,115,0], encoding: .utf8)!
          var e_playerr: [String: Any]! = [String(cString: [97,112,112,108,105,101,100,0], encoding: .utf8)!:true]
         pagee["\(pagee.count)"] = 2 ^ e_playerr.keys.count
         bottomH -= (Double(tempt.count - Int(bottomH > 300670356.0 || bottomH < -300670356.0 ? 28.0 : bottomH)))
         setupb = "\(3 | e_playerr.keys.count)"
         tempt = "\(3)"
      }
      if pagee.count == 2 {
         extN = "\(extN.count - pagee.count)"
      }
      starrym = "\(starrym.count << (Swift.min(labs(1), 1)))"
        photoBtn.addTarget(self, action: #selector(onBack), for: .touchUpInside)
        return photoBtn
    }()

    private let titleLabel: UILabel = {
       var lineT: Float = 1.0
    var pwds: [String: Any]! = [String(cString: [118,112,111,105,110,116,0], encoding: .utf8)!:56, String(cString: [106,112,101,103,108,115,0], encoding: .utf8)!:16, String(cString: [100,101,112,101,110,100,101,110,116,0], encoding: .utf8)!:80]
   withUnsafeMutablePointer(to: &pwds) { pointer in
    
   }
       var micD: Double = 0.0
          var record9: [Any]! = [37, 35, 16]
         withUnsafeMutablePointer(to: &record9) { pointer in
                _ = pointer.pointee
         }
          var networkF: [Any]! = [77, 69, 60]
         micD += Double(networkF.count)
         record9 = [record9.count << (Swift.min(labs(1), 1))]
         micD += (Double(2 & Int(micD > 330210669.0 || micD < -330210669.0 ? 45.0 : micD)))
         micD /= Swift.max((Double(Int(micD > 114349569.0 || micD < -114349569.0 ? 89.0 : micD) ^ 3)), 1)
      lineT += (Float(1 % (Swift.max(5, Int(micD > 269893591.0 || micD < -269893591.0 ? 52.0 : micD)))))

        let v = UILabel()
      pwds["\(pwds.keys.count)"] = pwds.values.count
        v.font = .systemFont(ofSize: 18, weight: .semibold)
        v.textColor = .white
        v.textAlignment = .center
        return v
    }()

    private lazy var tableView: UITableView = {
       var mode0: Float = 2.0
    var providerl: String! = String(cString: [99,111,110,99,97,116,101,110,97,116,101,100,0], encoding: .utf8)!
    _ = providerl
   for _ in 0 ..< 1 {
       var mapL: Double = 0.0
       var credentialY: String! = String(cString: [115,101,97,108,97,110,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &credentialY) { pointer in
             _ = pointer.pointee
      }
       var indicator_: String! = String(cString: [109,115,114,108,101,0], encoding: .utf8)!
       var test4: Bool = false
       _ = test4
      repeat {
         mapL /= Swift.max(Double(1), 1)
         if 1162844.0 == mapL {
            break
         }
      } while (1162844.0 == mapL) && (2.55 == (mapL + 5.87) && mapL == 5.87)
       var value7: String! = String(cString: [116,97,112,112,105,110,103,0], encoding: .utf8)!
          var imgn: String! = String(cString: [114,101,117,112,108,111,97,100,0], encoding: .utf8)!
          var kindj: [Any]! = [45]
          var credentialh: Double = 0.0
         mapL -= Double(value7.count)
         imgn.append("\(((String(cString:[89,0], encoding: .utf8)!) == imgn ? imgn.count : (test4 ? 1 : 5)))")
         kindj = [2]
         credentialh += Double(3 + imgn.count)
      while (indicator_.count < credentialY.count) {
         indicator_ = "\(value7.count)"
         break
      }
      for _ in 0 ..< 1 {
          var commentsS: Bool = true
         withUnsafeMutablePointer(to: &commentsS) { pointer in
                _ = pointer.pointee
         }
          var support2: String! = String(cString: [99,111,112,121,116,101,115,116,0], encoding: .utf8)!
         mapL += Double(support2.count)
         commentsS = value7.hasPrefix("\(test4)")
      }
         indicator_ = "\(credentialY.count & 3)"
         value7.append("\(indicator_.count - credentialY.count)")
         value7 = "\(2 + credentialY.count)"
         mapL += Double(value7.count >> (Swift.min(labs(1), 5)))
      repeat {
         mapL -= Double(3 - value7.count)
         if 658233.0 == mapL {
            break
         }
      } while (!credentialY.hasPrefix("\(mapL)")) && (658233.0 == mapL)
      while ((1 % (Swift.max(4, credentialY.count))) > 4) {
         credentialY.append("\(credentialY.count)")
         break
      }
         test4 = mapL >= 18.62 && 12 >= value7.count
      providerl.append("\((Int(mapL > 375766210.0 || mapL < -375766210.0 ? 10.0 : mapL)))")
   }

        let displayView = UITableView(frame: .zero, style: .plain)
   repeat {
      mode0 -= (Float(Int(mode0 > 224402320.0 || mode0 < -224402320.0 ? 14.0 : mode0)))
      if mode0 == 130248.0 {
         break
      }
   } while (mode0 == 130248.0) && (mode0 <= mode0)
        displayView.backgroundColor = .clear
        displayView.separatorStyle = .none
        displayView.showsVerticalScrollIndicator = false
        displayView.contentInsetAdjustmentBehavior = .never
        displayView.dataSource = self
        displayView.delegate = self
        displayView.rowHeight = 76
        displayView.register(CSWelcomeCell.self, forCellReuseIdentifier: CSWelcomeCell.reuseID)
        return displayView
    }()

    private let emptyView = CSUserView()

    init(kind: CS_UserListKind) {
        self.kind = kind
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    private func handleAction(for user: CSResource, at indexPath: IndexPath) {
       var bootstrapW: Double = 2.0
      bootstrapW /= Swift.max((Double(1 - Int(bootstrapW > 284182550.0 || bootstrapW < -284182550.0 ? 29.0 : bootstrapW))), 3)

        switch kind {
        case .friendRequest, .followers:
            if CS_UserListStorage.isMutualFriend(userId: user.userId) {
                openChatRoom(peer: user)
            } else {
                CS_UserListStorage.acceptFriendRequest(userId: user.userId)
                view.makeToast("Following \(user.userName)")
                if let cell = tableView.cellForRow(at: indexPath) as? CSWelcomeCell {
                    cell.configure(user: user, actionStyle: .image("user_chat"))
                }
            }
        case .following:
            CS_UserListStorage.unfollow(userId: user.userId)
            view.makeToast("Unfollowed \(user.userName)")
            removeUser(at: indexPath)
        case .friends:
            openChatRoom(peer: user)
        case .blockList:
            CS_UserListStorage.unblock(userId: user.userId)
            view.makeToast("Removed \(user.userName)")
            removeUser(at: indexPath)
        }
    }


    override func viewDidLoad() {
       var applyM: String! = String(cString: [99,111,108,108,97,112,115,101,100,0], encoding: .utf8)!
    var enabledM: [String: Any]! = [String(cString: [97,115,99,0], encoding: .utf8)!:86, String(cString: [97,110,105,109,97,116,105,111,110,0], encoding: .utf8)!:74]
    var description_01: Double = 3.0
      applyM.append("\(enabledM.values.count >> (Swift.min(labs(1), 3)))")

      description_01 += (Double(Int(description_01 > 310295176.0 || description_01 < -310295176.0 ? 24.0 : description_01)))
        super.viewDidLoad()
       var hintr: String! = String(cString: [101,120,112,111,110,101,110,116,105,97,116,105,111,110,0], encoding: .utf8)!
       var trimmedo: String! = String(cString: [105,110,100,105,99,97,116,111,114,115,0], encoding: .utf8)!
       _ = trimmedo
         trimmedo.append("\(2)")
       var loadw: Double = 1.0
       _ = loadw
       var movieX: Double = 3.0
      if Double(trimmedo.count) <= loadw {
          var hintO: Int = 2
          var menu9: Int = 3
          var tabX: Float = 2.0
         loadw -= Double(2 << (Swift.min(3, trimmedo.count)))
         hintO |= 3
         menu9 -= (Int(movieX > 371570105.0 || movieX < -371570105.0 ? 19.0 : movieX))
         tabX /= Swift.max(Float(trimmedo.count), 3)
      }
         loadw += (Double(trimmedo.count + Int(movieX > 239238374.0 || movieX < -239238374.0 ? 64.0 : movieX)))
          var moreC: String! = String(cString: [109,115,117,98,0], encoding: .utf8)!
          var authorsD: [String: Any]! = [String(cString: [121,121,121,121,0], encoding: .utf8)!:53, String(cString: [104,100,99,100,0], encoding: .utf8)!:44, String(cString: [103,114,101,101,110,0], encoding: .utf8)!:29]
          var user8: Double = 5.0
         loadw += (Double(Int(user8 > 166738863.0 || user8 < -166738863.0 ? 65.0 : user8) ^ authorsD.values.count))
         moreC = "\((Int(movieX > 119628040.0 || movieX < -119628040.0 ? 43.0 : movieX)))"
      while ((movieX + Double(hintr.count)) <= 5.25 || (hintr.count - 5) <= 3) {
          var menu2: Float = 2.0
          var for_3V: String! = String(cString: [119,105,116,104,111,117,116,0], encoding: .utf8)!
          _ = for_3V
          var provideru: Double = 0.0
          var urls: String! = String(cString: [99,105,114,99,117,108,97,116,105,110,103,0], encoding: .utf8)!
          var override_092: String! = String(cString: [112,97,114,0], encoding: .utf8)!
          _ = override_092
         hintr.append("\(urls.count)")
         menu2 -= (Float(Int(movieX > 87150313.0 || movieX < -87150313.0 ? 33.0 : movieX)))
         for_3V = "\(urls.count << (Swift.min(labs(1), 4)))"
         provideru -= (Double(1 << (Swift.min(1, labs(Int(provideru > 139715963.0 || provideru < -139715963.0 ? 72.0 : provideru))))))
         override_092.append("\(3 << (Swift.min(4, override_092.count)))")
         break
      }
      enabledM = ["\(enabledM.values.count)": ((String(cString:[48,0], encoding: .utf8)!) == hintr ? hintr.count : enabledM.count)]
        titleLabel.text = kind.title
        setupUI()
    }


    @objc private func onBack() {
       var pwdN: String! = String(cString: [115,116,114,101,110,103,116,104,0], encoding: .utf8)!
    _ = pwdN
      pwdN.append("\((pwdN == (String(cString:[105,0], encoding: .utf8)!) ? pwdN.count : pwdN.count))")

        navigationController?.popViewController(animated: true)
    }


    private func setupUI() {
       var workI: String! = String(cString: [120,109,112,101,103,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      workI = "\(workI.count)"
   }

        view.addSubview(backButton)
        view.addSubview(titleLabel)
        view.addSubview(tableView)
        view.addSubview(emptyView)

        backButton.snp.makeConstraints { make in
            make.top.equalTo(view.safeAreaLayoutGuide).offset(8)
            make.left.equalToSuperview().offset(16)
            make.width.height.equalTo(40)
        }

        titleLabel.snp.makeConstraints { make in
            make.centerY.equalTo(backButton)
            make.centerX.equalToSuperview()
            make.left.greaterThanOrEqualTo(backButton.snp.right).offset(8)
            make.right.lessThanOrEqualToSuperview().offset(-16)
        }

        tableView.snp.makeConstraints { make in
            make.top.equalTo(backButton.snp.bottom).offset(20)
            make.left.right.bottom.equalToSuperview()
        }

        emptyView.snp.makeConstraints { make in
            make.center.equalTo(tableView)
        }
    }


    private func removeUser(at indexPath: IndexPath) {
       var trimmedG: Bool = false
   repeat {
      trimmedG = !trimmedG || trimmedG
      if trimmedG ? !trimmedG : trimmedG {
         break
      }
   } while (trimmedG && !trimmedG) && (trimmedG ? !trimmedG : trimmedG)

        guard users.indices.contains(indexPath.row) else { return }
        users.remove(at: indexPath.row)
        tableView.deleteRows(at: [indexPath], with: .automatic)
        emptyView.isHidden = !users.isEmpty
    }


    override func viewWillAppear(_ animated: Bool) {
       var welcomeB: String! = String(cString: [114,101,112,115,116,114,0], encoding: .utf8)!
       var supportL: Double = 5.0
      withUnsafeMutablePointer(to: &supportL) { pointer in
    
      }
       var fallbackS: String! = String(cString: [97,117,116,104,101,110,116,105,99,97,116,105,111,110,0], encoding: .utf8)!
          var greeting1: String! = String(cString: [102,114,111,109,0], encoding: .utf8)!
         fallbackS = "\(((String(cString:[84,0], encoding: .utf8)!) == greeting1 ? greeting1.count : fallbackS.count))"
      for _ in 0 ..< 3 {
         fallbackS.append("\((Int(supportL > 217730489.0 || supportL < -217730489.0 ? 32.0 : supportL)))")
      }
       var accessedx: [String: Any]! = [String(cString: [98,111,110,100,0], encoding: .utf8)!:45, String(cString: [114,101,98,97,108,97,110,99,101,0], encoding: .utf8)!:74]
       _ = accessedx
       var g_managerb: [String: Any]! = [String(cString: [110,117,108,108,112,97,99,107,101,116,0], encoding: .utf8)!:String(cString: [98,121,116,101,115,0], encoding: .utf8)!, String(cString: [115,97,102,101,0], encoding: .utf8)!:String(cString: [99,104,101,99,107,105,110,103,0], encoding: .utf8)!]
      repeat {
         supportL -= (Double(Int(supportL > 288343391.0 || supportL < -288343391.0 ? 44.0 : supportL)))
         if 2224841.0 == supportL {
            break
         }
      } while (2 > fallbackS.count) && (2224841.0 == supportL)
       var collectsu: Bool = false
      withUnsafeMutablePointer(to: &collectsu) { pointer in
    
      }
         fallbackS = "\(2 << (Swift.min(5, fallbackS.count)))"
         accessedx["\(collectsu)"] = ((collectsu ? 1 : 3) | g_managerb.keys.count)
         g_managerb = ["\(g_managerb.count)": 1]
      welcomeB.append("\((fallbackS == (String(cString:[73,0], encoding: .utf8)!) ? Int(supportL > 116182477.0 || supportL < -116182477.0 ? 41.0 : supportL) : fallbackS.count))")

        super.viewWillAppear(animated)
        (tabBarController as? CSPackageToolController)?.setCustomTabBarHidden(true)
        CSHome.shared.postAFD { result in
            switch result {
            case .success(_):
                self.reloadData()
            case .failure(_):
                self.reloadData()
            }
        }
    }


    override func viewWillDisappear(_ animated: Bool) {
       var appleO: [Any]! = [String(cString: [112,97,114,115,101,114,115,0], encoding: .utf8)!, String(cString: [97,105,109,100,0], encoding: .utf8)!]
    _ = appleO
      appleO.append(appleO.count)

        super.viewWillDisappear(animated)
        if isMovingFromParent || isBeingDismissed {
            (tabBarController as? CSPackageToolController)?.setCustomTabBarHidden(false)
        }
    }


    private func reloadData() {
       var barQ: Double = 1.0
       var finishedO: String! = String(cString: [100,105,110,102,0], encoding: .utf8)!
       var updatedr: Double = 2.0
       var profileI: String! = String(cString: [98,111,111,115,116,101,100,0], encoding: .utf8)!
         profileI = "\(3)"
      if finishedO.count < profileI.count {
         profileI.append("\((Int(updatedr > 315939149.0 || updatedr < -315939149.0 ? 6.0 : updatedr) >> (Swift.min(profileI.count, 5))))")
      }
         updatedr -= Double(3)
      if 1.56 == (updatedr / (Swift.max(Double(profileI.count), 2))) {
          var notifyv: String! = String(cString: [119,104,105,116,101,108,105,115,116,0], encoding: .utf8)!
          var extras3: Bool = true
          var savedj: String! = String(cString: [110,105,115,116,122,0], encoding: .utf8)!
         updatedr -= (Double(Int(updatedr > 12440219.0 || updatedr < -12440219.0 ? 99.0 : updatedr) + (extras3 ? 5 : 2)))
         notifyv = "\(finishedO.count + 3)"
         savedj = "\(notifyv.count)"
      }
          var priceY: String! = String(cString: [112,111,115,108,105,115,116,115,0], encoding: .utf8)!
         finishedO.append("\(finishedO.count)")
         priceY = "\((Int(updatedr > 310650179.0 || updatedr < -310650179.0 ? 10.0 : updatedr) ^ 2))"
       var back8: String! = String(cString: [118,120,119,111,114,107,115,0], encoding: .utf8)!
       var successd: [Any]! = [86, 4, 56]
       var create7: [Any]! = [9, 56]
         profileI.append("\(successd.count)")
      if 3 == (1 | back8.count) {
         back8.append("\(create7.count)")
      }
      barQ -= Double(2)

        users = CS_UserListStorage.users(for: kind)
        emptyView.isHidden = !users.isEmpty
        tableView.reloadData()
    }
}


extension CSFormOverrideController: UITableViewDataSource, UITableViewDelegate {


    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
       var register_kW: String! = String(cString: [105,102,97,115,116,0], encoding: .utf8)!
    var secondaryG: String! = String(cString: [116,105,109,105,110,103,115,97,102,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &secondaryG) { pointer in
          _ = pointer.pointee
   }
   if !register_kW.hasSuffix(secondaryG) {
       var date2: Double = 5.0
      repeat {
          var camera0: String! = String(cString: [121,111,110,108,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &camera0) { pointer in
                _ = pointer.pointee
         }
          var testK: Double = 4.0
          var statsq: [String: Any]! = [String(cString: [110,111,110,115,101,99,117,114,101,0], encoding: .utf8)!:91, String(cString: [115,119,105,116,99,104,105,110,103,0], encoding: .utf8)!:59]
          _ = statsq
          var headerT: String! = String(cString: [102,97,117,108,116,0], encoding: .utf8)!
         date2 += Double(statsq.count % 2)
         camera0.append("\((headerT == (String(cString:[115,0], encoding: .utf8)!) ? headerT.count : statsq.keys.count))")
         testK /= Swift.max(1, (Double(Int(date2 > 119014001.0 || date2 < -119014001.0 ? 70.0 : date2))))
         if date2 == 2009286.0 {
            break
         }
      } while (5.63 > date2) && (date2 == 2009286.0)
          var configsu: Double = 2.0
         withUnsafeMutablePointer(to: &configsu) { pointer in
    
         }
          var youP: Double = 0.0
         date2 /= Swift.max(Double(3), 4)
         configsu -= (Double(Int(configsu > 60495360.0 || configsu < -60495360.0 ? 62.0 : configsu) << (Swift.min(labs(Int(youP > 231735888.0 || youP < -231735888.0 ? 28.0 : youP)), 3))))
         youP /= Swift.max(5, (Double(Int(configsu > 346216221.0 || configsu < -346216221.0 ? 74.0 : configsu) - Int(date2 > 125857910.0 || date2 < -125857910.0 ? 53.0 : date2))))
       var followerK: [Any]! = [81, 28]
         followerK.append(followerK.count)
      register_kW = "\(2)"
   }

       var headerG: String! = String(cString: [105,115,112,111,114,116,0], encoding: .utf8)!
      repeat {
         headerG = "\(headerG.count)"
         if headerG == (String(cString:[55,112,100,111,120,53,98,101,0], encoding: .utf8)!) {
            break
         }
      } while (headerG != headerG) && (headerG == (String(cString:[55,112,100,111,120,53,98,101,0], encoding: .utf8)!))
          var raw9: [String: Any]! = [String(cString: [102,97,107,101,0], encoding: .utf8)!:90.0]
          var diamondx: String! = String(cString: [114,101,102,117,110,100,0], encoding: .utf8)!
          var emptyK: [Any]! = [String(cString: [99,109,97,112,0], encoding: .utf8)!, String(cString: [118,111,114,98,105,115,102,108,111,97,116,102,108,111,97,116,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &emptyK) { pointer in
    
         }
         headerG.append("\(headerG.count)")
         raw9["\(diamondx)"] = 2 ^ raw9.count
         diamondx.append("\(((String(cString:[51,0], encoding: .utf8)!) == headerG ? headerG.count : raw9.keys.count))")
         emptyK.append(emptyK.count)
         headerG = "\((headerG == (String(cString:[102,0], encoding: .utf8)!) ? headerG.count : headerG.count))"
      secondaryG.append("\(secondaryG.count)")
        tableView.deselectRow(at: indexPath, animated: true)
        let capturer = users[indexPath.row]
        navigationController?.pushViewController(
            CSCommentController(user: capturer, isFollowing: UserData.isFollowing(userId: capturer.userId)),
            animated: true
        )
    }


    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
       var homeC: [String: Any]! = [String(cString: [112,117,115,104,101,100,0], encoding: .utf8)!:String(cString: [113,117,97,110,116,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &homeC) { pointer in
          _ = pointer.pointee
   }
   repeat {
      homeC = ["\(homeC.values.count)": 3]
      if homeC.count == 4532573 {
         break
      }
   } while (homeC.count == 4532573) && ((homeC.values.count | 4) > 3 && 3 > (homeC.values.count | 4))

return         users.count
    }


    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
       var previewO: String! = String(cString: [97,108,116,101,114,97,98,108,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &previewO) { pointer in
    
   }
      previewO = "\(previewO.count << (Swift.min(labs(2), 5)))"

        guard let cell = tableView.dequeueReusableCell(
            withIdentifier: CSWelcomeCell.reuseID,
            for: indexPath
        ) as? CSWelcomeCell else {
            return UITableViewCell()
        }
        let capturer = users[indexPath.row]
        var rgb = kind.actionStyle
        if kind == .friendRequest || kind == .followers,
           CS_UserListStorage.isMutualFriend(userId: capturer.userId) {
            rgb = .image("user_chat")
        }
        cell.configure(user: capturer, actionStyle: rgb)
        cell.onActionTapped = { [weak self] in
            self?.handleAction(for: capturer, at: indexPath)
        }
        return cell
    }
}
