
import Foundation

import PhotosUI
import Toast_Swift
import UIKit

class CSPostController: CSConversationController {

    private var pendingAvatarPath: String?

    private lazy var backButton: UIButton = {
       var diamondE: Bool = false
    var extension_x43: String! = String(cString: [115,117,112,112,111,114,116,105,110,103,0], encoding: .utf8)!
      diamondE = extension_x43.count > 94 || !diamondE

        let photoBtn = UIButton(type: .custom)
      extension_x43 = "\(2)"
        photoBtn.setImage("common_back".toImage, for: .normal)
        photoBtn.addTarget(self, action: #selector(onBack), for: .touchUpInside)
        return photoBtn
    }()

    private let titleLabel: UILabel = {
       var loads: Int = 5
    var modely: Int = 5
   withUnsafeMutablePointer(to: &modely) { pointer in
          _ = pointer.pointee
   }
   repeat {
      loads -= modely
      if 3874056 == loads {
         break
      }
   } while (3874056 == loads) && (1 > loads)

        let v = UILabel()
       var runningC: [String: Any]! = [String(cString: [114,103,98,97,0], encoding: .utf8)!:String(cString: [100,105,115,112,97,116,99,104,105,110,103,0], encoding: .utf8)!, String(cString: [98,108,105,110,107,0], encoding: .utf8)!:String(cString: [108,105,98,112,104,111,110,101,110,117,109,98,101,114,0], encoding: .utf8)!, String(cString: [115,99,114,117,98,98,105,110,103,0], encoding: .utf8)!:String(cString: [103,97,105,110,115,0], encoding: .utf8)!]
       var wrapB: String! = String(cString: [114,101,97,100,99,98,0], encoding: .utf8)!
         wrapB = "\(runningC.values.count)"
      if runningC.values.count >= wrapB.count {
         runningC[wrapB] = runningC.keys.count % (Swift.max(3, 4))
      }
          var keyz: Double = 2.0
          var tabc: Double = 1.0
          var publishz: [Any]! = [87.0]
         wrapB.append("\((runningC.values.count & Int(tabc > 202746740.0 || tabc < -202746740.0 ? 68.0 : tabc)))")
         keyz -= (Double(publishz.count % (Swift.max(1, Int(keyz > 229092044.0 || keyz < -229092044.0 ? 22.0 : keyz)))))
         publishz = [(Int(keyz > 93777398.0 || keyz < -93777398.0 ? 72.0 : keyz))]
      for _ in 0 ..< 3 {
         wrapB.append("\(3)")
      }
         wrapB.append("\(1 * runningC.values.count)")
      if 4 >= (4 + wrapB.count) {
         runningC[wrapB] = wrapB.count
      }
      modely &= 1
        v.text = "Edit Profile"
        v.textColor = .white
        v.font = .systemFont(ofSize: 18, weight: .semibold)
        v.textAlignment = .center
        return v
    }()

    private lazy var avatarButton: UIButton = {
       var nowX: [String: Any]! = [String(cString: [97,100,100,105,110,103,0], encoding: .utf8)!:38, String(cString: [98,108,97,107,101,98,0], encoding: .utf8)!:80]
       var sideq: Int = 0
       _ = sideq
      if sideq >= sideq {
         sideq %= Swift.max(4, 3)
      }
          var rooma: String! = String(cString: [114,111,117,116,101,0], encoding: .utf8)!
          var speakerY: Bool = false
          var rangeo: String! = String(cString: [99,104,117,110,107,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &rangeo) { pointer in
                _ = pointer.pointee
         }
         sideq -= ((speakerY ? 5 : 2))
         rooma = "\(rooma.count & 2)"
         rangeo.append("\(3 << (Swift.min(labs(sideq), 1)))")
      for _ in 0 ..< 2 {
         sideq += 3 ^ sideq
      }
      nowX["\(sideq)"] = 2

        let photoBtn = UIButton(type: .custom)
        photoBtn.addTarget(self, action: #selector(onAvatarTapped), for: .touchUpInside)
        return photoBtn
    }()

    private let avatarImageView: UIImageView = {
       var recordsS: Double = 2.0
    var hintr: String! = String(cString: [119,114,105,116,101,99,108,101,97,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &hintr) { pointer in
          _ = pointer.pointee
   }
      recordsS /= Swift.max(2, (Double(hintr == (String(cString:[115,0], encoding: .utf8)!) ? Int(recordsS > 145762474.0 || recordsS < -145762474.0 ? 33.0 : recordsS) : hintr.count)))

        let v = UIImageView()
       var panelr: String! = String(cString: [119,101,108,115,0], encoding: .utf8)!
      while (panelr != panelr) {
         panelr.append("\(panelr.count)")
         break
      }
      repeat {
         panelr = "\(panelr.count >> (Swift.min(panelr.count, 3)))"
         if panelr.count == 1443428 {
            break
         }
      } while (panelr.hasSuffix(panelr)) && (panelr.count == 1443428)
         panelr = "\(panelr.count)"
      hintr = "\((Int(recordsS > 266318210.0 || recordsS < -266318210.0 ? 41.0 : recordsS) >> (Swift.min(hintr.count, 4))))"
        v.contentMode = .scaleAspectFill
        v.clipsToBounds = true
        v.layer.cornerRadius = 93 / 2
        v.backgroundColor = UIColor(hex: "#D4C4A8")
        v.isUserInteractionEnabled = false
        return v
    }()

    private let cameraImageView: UIImageView = {
       var homec: String! = String(cString: [117,118,109,118,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &homec) { pointer in
    
   }
    var unreadM: String! = String(cString: [110,97,116,105,111,110,97,108,0], encoding: .utf8)!
      homec.append("\(1 ^ homec.count)")

        let v = UIImageView(image: "info_camera".toImage)
      unreadM = "\(((String(cString:[76,0], encoding: .utf8)!) == unreadM ? unreadM.count : unreadM.count))"
        v.contentMode = .scaleAspectFit
        v.isUserInteractionEnabled = false
        return v
    }()

    private lazy var nameField = CSPostController.makeInputField(placeholder: "Your name")

    private lazy var bioTextView: UITextView = {
       var tableO: [String: Any]! = [String(cString: [104,101,97,100,112,104,111,110,101,0], encoding: .utf8)!:63, String(cString: [109,101,114,103,101,114,0], encoding: .utf8)!:90, String(cString: [100,116,111,97,0], encoding: .utf8)!:1]
      tableO = ["\(tableO.keys.count)": 2]

        let displayView = UITextView()
        displayView.font = .systemFont(ofSize: 15)
        displayView.textColor = .white
        displayView.backgroundColor = UIColor(hex: "#F3F7BB", alpha: 0.5)
        displayView.layer.cornerRadius = 16
        displayView.textContainerInset = UIEdgeInsets(top: 14, left: 10, bottom: 14, right: 10)
        displayView.autocapitalizationType = .sentences
        displayView.delegate = self
        return displayView
    }()

    private let bioPlaceholderLabel: UILabel = {
       var pwdA: String! = String(cString: [109,101,115,111,110,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      pwdA.append("\(pwdA.count % (Swift.max(2, 5)))")
   }

        let label = UILabel()
        label.text = "Your bio"
        label.font = .systemFont(ofSize: 15)
        label.textColor = UIColor.white.withAlphaComponent(0.85)
        return label
    }()

    private lazy var saveButton: UIButton = {
       var extK: String! = String(cString: [111,118,101,114,108,97,121,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      extK.append("\(extK.count)")
   }

        let photoBtn = UIButton(type: .custom)
        photoBtn.setImage("edit_save".toImage, for: .normal)
        photoBtn.contentHorizontalAlignment = .fill
        photoBtn.contentVerticalAlignment = .fill
        photoBtn.imageView?.contentMode = .scaleAspectFit
        photoBtn.addTarget(self, action: #selector(onSave), for: .touchUpInside)
        return photoBtn
    }()


    private func loadCurrentUser() {
       var y_imageW: String! = String(cString: [102,105,114,101,100,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      y_imageW = "\(3)"
   }

        guard let user = CSDeleteForm.shared.user else { return }
        nameField.text = user.userName
        bioTextView.text = user.signature
        pendingAvatarPath = user.avatarURL
        applyAvatarImage()
        updateBioPlaceholder()
    }


    override func viewWillDisappear(_ animated: Bool) {
       var m_layert: String! = String(cString: [112,97,103,101,110,117,109,98,101,114,0], encoding: .utf8)!
    var blockedy: Double = 4.0
      blockedy /= Swift.max(3, Double(2))

      m_layert.append("\(m_layert.count % 1)")
        super.viewWillDisappear(animated)
        if isMovingFromParent || isBeingDismissed {
            (tabBarController as? CSPackageToolController)?.setCustomTabBarHidden(false)
        }
    }


    @objc private func onSave() {
       var statusb: String! = String(cString: [102,105,108,101,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &statusb) { pointer in
    
   }
   while (statusb.count <= statusb.count) {
      statusb.append("\(statusb.count)")
      break
   }

        let top = nameField.text?.trimmingCharacters(in: .whitespacesAndNewlines) ?? ""
        let color = bioTextView.text.trimmingCharacters(in: .whitespacesAndNewlines)
        let textm = pendingAvatarPath
            ?? CSDeleteForm.shared.user?.avatarURL
            ?? "info_avatar"

        guard !top.isEmpty else {
            view.makeToast("Please enter your name")
            return
        }

        let extension_n = color.isEmpty ? "Personal signature~" : color
        guard CSDeleteForm.shared.updateProfile(
            userName: top,
            signature: extension_n,
            avatarURL: textm
        ) else {
            view.makeToast("Unable to save profile")
            return
        }

        view.makeToast("Profile saved")
        navigationController?.popViewController(animated: true)
    }


    private func applyAvatarImage() {
       var imagesv: String! = String(cString: [116,112,99,0], encoding: .utf8)!
    _ = imagesv
    var catalogv: [String: Any]! = [String(cString: [114,101,97,100,98,121,116,101,0], encoding: .utf8)!:String(cString: [99,108,99,112,0], encoding: .utf8)!, String(cString: [119,97,108,108,112,97,112,101,114,115,0], encoding: .utf8)!:String(cString: [101,113,117,97,108,0], encoding: .utf8)!, String(cString: [102,114,97,103,109,101,110,116,0], encoding: .utf8)!:String(cString: [117,110,105,119,103,116,0], encoding: .utf8)!]
    _ = catalogv
   if (catalogv.keys.count ^ imagesv.count) >= 1 && (catalogv.keys.count ^ 1) >= 4 {
       var parameterst: Double = 0.0
       var delayY: [Any]! = [5.0]
         parameterst -= (Double(delayY.count | Int(parameterst > 149448289.0 || parameterst < -149448289.0 ? 5.0 : parameterst)))
      repeat {
         delayY = [delayY.count << (Swift.min(labs(2), 4))]
         if delayY.count == 1533269 {
            break
         }
      } while (!delayY.contains { $0 as? Double == parameterst }) && (delayY.count == 1533269)
      repeat {
          var cachedy: Bool = true
         delayY.append((Int(parameterst > 124949451.0 || parameterst < -124949451.0 ? 74.0 : parameterst) << (Swift.min(3, labs(1)))))
         cachedy = 29.80 <= parameterst && 23 <= delayY.count
         if delayY.count == 2203570 {
            break
         }
      } while ((2 & delayY.count) > 2 || 1.71 > (parameterst / 2.32)) && (delayY.count == 2203570)
      repeat {
         parameterst -= (Double(Int(parameterst > 209675076.0 || parameterst < -209675076.0 ? 74.0 : parameterst)))
         if parameterst == 1846473.0 {
            break
         }
      } while (parameterst == 1846473.0) && (delayY.contains { $0 as? Double == parameterst })
      repeat {
         delayY.append(delayY.count)
         if 3472412 == delayY.count {
            break
         }
      } while (3472412 == delayY.count) && ((Int(parameterst > 234018332.0 || parameterst < -234018332.0 ? 86.0 : parameterst) / (Swift.max(delayY.count, 10))) >= 2)
         delayY = [1]
      imagesv.append("\(1)")
   }

        if let path = pendingAvatarPath, !path.isEmpty {
            avatarImageView.image = path.resourceFileImage ?? path.toImage
        } else {
            avatarImageView.image = "info_avatar".toImage
        }
        avatarImageView.backgroundColor = avatarImageView.image == nil
            ? UIColor(hex: "#D4C4A8") : .clear
    }


    private func updateBioPlaceholder() {
       var delegate_mtL: [String: Any]! = [String(cString: [108,105,98,115,115,104,0], encoding: .utf8)!:81, String(cString: [101,110,99,97,112,115,117,108,97,116,101,0], encoding: .utf8)!:74, String(cString: [97,115,115,105,103,110,101,100,0], encoding: .utf8)!:86]
    _ = delegate_mtL
      delegate_mtL["\(delegate_mtL.values.count)"] = 1 & delegate_mtL.values.count

        bioPlaceholderLabel.isHidden = !bioTextView.text.isEmpty
    }

    
    override func viewDidLoad() {
       var chromec: [String: Any]! = [String(cString: [109,105,100,105,0], encoding: .utf8)!:4, String(cString: [109,105,99,114,111,115,111,102,116,0], encoding: .utf8)!:71]
   withUnsafeMutablePointer(to: &chromec) { pointer in
    
   }
   while ((chromec.count & 3) < 1 || 1 < (3 & chromec.keys.count)) {
      chromec["\(chromec.values.count)"] = chromec.keys.count
      break
   }

        super.viewDidLoad()
        setupUI()
        loadCurrentUser()
    }


    private func setupUI() {
       var accessb: String! = String(cString: [115,104,97,114,100,0], encoding: .utf8)!
      accessb = "\(accessb.count ^ accessb.count)"

        view.addSubview(backButton)
        view.addSubview(titleLabel)
        view.addSubview(avatarButton)
        avatarButton.addSubview(avatarImageView)
        avatarButton.addSubview(cameraImageView)
        view.addSubview(nameField)
        view.addSubview(bioTextView)
        bioTextView.addSubview(bioPlaceholderLabel)
        view.addSubview(saveButton)

        backButton.snp.makeConstraints { make in
            make.top.equalTo(view.safeAreaLayoutGuide).offset(8)
            make.left.equalToSuperview().offset(16)
            make.width.height.equalTo(40)
        }

        titleLabel.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.centerY.equalTo(backButton)
            make.left.greaterThanOrEqualTo(backButton.snp.right).offset(8)
            make.right.lessThanOrEqualToSuperview().offset(-16)
        }

        avatarButton.snp.makeConstraints { make in
            make.top.equalTo(titleLabel.snp.bottom).offset(28)
            make.centerX.equalToSuperview()
            make.width.height.equalTo(93)
        }

        avatarImageView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        cameraImageView.snp.makeConstraints { make in
            make.center.equalToSuperview()
            make.width.height.equalTo(20)
        }

        nameField.snp.makeConstraints { make in
            make.top.equalTo(avatarButton.snp.bottom).offset(32)
            make.left.right.equalToSuperview().inset(16)
            make.height.equalTo(65)
        }

        bioTextView.snp.makeConstraints { make in
            make.top.equalTo(nameField.snp.bottom).offset(16)
            make.left.right.equalTo(nameField)
            make.height.equalTo(140)
        }

        bioPlaceholderLabel.snp.makeConstraints { make in
            make.top.equalToSuperview().offset(14)
            make.left.equalToSuperview().offset(15)
        }

        saveButton.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.width.equalTo(358)
            make.height.equalTo(60)
            make.bottom.equalTo(view.safeAreaLayoutGuide).offset(-16)
        }
    }


    @objc private func onBack() {
       var itemsD: [String: Any]! = [String(cString: [114,101,109,111,118,101,114,0], encoding: .utf8)!:67, String(cString: [106,112,101,103,100,115,112,0], encoding: .utf8)!:62, String(cString: [118,105,101,119,101,100,0], encoding: .utf8)!:8]
   while (1 <= (itemsD.count * itemsD.keys.count)) {
       var videoa: Bool = true
      for _ in 0 ..< 2 {
         videoa = (!videoa ? !videoa : !videoa)
      }
      repeat {
          var timerg: [Any]! = [22, 16]
          var parametersO: Bool = true
          _ = parametersO
         videoa = !videoa && timerg.count >= 72
         parametersO = timerg.count == 31
         if videoa ? !videoa : videoa {
            break
         }
      } while (videoa) && (videoa ? !videoa : videoa)
          var followersg: String! = String(cString: [100,101,114,105,118,97,116,105,118,101,0], encoding: .utf8)!
          var livev: [Any]! = [54, 10, 75]
          _ = livev
         videoa = 83 < livev.count
         followersg = "\(followersg.count)"
      itemsD = ["\(itemsD.count)": ((videoa ? 1 : 3) << (Swift.min(labs(3), 3)))]
      break
   }

        navigationController?.popViewController(animated: true)
    }


    @objc private func onAvatarTapped() {
       var starryg: Double = 1.0
    var pwdM: [Any]! = [62, 2, 91]
   withUnsafeMutablePointer(to: &pwdM) { pointer in
    
   }
   for _ in 0 ..< 2 {
      pwdM = [pwdM.count]
   }

   repeat {
      pwdM.append(2)
      if 72469 == pwdM.count {
         break
      }
   } while (72469 == pwdM.count) && (2.35 <= starryg)
        var vcs = PHPickerConfiguration(photoLibrary: .shared())
   repeat {
      starryg -= (Double(Int(starryg > 263299467.0 || starryg < -263299467.0 ? 55.0 : starryg) ^ 2))
      if starryg == 4851285.0 {
         break
      }
   } while (5.44 >= starryg) && (starryg == 4851285.0)
        vcs.filter = .images
        vcs.selectionLimit = 1
        let record = PHPickerViewController(configuration: vcs)
        record.delegate = self
        present(record, animated: true)
    }


    override func viewWillAppear(_ animated: Bool) {
       var warning7: Float = 1.0
   if 2.0 >= warning7 {
       var max_9J: String! = String(cString: [105,114,97,110,100,0], encoding: .utf8)!
       var loggedR: String! = String(cString: [99,97,112,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &loggedR) { pointer in
             _ = pointer.pointee
      }
       var screenr: Double = 2.0
       _ = screenr
       var createdr: String! = String(cString: [111,97,101,112,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &createdr) { pointer in
             _ = pointer.pointee
      }
      while (max_9J.count >= 3) {
         loggedR.append("\(loggedR.count << (Swift.min(labs(3), 2)))")
         break
      }
         max_9J = "\((Int(screenr > 384321206.0 || screenr < -384321206.0 ? 11.0 : screenr)))"
      while (max_9J.count <= 1) {
          var collectse: Float = 2.0
         max_9J.append("\(loggedR.count % 3)")
         collectse /= Swift.max(Float(loggedR.count ^ 3), 3)
         break
      }
      if loggedR.count == (Int(screenr > 97866965.0 || screenr < -97866965.0 ? 21.0 : screenr)) {
         screenr += (Double(Int(screenr > 295150472.0 || screenr < -295150472.0 ? 70.0 : screenr) ^ loggedR.count))
      }
         max_9J = "\(loggedR.count & 2)"
         createdr = "\(((String(cString:[85,0], encoding: .utf8)!) == createdr ? createdr.count : loggedR.count))"
         createdr.append("\((Int(screenr > 355125135.0 || screenr < -355125135.0 ? 85.0 : screenr) / (Swift.max(3, 3))))")
      while (loggedR.hasPrefix("\(max_9J.count)")) {
         loggedR = "\(((String(cString:[68,0], encoding: .utf8)!) == createdr ? max_9J.count : createdr.count))"
         break
      }
       var runningP: Float = 2.0
          var imgz: Double = 0.0
         createdr = "\((Int(imgz > 143652534.0 || imgz < -143652534.0 ? 89.0 : imgz) / (Swift.max(8, Int(runningP > 291423428.0 || runningP < -291423428.0 ? 94.0 : runningP)))))"
      repeat {
         max_9J = "\(max_9J.count)"
         if 4626065 == max_9J.count {
            break
         }
      } while (4626065 == max_9J.count) && ((max_9J.count + 2) == 3)
         createdr = "\(loggedR.count >> (Swift.min(1, max_9J.count)))"
      warning7 += Float(createdr.count)
   }

        super.viewWillAppear(animated)
        (tabBarController as? CSPackageToolController)?.setCustomTabBarHidden(true)
    }


    private static func makeInputField(placeholder: String) -> UITextField {
       var settingsG: String! = String(cString: [113,117,97,100,114,0], encoding: .utf8)!
    var tabF: Double = 2.0
      tabF += (Double(settingsG == (String(cString:[120,0], encoding: .utf8)!) ? Int(tabF > 199260247.0 || tabF < -199260247.0 ? 44.0 : tabF) : settingsG.count))

        let unread = UITextField()
        unread.font = .systemFont(ofSize: 15)
        unread.textColor = .white
        unread.backgroundColor = UIColor(hex: "#F3F7BB", alpha: 0.5)
        unread.layer.cornerRadius = 16
        unread.leftView = UIView(frame: CGRect(x: 0, y: 0, width: 14, height: 1))
        unread.leftViewMode = .always
        unread.attributedPlaceholder = NSAttributedString(
            string: placeholder,
            attributes: [.foregroundColor: UIColor.white.withAlphaComponent(0.85)]
        )
        return unread
    }
}


extension CSPostController: UITextViewDelegate {


    func textViewDidChange(_ textView: UITextView) {
       var controlV: String! = String(cString: [108,111,117,112,101,0], encoding: .utf8)!
    var override_4x: String! = String(cString: [112,114,101,100,120,108,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &override_4x) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 2 {
      override_4x.append("\(controlV.count - override_4x.count)")
   }
   for _ in 0 ..< 1 {
      override_4x.append("\(override_4x.count ^ controlV.count)")
   }

        updateBioPlaceholder()
    }
}


extension CSPostController: PHPickerViewControllerDelegate {


    func picker(_ picker: PHPickerViewController, didFinishPicking results: [PHPickerResult]) {
       var stored1: String! = String(cString: [106,100,99,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &stored1) { pointer in
    
   }
   if !stored1.contains("\(stored1.count)") {
       var linkg: String! = String(cString: [103,101,116,99,0], encoding: .utf8)!
       var targetl: Bool = false
      withUnsafeMutablePointer(to: &targetl) { pointer in
    
      }
       var fileB: String! = String(cString: [99,104,101,99,107,112,111,105,110,116,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &fileB) { pointer in
             _ = pointer.pointee
      }
       var buttonsk: Double = 4.0
       var settingsK: Double = 1.0
      withUnsafeMutablePointer(to: &settingsK) { pointer in
             _ = pointer.pointee
      }
      if !targetl {
          var finishc: [Any]! = [87, 48]
         settingsK -= Double(linkg.count)
         finishc = [(1 >> (Swift.min(labs(Int(settingsK > 64752005.0 || settingsK < -64752005.0 ? 78.0 : settingsK)), 4)))]
      }
         settingsK -= Double(2 | linkg.count)
         targetl = linkg == fileB
       var option8: String! = String(cString: [115,116,114,111,107,101,114,0], encoding: .utf8)!
          var default_d6: String! = String(cString: [112,114,111,103,114,97,109,115,0], encoding: .utf8)!
         option8.append("\((Int(buttonsk > 19519802.0 || buttonsk < -19519802.0 ? 85.0 : buttonsk) & 2))")
         default_d6.append("\(default_d6.count / (Swift.max(7, option8.count)))")
         settingsK -= (Double(3 ^ Int(settingsK > 373625178.0 || settingsK < -373625178.0 ? 74.0 : settingsK)))
      if 2 == (option8.count >> (Swift.min(labs(1), 1))) || 1 == (option8.count - Int(buttonsk > 212970530.0 || buttonsk < -212970530.0 ? 79.0 : buttonsk)) {
          var existingu: Float = 3.0
         withUnsafeMutablePointer(to: &existingu) { pointer in
    
         }
          var videoh: Float = 3.0
         withUnsafeMutablePointer(to: &videoh) { pointer in
    
         }
          var collectionY: String! = String(cString: [104,111,117,114,115,0], encoding: .utf8)!
          var inverseC: [Any]! = [String(cString: [115,117,112,112,114,101,115,115,0], encoding: .utf8)!, String(cString: [99,117,115,116,111,109,105,122,101,0], encoding: .utf8)!, String(cString: [117,115,114,99,0], encoding: .utf8)!]
          var inputX: Bool = true
         option8.append("\(fileB.count ^ 3)")
         existingu -= Float(2)
         videoh -= (Float((inputX ? 4 : 5)))
         collectionY.append("\(((targetl ? 1 : 2) & (inputX ? 5 : 3)))")
         inverseC.append((inverseC.count ^ (targetl ? 3 : 2)))
      }
         fileB = "\(linkg.count & option8.count)"
          var workx: Double = 0.0
          var movieo: Int = 5
         targetl = !fileB.hasPrefix("\(targetl)")
         workx *= Double(1)
         movieo ^= (1 - Int(buttonsk > 342440719.0 || buttonsk < -342440719.0 ? 82.0 : buttonsk))
         settingsK /= Swift.max(1, (Double(Int(settingsK > 57605647.0 || settingsK < -57605647.0 ? 22.0 : settingsK) ^ option8.count)))
         targetl = option8 == (String(cString:[71,0], encoding: .utf8)!)
      if linkg.count > 4 {
          var fileT: Double = 0.0
          var accessedQ: String! = String(cString: [115,101,114,105,102,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &accessedQ) { pointer in
    
         }
          var formatterO: String! = String(cString: [97,117,116,104,111,114,105,122,101,114,0], encoding: .utf8)!
          _ = formatterO
         linkg.append("\((Int(buttonsk > 289819506.0 || buttonsk < -289819506.0 ? 33.0 : buttonsk)))")
         fileT -= Double(1)
         accessedQ = "\(((targetl ? 2 : 3) << (Swift.min(labs(Int(buttonsk > 207565311.0 || buttonsk < -207565311.0 ? 28.0 : buttonsk)), 3))))"
         formatterO.append("\(3)")
      }
          var wrapF: [String: Any]! = [String(cString: [100,101,97,100,105,110,101,0], encoding: .utf8)!:86, String(cString: [105,108,98,99,100,97,116,97,0], encoding: .utf8)!:70]
          var documents2: String! = String(cString: [108,111,103,103,105,110,103,0], encoding: .utf8)!
          var string5: Int = 1
          _ = string5
         targetl = (fileB.count ^ documents2.count) < 33
         wrapF[fileB] = ((String(cString:[106,0], encoding: .utf8)!) == fileB ? fileB.count : string5)
         string5 /= Swift.max(((targetl ? 5 : 5) ^ Int(buttonsk > 156787662.0 || buttonsk < -156787662.0 ? 23.0 : buttonsk)), 4)
         fileB.append("\(((String(cString:[112,0], encoding: .utf8)!) == linkg ? Int(settingsK > 260640229.0 || settingsK < -260640229.0 ? 82.0 : settingsK) : linkg.count))")
      for _ in 0 ..< 3 {
         buttonsk += Double(3 - linkg.count)
      }
      stored1 = "\(((targetl ? 5 : 2) & 1))"
   }

        picker.dismiss(animated: true)
        guard let provider = results.first?.itemProvider,
              provider.canLoadObject(ofClass: UIImage.self) else { return }

        provider.loadObject(ofClass: UIImage.self) { [weak self] object, _ in
            guard let self, let image = object as? UIImage else { return }
            DispatchQueue.main.async {
                if let path = CSDeleteForm.shared.saveAvatarImage(image) {
                    self.pendingAvatarPath = path
                    self.avatarImageView.image = image
                    self.avatarImageView.backgroundColor = .clear
                }
            }
        }
    }
}
