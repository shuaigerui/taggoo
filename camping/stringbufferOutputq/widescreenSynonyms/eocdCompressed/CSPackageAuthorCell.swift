
import Foundation

import UIKit

final class CSPackageAuthorCell: UICollectionViewCell {

    static let reuseID = "CSPackageAuthorCell"

    private var coverVideoPath: String?

    private let coverImageView: UIImageView = {
       var displayK: Float = 0.0
    _ = displayK
    var legacyw: Int = 2
    _ = legacyw
      legacyw >>= Swift.min(2, labs((Int(displayK > 285129918.0 || displayK < -285129918.0 ? 23.0 : displayK))))

        let v = UIImageView()
      legacyw |= (Int(displayK > 352117596.0 || displayK < -352117596.0 ? 12.0 : displayK))
        v.contentMode = .scaleAspectFill
        v.clipsToBounds = true
        v.layer.cornerRadius = 16
        return v
    }()

    private let gradientView: UIView = {
       var formatter1: Int = 0
   withUnsafeMutablePointer(to: &formatter1) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 1 {
      formatter1 -= formatter1 << (Swift.min(labs(3), 4))
   }

        let v = UIView()
        v.layer.cornerRadius = 16
        v.clipsToBounds = true
        return v
    }()

    private let viewerBadge: UIView = {
       var colorsZ: [String: Any]! = [String(cString: [115,101,110,116,101,110,99,101,0], encoding: .utf8)!:13, String(cString: [100,118,98,116,120,116,0], encoding: .utf8)!:69]
   if !colorsZ.keys.contains("\(colorsZ.values.count)") {
      colorsZ["\(colorsZ.values.count)"] = colorsZ.count << (Swift.min(labs(2), 1))
   }

        let v = UIView()
        v.backgroundColor = UIColor.black.withAlphaComponent(0.45)
        v.layer.cornerRadius = 12
        return v
    }()

    private let peopleIconView: UIImageView = {
       var urli: String! = String(cString: [102,105,108,116,101,114,102,0], encoding: .utf8)!
    _ = urli
   repeat {
      urli = "\(urli.count % (Swift.max(urli.count, 7)))"
      if (String(cString:[115,50,51,113,117,95,114,57,100,0], encoding: .utf8)!) == urli {
         break
      }
   } while ((String(cString:[115,50,51,113,117,95,114,57,100,0], encoding: .utf8)!) == urli) && (urli.count >= urli.count)

        let v = UIImageView(image: "discover_people".toImage)
        v.contentMode = .scaleAspectFit
        return v
    }()

    private let viewerLabel: UILabel = {
       var displayr: String! = String(cString: [109,101,109,100,101,98,117,103,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
       var relativea: String! = String(cString: [114,99,118,100,0], encoding: .utf8)!
       _ = relativea
       var recordV: String! = String(cString: [114,101,115,101,116,98,117,102,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
         recordV.append("\(relativea.count + 1)")
      }
       var backgrounda: String! = String(cString: [115,111,117,110,0], encoding: .utf8)!
       var listL: String! = String(cString: [99,111,109,112,108,105,97,110,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &listL) { pointer in
    
      }
         relativea = "\(recordV.count)"
      if backgrounda.count <= 3 {
         listL = "\((relativea == (String(cString:[120,0], encoding: .utf8)!) ? relativea.count : listL.count))"
      }
      if relativea == recordV {
         recordV.append("\(2)")
      }
         recordV = "\(backgrounda.count * 1)"
      displayr.append("\(displayr.count % (Swift.max(relativea.count, 8)))")
   }

        let v = UILabel()
        v.font = .systemFont(ofSize: 12, weight: .semibold)
        v.textColor = .white
        return v
    }()

    private let titleLabel: UILabel = {
       var matchedc: Double = 5.0
    var successR: String! = String(cString: [111,118,101,114,114,105,100,105,110,103,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &successR) { pointer in
          _ = pointer.pointee
   }
    var reportedW: Bool = true
   repeat {
      successR = "\((Int(matchedc > 8866303.0 || matchedc < -8866303.0 ? 10.0 : matchedc) & successR.count))"
      if successR.count == 1265303 {
         break
      }
   } while (successR.count == 1265303) && (2.88 < (5.91 * matchedc) && 1.7 < (matchedc * 5.91))

        let v = UILabel()
      reportedW = (33 >= ((!reportedW ? successR.count : 33) << (Swift.min(successR.count, 2))))
        v.font = .systemFont(ofSize: 13, weight: .semibold)
        v.textColor = .white
        v.numberOfLines = 2
        return v
    }()

    override init(frame: CGRect) {
        super.init(frame: frame)
        setupUI()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    override func prepareForReuse() {
       var thumbF: Double = 1.0
   repeat {
       var sendera: Int = 0
      withUnsafeMutablePointer(to: &sendera) { pointer in
    
      }
       var gem0: String! = String(cString: [99,111,101,102,0], encoding: .utf8)!
       var movier: String! = String(cString: [105,115,114,101,97,100,111,110,108,121,0], encoding: .utf8)!
       _ = movier
       var encodedv: String! = String(cString: [100,105,115,112,97,116,99,104,0], encoding: .utf8)!
       _ = encodedv
       var likedz: String! = String(cString: [117,110,98,97,110,0], encoding: .utf8)!
         sendera %= Swift.max((encodedv == (String(cString:[79,0], encoding: .utf8)!) ? encodedv.count : gem0.count), 1)
      while (1 == (movier.count / (Swift.max(8, sendera))) || 1 == (movier.count / (Swift.max(9, sendera)))) {
          var resourceH: [String: Any]! = [String(cString: [97,98,115,116,114,97,99,116,0], encoding: .utf8)!:38, String(cString: [109,97,105,108,0], encoding: .utf8)!:53, String(cString: [99,112,117,105,100,0], encoding: .utf8)!:54]
          _ = resourceH
          var target3: Int = 5
         movier.append("\(movier.count)")
         resourceH = ["\(target3)": 2 >> (Swift.min(1, labs(target3)))]
         break
      }
         gem0 = "\(gem0.count >> (Swift.min(2, labs(sendera))))"
      repeat {
         likedz.append("\(((String(cString:[79,0], encoding: .utf8)!) == gem0 ? gem0.count : likedz.count))")
         if likedz == (String(cString:[109,49,114,111,116,95,106,98,52,98,0], encoding: .utf8)!) {
            break
         }
      } while (movier != likedz) && (likedz == (String(cString:[109,49,114,111,116,95,106,98,52,98,0], encoding: .utf8)!))
      repeat {
          var gallerye: String! = String(cString: [109,111,100,105,102,105,101,114,115,0], encoding: .utf8)!
          var didU: Bool = false
          var stateq: String! = String(cString: [108,97,116,105,110,0], encoding: .utf8)!
         gem0.append("\(stateq.count)")
         gallerye.append("\(stateq.count)")
         didU = 60 == gem0.count
         if gem0.count == 890446 {
            break
         }
      } while (sendera < 1) && (gem0.count == 890446)
         sendera += likedz.count * 1
      repeat {
          var videox: Bool = true
          _ = videox
          var catalogA: String! = String(cString: [115,112,97,99,101,114,115,0], encoding: .utf8)!
          var roomM: String! = String(cString: [111,114,103,0], encoding: .utf8)!
         sendera %= Swift.max(gem0.count, 1)
         videox = sendera <= movier.count
         catalogA = "\(catalogA.count)"
         roomM.append("\(gem0.count)")
         if sendera == 3151935 {
            break
         }
      } while (likedz.count > sendera) && (sendera == 3151935)
         gem0.append("\(likedz.count)")
         gem0 = "\(sendera)"
         movier.append("\(gem0.count)")
      for _ in 0 ..< 3 {
          var hangR: String! = String(cString: [108,117,109,98,101,114,106,97,99,107,0], encoding: .utf8)!
         gem0 = "\(sendera)"
         hangR.append("\((encodedv == (String(cString:[117,0], encoding: .utf8)!) ? likedz.count : encodedv.count))")
      }
      while (4 == movier.count) {
         likedz.append("\(2)")
         break
      }
      while (!encodedv.contains("\(likedz.count)")) {
         likedz.append("\(encodedv.count / (Swift.max(gem0.count, 10)))")
         break
      }
      while (5 > (1 ^ likedz.count)) {
         likedz = "\(movier.count - gem0.count)"
         break
      }
      while (!likedz.contains(movier)) {
         likedz = "\(movier.count ^ gem0.count)"
         break
      }
      thumbF += Double(1 * gem0.count)
      if thumbF == 3926307.0 {
         break
      }
   } while (thumbF == 3926307.0) && ((1.98 + thumbF) < 5.79)

        super.prepareForReuse()
        coverVideoPath = nil
        coverImageView.image = nil
        coverImageView.backgroundColor = UIColor(hex: "#4A5A42")
    }


    private func setupUI() {
       var accessede: Int = 5
   withUnsafeMutablePointer(to: &accessede) { pointer in
    
   }
    var dimR: String! = String(cString: [105,109,112,114,101,115,115,105,111,110,0], encoding: .utf8)!
       var headerJ: String! = String(cString: [116,114,101,97,100,0], encoding: .utf8)!
       _ = headerJ
       var i_badgeQ: Double = 4.0
      while ((i_badgeQ * 2.89) <= 5.18 || 1 <= (Int(i_badgeQ > 52474311.0 || i_badgeQ < -52474311.0 ? 94.0 : i_badgeQ) * 4)) {
         headerJ.append("\((headerJ == (String(cString:[50,0], encoding: .utf8)!) ? headerJ.count : Int(i_badgeQ > 269498648.0 || i_badgeQ < -269498648.0 ? 69.0 : i_badgeQ)))")
         break
      }
      if !headerJ.contains("\(i_badgeQ)") {
         headerJ.append("\((Int(i_badgeQ > 50369514.0 || i_badgeQ < -50369514.0 ? 87.0 : i_badgeQ) ^ 3))")
      }
          var state4: String! = String(cString: [114,101,108,111,97,100,101,114,0], encoding: .utf8)!
          _ = state4
          var toggleL: String! = String(cString: [117,114,98,103,0], encoding: .utf8)!
          var notifyo: [Any]! = [23, 83, 54]
         headerJ = "\((headerJ.count % (Swift.max(4, Int(i_badgeQ > 290134400.0 || i_badgeQ < -290134400.0 ? 14.0 : i_badgeQ)))))"
         state4 = "\((Int(i_badgeQ > 267552717.0 || i_badgeQ < -267552717.0 ? 31.0 : i_badgeQ) << (Swift.min(notifyo.count, 2))))"
         toggleL.append("\(3)")
         notifyo = [(toggleL == (String(cString:[112,0], encoding: .utf8)!) ? toggleL.count : Int(i_badgeQ > 181588743.0 || i_badgeQ < -181588743.0 ? 91.0 : i_badgeQ))]
         i_badgeQ += (Double(Int(i_badgeQ > 113459448.0 || i_badgeQ < -113459448.0 ? 100.0 : i_badgeQ) | 1))
      while (3.93 <= (Double(headerJ.count) - i_badgeQ) && 1 <= (headerJ.count - Int(i_badgeQ > 157998394.0 || i_badgeQ < -157998394.0 ? 24.0 : i_badgeQ))) {
         headerJ.append("\((Int(i_badgeQ > 113286043.0 || i_badgeQ < -113286043.0 ? 55.0 : i_badgeQ)))")
         break
      }
      if !headerJ.hasPrefix("\(i_badgeQ)") {
         headerJ = "\(((String(cString:[87,0], encoding: .utf8)!) == headerJ ? Int(i_badgeQ > 64052424.0 || i_badgeQ < -64052424.0 ? 17.0 : i_badgeQ) : headerJ.count))"
      }
      accessede += headerJ.count

   for _ in 0 ..< 3 {
       var diamondi: Double = 2.0
       var matchedS: [Any]! = [42]
      withUnsafeMutablePointer(to: &matchedS) { pointer in
             _ = pointer.pointee
      }
       var pnew_am: Float = 2.0
      repeat {
         pnew_am -= Float(matchedS.count)
         if 3457512.0 == pnew_am {
            break
         }
      } while (5 <= matchedS.count) && (3457512.0 == pnew_am)
      while (!matchedS.contains { $0 as? Float == pnew_am }) {
         matchedS.append(matchedS.count >> (Swift.min(labs(1), 5)))
         break
      }
         matchedS.append((3 >> (Swift.min(labs(Int(pnew_am > 173754652.0 || pnew_am < -173754652.0 ? 38.0 : pnew_am)), 5))))
         matchedS.append((Int(pnew_am > 222981978.0 || pnew_am < -222981978.0 ? 59.0 : pnew_am)))
      if 1 <= (1 - matchedS.count) || (Int(pnew_am > 208001520.0 || pnew_am < -208001520.0 ? 42.0 : pnew_am) - matchedS.count) <= 1 {
         matchedS.append(matchedS.count + 3)
      }
         diamondi /= Swift.max(Double(2), 1)
      repeat {
         pnew_am -= Float(3)
         if 4555841.0 == pnew_am {
            break
         }
      } while (4555841.0 == pnew_am) && ((2.8 - pnew_am) <= 3.82)
         diamondi += (Double(Int(pnew_am > 357706889.0 || pnew_am < -357706889.0 ? 35.0 : pnew_am) % (Swift.max(4, matchedS.count))))
      if 3.59 == pnew_am {
         diamondi -= Double(3)
      }
      dimR = "\(2)"
   }
        contentView.addSubview(coverImageView)
        contentView.addSubview(gradientView)
        contentView.addSubview(viewerBadge)
        viewerBadge.addSubview(peopleIconView)
        viewerBadge.addSubview(viewerLabel)
        contentView.addSubview(titleLabel)

        coverImageView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        gradientView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        viewerBadge.snp.makeConstraints { make in
            make.top.left.equalToSuperview().offset(10)
            make.height.equalTo(24)
        }

        peopleIconView.snp.makeConstraints { make in
            make.left.equalToSuperview().offset(6)
            make.centerY.equalToSuperview()
            make.width.height.equalTo(16)
        }

        viewerLabel.snp.makeConstraints { make in
            make.left.equalTo(peopleIconView.snp.right).offset(4)
            make.right.equalToSuperview().offset(-8)
            make.centerY.equalToSuperview()
        }

        titleLabel.snp.makeConstraints { make in
            make.left.right.equalToSuperview().inset(10)
            make.bottom.equalToSuperview().offset(-10)
        }
    }


    override func layoutSubviews() {
       var folderv: Int = 0
   withUnsafeMutablePointer(to: &folderv) { pointer in
    
   }
   if 4 >= folderv {
       var idsn: String! = String(cString: [98,97,115,101,0], encoding: .utf8)!
       _ = idsn
       var mapX: String! = String(cString: [99,104,101,99,107,109,97,114,107,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &mapX) { pointer in
    
      }
       var closeu: String! = String(cString: [120,112,97,108,100,118,0], encoding: .utf8)!
       var warningg: String! = String(cString: [102,105,116,122,0], encoding: .utf8)!
      repeat {
          var extrasF: Int = 5
          _ = extrasF
          var chrome1: [Any]! = [16, 44]
          var recordsM: String! = String(cString: [102,97,115,116,116,101,115,116,0], encoding: .utf8)!
          var appleM: String! = String(cString: [115,119,105,112,101,0], encoding: .utf8)!
          _ = appleM
          var with_pzl: [String: Any]! = [String(cString: [97,107,97,114,111,115,0], encoding: .utf8)!:62, String(cString: [115,116,114,103,108,111,98,0], encoding: .utf8)!:65, String(cString: [112,105,99,116,117,114,101,115,0], encoding: .utf8)!:94]
          _ = with_pzl
         mapX = "\(((String(cString:[53,0], encoding: .utf8)!) == idsn ? idsn.count : with_pzl.values.count))"
         extrasF /= Swift.max(2, closeu.count - 3)
         chrome1.append(((String(cString:[65,0], encoding: .utf8)!) == appleM ? recordsM.count : appleM.count))
         recordsM = "\((appleM == (String(cString:[112,0], encoding: .utf8)!) ? extrasF : appleM.count))"
         if (String(cString:[100,105,107,50,57,0], encoding: .utf8)!) == mapX {
            break
         }
      } while ((String(cString:[100,105,107,50,57,0], encoding: .utf8)!) == mapX) && (2 == mapX.count)
          var edit7: Float = 5.0
          var finishedW: [Any]! = [58, 28, 90]
          var i_managerP: [String: Any]! = [String(cString: [100,105,102,102,105,110,103,0], encoding: .utf8)!:String(cString: [101,120,116,101,110,115,105,98,108,101,0], encoding: .utf8)!, String(cString: [105,110,105,116,105,97,116,101,100,0], encoding: .utf8)!:String(cString: [114,101,116,97,105,110,0], encoding: .utf8)!, String(cString: [104,105,110,116,105,110,103,0], encoding: .utf8)!:String(cString: [99,111,108,0], encoding: .utf8)!]
          _ = i_managerP
         idsn = "\((Int(edit7 > 392643054.0 || edit7 < -392643054.0 ? 26.0 : edit7) % (Swift.max(warningg.count, 10))))"
         finishedW.append((idsn == (String(cString:[109,0], encoding: .utf8)!) ? mapX.count : idsn.count))
         i_managerP["\(finishedW.count)"] = i_managerP.count / (Swift.max(1, 6))
      if 5 == mapX.count && idsn == String(cString:[56,0], encoding: .utf8)! {
          var transitionI: Int = 1
          var imgD: Int = 3
         mapX.append("\(imgD)")
         transitionI |= (closeu == (String(cString:[87,0], encoding: .utf8)!) ? imgD : closeu.count)
      }
      while (closeu.count >= idsn.count) {
          var joinC: String! = String(cString: [117,110,109,97,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &joinC) { pointer in
                _ = pointer.pointee
         }
          var replies2: [Any]! = [1, 9, 96]
         idsn = "\((warningg == (String(cString:[52,0], encoding: .utf8)!) ? warningg.count : mapX.count))"
         joinC = "\(((String(cString:[114,0], encoding: .utf8)!) == idsn ? idsn.count : joinC.count))"
         replies2 = [warningg.count * closeu.count]
         break
      }
      repeat {
         warningg.append("\(mapX.count * idsn.count)")
         if 2456996 == warningg.count {
            break
         }
      } while (warningg.count > 3) && (2456996 == warningg.count)
      folderv += 2
   }

        super.layoutSubviews()
        gradientView.layer.sublayers?.removeAll(where: { $0 is CAGradientLayer })
        let bottom = CAGradientLayer()
        bottom.colors = [
            UIColor.clear.cgColor,
            UIColor.black.withAlphaComponent(0.55).cgColor
        ]
        bottom.locations = [0.4, 1.0]
        bottom.frame = gradientView.bounds
        gradientView.layer.insertSublayer(bottom, at: 0)
    }


    func configure(with item: CSWelcomeRegisterItem) {
       var q_counta: String! = String(cString: [115,111,98,105,110,100,0], encoding: .utf8)!
   repeat {
      q_counta.append("\((q_counta == (String(cString:[113,0], encoding: .utf8)!) ? q_counta.count : q_counta.count))")
      if (String(cString:[106,108,49,112,114,118,53,117,0], encoding: .utf8)!) == q_counta {
         break
      }
   } while (q_counta.hasSuffix("\(q_counta.count)")) && ((String(cString:[106,108,49,112,114,118,53,117,0], encoding: .utf8)!) == q_counta)

        coverVideoPath = item.videoPath
        viewerLabel.text = "\(item.viewerCount)"
        titleLabel.text = item.title

        if let cached = CS_VideoThumbnail.cachedImage(forVideoPath: item.videoPath) {
            coverImageView.image = cached
            coverImageView.backgroundColor = .clear
            return
        }

        coverImageView.image = nil
        coverImageView.backgroundColor = UIColor(hex: "#4A5A42")
        CS_VideoThumbnail.loadFirstFrame(forVideoPath: item.videoPath) { [weak self] image in
            guard let self, self.coverVideoPath == item.videoPath else { return }
            self.coverImageView.image = image
            self.coverImageView.backgroundColor = image == nil ? UIColor(hex: "#4A5A42") : .clear
        }
    }
}
