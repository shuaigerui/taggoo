
import Foundation

import AVFoundation
import UIKit

final class CSGalleryLaunchController: CSConversationController {

    private let postModel: CSMenu
    private var player: AVPlayer?
    private var playerLayer: AVPlayerLayer?
    private var isPlaying = false

    private let playerContainerView: UIView = {
       var buttonsN: String! = String(cString: [102,109,117,108,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      buttonsN = "\(buttonsN.count | 2)"
   }

        let v = UIView()
        v.backgroundColor = .black
        return v
    }()

    private lazy var backButton: UIButton = {
       var buttonso: String! = String(cString: [109,115,101,112,115,110,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &buttonso) { pointer in
    
   }
    var contactf: String! = String(cString: [100,97,116,97,99,101,110,116,101,114,0], encoding: .utf8)!
   repeat {
       var recordf: Float = 2.0
      withUnsafeMutablePointer(to: &recordf) { pointer in
             _ = pointer.pointee
      }
         recordf /= Swift.max((Float(Int(recordf > 244267102.0 || recordf < -244267102.0 ? 48.0 : recordf))), 3)
         recordf -= (Float(Int(recordf > 250632443.0 || recordf < -250632443.0 ? 89.0 : recordf) ^ Int(recordf > 62036456.0 || recordf < -62036456.0 ? 45.0 : recordf)))
      repeat {
          var conversationz: Bool = true
          var httpn: String! = String(cString: [101,120,112,108,105,99,105,116,108,121,0], encoding: .utf8)!
          var authh: Float = 3.0
         withUnsafeMutablePointer(to: &authh) { pointer in
    
         }
          var publishedI: String! = String(cString: [102,97,99,116,111,114,115,0], encoding: .utf8)!
         recordf /= Swift.max(5, Float(1 | httpn.count))
         conversationz = authh >= 68.42
         authh /= Swift.max(1, (Float(Int(recordf > 192134571.0 || recordf < -192134571.0 ? 28.0 : recordf) ^ 1)))
         publishedI.append("\((2 | Int(recordf > 368744399.0 || recordf < -368744399.0 ? 38.0 : recordf)))")
         if recordf == 560055.0 {
            break
         }
      } while ((recordf - recordf) > 1.49) && (recordf == 560055.0)
      contactf = "\(buttonso.count - 3)"
      if contactf.count == 2816485 {
         break
      }
   } while (contactf.count == 2816485) && (5 < buttonso.count)

        let photoBtn = UIButton(type: .custom)
      contactf.append("\(1)")
        photoBtn.setImage("common_back".toImage, for: .normal)
        photoBtn.addTarget(self, action: #selector(backTapped), for: .touchUpInside)
        return photoBtn
    }()

    private lazy var playButton: UIButton = {
       var sidep: String! = String(cString: [115,109,97,114,116,0], encoding: .utf8)!
   while (sidep == String(cString:[117,0], encoding: .utf8)!) {
       var collectione: String! = String(cString: [114,97,105,115,101,100,0], encoding: .utf8)!
       var finishd: String! = String(cString: [101,97,99,115,0], encoding: .utf8)!
       var timeoutE: String! = String(cString: [112,114,111,112,111,115,101,0], encoding: .utf8)!
       var with_dA: String! = String(cString: [115,99,114,101,101,110,0], encoding: .utf8)!
       var senderF: String! = String(cString: [100,111,119,110,108,111,97,100,101,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &senderF) { pointer in
    
      }
         finishd.append("\(1 | timeoutE.count)")
         with_dA = "\(2)"
         senderF.append("\(collectione.count / (Swift.max(1, 3)))")
          var listg: Double = 2.0
          _ = listg
          var reuse0: Int = 3
          var barl: Double = 3.0
         withUnsafeMutablePointer(to: &barl) { pointer in
    
         }
         finishd = "\(with_dA.count & 1)"
         listg -= Double(reuse0)
         reuse0 -= 3
         barl += Double(collectione.count / 2)
      if with_dA.count > 2 && senderF != String(cString:[121,0], encoding: .utf8)! {
         with_dA.append("\(3)")
      }
         timeoutE = "\(finishd.count - collectione.count)"
      repeat {
          var folderh: String! = String(cString: [108,97,110,100,115,99,97,112,101,0], encoding: .utf8)!
          var pathsA: [String: Any]! = [String(cString: [115,117,103,103,101,115,116,105,111,110,115,0], encoding: .utf8)!:92, String(cString: [102,97,115,116,102,105,114,115,116,112,97,115,115,0], encoding: .utf8)!:18]
          var replyn: [Any]! = [9]
         finishd.append("\(finishd.count >> (Swift.min(2, pathsA.values.count)))")
         folderh = "\((folderh == (String(cString:[114,0], encoding: .utf8)!) ? senderF.count : folderh.count))"
         replyn.append(pathsA.count / (Swift.max(7, senderF.count)))
         if (String(cString:[48,109,56,112,120,51,0], encoding: .utf8)!) == finishd {
            break
         }
      } while (with_dA.count >= finishd.count) && ((String(cString:[48,109,56,112,120,51,0], encoding: .utf8)!) == finishd)
          var normalized9: Bool = true
         withUnsafeMutablePointer(to: &normalized9) { pointer in
    
         }
         with_dA.append("\(((normalized9 ? 4 : 1)))")
          var t_productsd: Int = 5
         collectione.append("\(timeoutE.count)")
         t_productsd -= senderF.count
      repeat {
         senderF = "\(senderF.count)"
         if (String(cString:[97,120,49,50,122,120,122,122,109,0], encoding: .utf8)!) == senderF {
            break
         }
      } while (finishd == String(cString:[114,0], encoding: .utf8)!) && ((String(cString:[97,120,49,50,122,120,122,122,109,0], encoding: .utf8)!) == senderF)
         timeoutE.append("\(senderF.count - with_dA.count)")
      repeat {
         with_dA.append("\(with_dA.count)")
         if with_dA == (String(cString:[56,99,108,48,0], encoding: .utf8)!) {
            break
         }
      } while (with_dA == (String(cString:[56,99,108,48,0], encoding: .utf8)!)) && (senderF.count <= 2)
          var statew: String! = String(cString: [98,117,99,107,101,116,97,108,108,111,99,0], encoding: .utf8)!
          var formatterW: [Any]! = [37.0]
          var speakerC: [String: Any]! = [String(cString: [100,97,114,107,0], encoding: .utf8)!:String(cString: [105,110,116,101,103,114,97,108,0], encoding: .utf8)!]
         senderF.append("\(((String(cString:[53,0], encoding: .utf8)!) == collectione ? statew.count : collectione.count))")
         formatterW.append(statew.count)
         speakerC[statew] = statew.count
      if 1 >= senderF.count {
         senderF = "\(3)"
      }
         finishd = "\(1)"
      sidep.append("\(senderF.count)")
      break
   }

        let photoBtn = UIButton(type: .custom)
        photoBtn.setImage("detail_play".toImage, for: .normal)
        photoBtn.isHidden = true
        photoBtn.addTarget(self, action: #selector(togglePlayPause), for: .touchUpInside)
        return photoBtn
    }()

    private let contentLabel: UILabel = {
       var rnew_3_S: String! = String(cString: [117,112,115,104,105,102,116,0], encoding: .utf8)!
      rnew_3_S = "\(rnew_3_S.count)"

        let v = UILabel()
        v.font = .systemFont(ofSize: 14)
        v.textColor = .white
        v.numberOfLines = 0
        return v
    }()

    private let avatarView: UIImageView = {
       var extrax: String! = String(cString: [99,111,110,115,116,114,97,105,110,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &extrax) { pointer in
          _ = pointer.pointee
   }
   repeat {
      extrax = "\(1 << (Swift.min(5, extrax.count)))"
      if (String(cString:[109,99,97,99,109,54,109,0], encoding: .utf8)!) == extrax {
         break
      }
   } while ((String(cString:[109,99,97,99,109,54,109,0], encoding: .utf8)!) == extrax) && (extrax.count >= extrax.count)

        let v = UIImageView()
        v.layer.cornerRadius = 14
        v.clipsToBounds = true
        v.contentMode = .scaleAspectFill
        v.backgroundColor = UIColor(hex: "#D4C4A8")
        return v
    }()

    private let userNameLabel: UILabel = {
       var starryT: [Any]! = [89, 44]
   if (starryT.count - 5) <= 1 || (starryT.count - starryT.count) <= 5 {
       var shared1: Double = 1.0
      for _ in 0 ..< 1 {
          var peopleG: String! = String(cString: [113,116,97,98,108,101,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &peopleG) { pointer in
                _ = pointer.pointee
         }
          var sectionV: Double = 3.0
         shared1 -= (Double(Int(shared1 > 87692054.0 || shared1 < -87692054.0 ? 82.0 : shared1)))
         peopleG.append("\(1)")
         sectionV -= (Double(Int(shared1 > 12326615.0 || shared1 < -12326615.0 ? 66.0 : shared1) >> (Swift.min(peopleG.count, 2))))
      }
      repeat {
          var toolD: Double = 0.0
          var chromeN: Double = 1.0
         shared1 /= Swift.max((Double(Int(chromeN > 333960923.0 || chromeN < -333960923.0 ? 72.0 : chromeN))), 2)
         toolD += Double(3)
         if shared1 == 1125022.0 {
            break
         }
      } while (shared1 == 1125022.0) && (1.43 == (shared1 / 3.70))
       var spacing4: Double = 0.0
      withUnsafeMutablePointer(to: &spacing4) { pointer in
    
      }
         spacing4 += (Double(Int(shared1 > 221724266.0 || shared1 < -221724266.0 ? 52.0 : shared1) / 3))
      starryT.append(starryT.count)
   }

        let v = UILabel()
        v.font = .systemFont(ofSize: 13, weight: .bold)
        v.textColor = .white
        return v
    }()

    init(postModel: CSMenu) {
        self.postModel = postModel
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    private func applyPostInfo() {
       var catalogt: Double = 0.0
   for _ in 0 ..< 1 {
      catalogt /= Swift.max((Double(Int(catalogt > 392087651.0 || catalogt < -392087651.0 ? 81.0 : catalogt))), 5)
   }

        contentLabel.text = postModel.content
        userNameLabel.text = postModel.userName.uppercased()

        if let avatarPath = postModel.avatarURL, !avatarPath.isEmpty {
            avatarView.image = avatarPath.resourceFileImage ?? avatarPath.toImage
            avatarView.backgroundColor = avatarView.image == nil
                ? UIColor(hex: "#D4C4A8") : .clear
        } else {
            avatarView.image = "info_avatar".toImage
        }
    }


    override func viewDidLoad() {
       var hang6: String! = String(cString: [109,105,99,0], encoding: .utf8)!
    _ = hang6
   for _ in 0 ..< 1 {
      hang6.append("\(hang6.count)")
   }

        super.viewDidLoad()
        bgView.isHidden = true
        view.backgroundColor = .black
        setupUI()
        applyPostInfo()
        setupPlayer()
    }


    @objc private func playerDidFinish() {
       var generatorF: [Any]! = [95, 79, 35]
   withUnsafeMutablePointer(to: &generatorF) { pointer in
          _ = pointer.pointee
   }
   if 4 >= (4 << (Swift.min(1, generatorF.count))) && (generatorF.count << (Swift.min(1, generatorF.count))) >= 4 {
      generatorF = [1]
   }

        player?.seek(to: .zero)
        player?.play()
        isPlaying = true
        playButton.isHidden = true
    }


    override func viewDidLayoutSubviews() {
       var rows: Bool = false
      rows = (rows ? rows : !rows)

        super.viewDidLayoutSubviews()
        playerLayer?.frame = playerContainerView.bounds
    }


    private func setupUI() {
       var collectionv: [String: Any]! = [String(cString: [109,98,116,114,101,101,0], encoding: .utf8)!:String(cString: [100,114,105,118,101,114,115,0], encoding: .utf8)!, String(cString: [116,97,115,107,0], encoding: .utf8)!:String(cString: [112,99,109,98,0], encoding: .utf8)!, String(cString: [117,108,116,114,97,0], encoding: .utf8)!:String(cString: [111,110,116,97,99,116,115,0], encoding: .utf8)!]
       var models0: Double = 3.0
      withUnsafeMutablePointer(to: &models0) { pointer in
             _ = pointer.pointee
      }
       var playingi: [Any]! = [62, 30, 72]
       var successt: String! = String(cString: [116,114,97,110,115,102,111,114,109,101,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &successt) { pointer in
    
      }
      repeat {
         playingi.append((3 + Int(models0 > 95229840.0 || models0 < -95229840.0 ? 79.0 : models0)))
         if 687256 == playingi.count {
            break
         }
      } while (successt.count > 5) && (687256 == playingi.count)
          var placeholderT: String! = String(cString: [112,101,114,109,117,116,97,116,105,111,110,115,0], encoding: .utf8)!
         models0 += (Double(Int(models0 > 261206557.0 || models0 < -261206557.0 ? 66.0 : models0)))
         placeholderT.append("\(playingi.count)")
       var documents1: Double = 0.0
      repeat {
         models0 += Double(3 - successt.count)
         if 4814874.0 == models0 {
            break
         }
      } while ((documents1 / (Swift.max(models0, 9))) < 4.49 || 2.3 < (models0 / 4.49)) && (4814874.0 == models0)
      while (models0 > 1.87) {
          var collectionD: Int = 3
          var enableda: Float = 2.0
         playingi = [2 ^ successt.count]
         collectionD >>= Swift.min(5, labs(1))
         enableda += (Float(Int(enableda > 39940738.0 || enableda < -39940738.0 ? 11.0 : enableda) << (Swift.min(2, labs(collectionD)))))
         break
      }
         documents1 += Double(successt.count + 3)
         documents1 /= Swift.max(Double(playingi.count), 2)
         models0 -= Double(2 / (Swift.max(7, playingi.count)))
         successt.append("\(playingi.count)")
      collectionv = ["\(models0)": successt.count]

        view.addSubview(playerContainerView)
        view.addSubview(backButton)
        view.addSubview(playButton)
        view.addSubview(contentLabel)
        view.addSubview(avatarView)
        view.addSubview(userNameLabel)

        playerContainerView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        backButton.snp.makeConstraints { make in
            make.top.equalTo(view.safeAreaLayoutGuide).offset(8)
            make.left.equalToSuperview().offset(16)
            make.width.height.equalTo(40)
        }

        playButton.snp.makeConstraints { make in
            make.center.equalToSuperview()
            make.width.height.equalTo(64)
        }

        avatarView.snp.makeConstraints { make in
            make.left.equalToSuperview().offset(16)
            make.bottom.equalTo(view.safeAreaLayoutGuide).offset(-16)
            make.width.height.equalTo(28)
        }

        userNameLabel.snp.makeConstraints { make in
            make.centerY.equalTo(avatarView)
            make.left.equalTo(avatarView.snp.right).offset(8)
            make.right.lessThanOrEqualToSuperview().offset(-16)
        }

        contentLabel.snp.makeConstraints { make in
            make.left.right.equalToSuperview().inset(16)
            make.bottom.equalTo(avatarView.snp.top).offset(-12)
        }

        let prices = UITapGestureRecognizer(target: self, action: #selector(togglePlayPause))
        playerContainerView.addGestureRecognizer(prices)
    }


    @objc private func togglePlayPause() {
       var preferredm: [String: Any]! = [String(cString: [99,114,111,119,100,105,110,0], encoding: .utf8)!:[79, 60, 39]]
   withUnsafeMutablePointer(to: &preferredm) { pointer in
          _ = pointer.pointee
   }
      preferredm = ["\(preferredm.values.count)": 3]

        guard let player else { return }
        if isPlaying {
            player.pause()
            isPlaying = false
        } else {
            player.play()
            isPlaying = true
        }
        updatePlayButtonVisibility()
    }


    private func setupPlayer() {
       var img3: String! = String(cString: [100,105,114,112,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &img3) { pointer in
    
   }
   for _ in 0 ..< 2 {
      img3 = "\(2)"
   }

        guard let path = postModel.media.videoURL,
              let built = path.resourceFileURL else {
            return
        }

        let reportItem = AVPlayerItem(url: built)
        let network = AVPlayer(playerItem: reportItem)
        self.player = network

        let max_5 = AVPlayerLayer(player: network)
        max_5.videoGravity = .resizeAspectFill
        max_5.frame = playerContainerView.bounds
        playerContainerView.layer.insertSublayer(max_5, at: 0)
        playerLayer = max_5

        NotificationCenter.default.addObserver(
            self,
            selector: #selector(playerDidFinish),
            name: .AVPlayerItemDidPlayToEndTime,
            object: reportItem
        )

        startPlayback()
    }


    @objc private func backTapped() {
       var actions4: String! = String(cString: [114,111,111,109,0], encoding: .utf8)!
    _ = actions4
      actions4 = "\(3 ^ actions4.count)"

        navigationController?.popViewController(animated: true)
    }


    private func updatePlayButtonVisibility() {
       var resolvedI: [Any]! = [String(cString: [115,116,117,110,0], encoding: .utf8)!, String(cString: [99,111,109,101,0], encoding: .utf8)!]
    var final_d6: String! = String(cString: [102,111,117,110,100,0], encoding: .utf8)!
    _ = final_d6
   repeat {
       var galleryQ: Double = 0.0
      withUnsafeMutablePointer(to: &galleryQ) { pointer in
    
      }
       var unew_o3b: String! = String(cString: [100,105,97,108,111,103,0], encoding: .utf8)!
       _ = unew_o3b
         unew_o3b = "\(unew_o3b.count << (Swift.min(labs(1), 4)))"
          var star0: Bool = false
          var nowS: String! = String(cString: [99,97,116,101,103,111,114,105,115,101,100,0], encoding: .utf8)!
          _ = nowS
         unew_o3b = "\(((String(cString:[88,0], encoding: .utf8)!) == nowS ? nowS.count : Int(galleryQ > 302126142.0 || galleryQ < -302126142.0 ? 24.0 : galleryQ)))"
         star0 = !star0
      repeat {
         unew_o3b = "\((3 << (Swift.min(3, labs(Int(galleryQ > 129826844.0 || galleryQ < -129826844.0 ? 84.0 : galleryQ))))))"
         if unew_o3b == (String(cString:[99,109,52,104,97,118,107,51,0], encoding: .utf8)!) {
            break
         }
      } while (4.15 >= (galleryQ / (Swift.max(Double(unew_o3b.count), 3))) || 4 >= (unew_o3b.count - 5)) && (unew_o3b == (String(cString:[99,109,52,104,97,118,107,51,0], encoding: .utf8)!))
      if 5.63 >= (galleryQ / (Swift.max(5.59, 7))) {
         unew_o3b = "\((Int(galleryQ > 108234297.0 || galleryQ < -108234297.0 ? 43.0 : galleryQ) & 2))"
      }
         galleryQ -= Double(1)
      repeat {
         unew_o3b = "\(1 >> (Swift.min(3, unew_o3b.count)))"
         if unew_o3b.count == 4336694 {
            break
         }
      } while (5.12 > galleryQ) && (unew_o3b.count == 4336694)
      final_d6.append("\(3)")
      if (String(cString:[49,95,121,97,113,107,54,99,103,0], encoding: .utf8)!) == final_d6 {
         break
      }
   } while (resolvedI.count < final_d6.count) && ((String(cString:[49,95,121,97,113,107,54,99,103,0], encoding: .utf8)!) == final_d6)
   for _ in 0 ..< 3 {
       var jcopy_y1T: String! = String(cString: [104,113,112,101,108,0], encoding: .utf8)!
       var speakerB: String! = String(cString: [97,108,103,111,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &speakerB) { pointer in
    
      }
       var areav: String! = String(cString: [116,114,101,110,100,105,110,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &areav) { pointer in
             _ = pointer.pointee
      }
       var mediag: [String: Any]! = [String(cString: [97,115,99,101,110,100,101,114,0], encoding: .utf8)!:false]
      for _ in 0 ..< 1 {
          var themeu: String! = String(cString: [109,97,116,99,104,0], encoding: .utf8)!
          var published_: String! = String(cString: [112,111,112,112,101,100,0], encoding: .utf8)!
         speakerB = "\(areav.count)"
         themeu = "\(2)"
         published_ = "\(2 << (Swift.min(1, themeu.count)))"
      }
      while (areav.count < speakerB.count) {
         speakerB = "\(3 + speakerB.count)"
         break
      }
      while (speakerB != String(cString:[109,0], encoding: .utf8)!) {
          var existing_: Int = 3
         jcopy_y1T.append("\(jcopy_y1T.count - mediag.keys.count)")
         existing_ &= areav.count
         break
      }
       var friend_lfI: Bool = true
      withUnsafeMutablePointer(to: &friend_lfI) { pointer in
    
      }
      for _ in 0 ..< 1 {
         mediag[jcopy_y1T] = jcopy_y1T.count
      }
          var barj: Bool = false
         mediag = [jcopy_y1T: ((friend_lfI ? 1 : 2))]
         barj = friend_lfI
         mediag[areav] = 3
      while ((mediag.count << (Swift.min(labs(3), 3))) <= 2) {
         mediag[areav] = 3
         break
      }
         friend_lfI = friend_lfI || mediag.count == 35
      while ((speakerB.count + mediag.values.count) <= 3) {
         speakerB.append("\(mediag.count ^ jcopy_y1T.count)")
         break
      }
      repeat {
         friend_lfI = speakerB.count <= 3
         if friend_lfI ? !friend_lfI : friend_lfI {
            break
         }
      } while (friend_lfI) && (friend_lfI ? !friend_lfI : friend_lfI)
          var extrasM: [String: Any]! = [String(cString: [101,116,104,101,114,110,101,116,0], encoding: .utf8)!:String(cString: [97,108,116,101,114,110,97,116,105,118,101,0], encoding: .utf8)!, String(cString: [116,111,110,97,108,0], encoding: .utf8)!:String(cString: [117,110,114,111,117,110,100,101,100,0], encoding: .utf8)!]
          var configsK: String! = String(cString: [114,101,99,116,97,110,103,108,101,115,0], encoding: .utf8)!
          var layoutu: String! = String(cString: [99,111,108,115,101,116,0], encoding: .utf8)!
         speakerB = "\(jcopy_y1T.count)"
         extrasM = ["\(mediag.count)": 2]
         configsK = "\(speakerB.count & jcopy_y1T.count)"
         layoutu.append("\(speakerB.count)")
      final_d6 = "\(mediag.count << (Swift.min(areav.count, 3)))"
   }

   if 1 == resolvedI.count {
      resolvedI = [resolvedI.count * resolvedI.count]
   }
        playButton.isHidden = isPlaying
    }


    override func viewWillAppear(_ animated: Bool) {
       var median: String! = String(cString: [109,115,122,104,0], encoding: .utf8)!
      median.append("\(3)")

        super.viewWillAppear(animated)
        (tabBarController as? CSPackageToolController)?.setCustomTabBarHidden(true)
    }


    private func startPlayback() {
       var send_: [String: Any]! = [String(cString: [97,118,111,105,100,0], encoding: .utf8)!:String(cString: [105,110,116,101,114,110,97,108,0], encoding: .utf8)!, String(cString: [108,111,99,111,0], encoding: .utf8)!:String(cString: [119,114,97,112,112,105,110,103,0], encoding: .utf8)!]
      send_["\(send_.keys.count)"] = 2 >> (Swift.min(1, send_.count))

        player?.play()
        isPlaying = true
        playButton.isHidden = true
    }


    override func viewWillDisappear(_ animated: Bool) {
       var rangek: Double = 5.0
      rangek /= Swift.max((Double(Int(rangek > 283349378.0 || rangek < -283349378.0 ? 83.0 : rangek) ^ 3)), 2)

        super.viewWillDisappear(animated)
        player?.pause()
        NotificationCenter.default.removeObserver(self)
        if isMovingFromParent || isBeingDismissed {
            (tabBarController as? CSPackageToolController)?.setCustomTabBarHidden(false)
        }
    }
}
