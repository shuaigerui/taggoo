
import Foundation

import UIKit

final class CSRechargeSetup: UIView {

    var onSendTapped: ((String) -> Void)?

    private let fieldContainer: UIView = {
       var documentsG: String! = String(cString: [97,100,106,117,115,116,101,100,0], encoding: .utf8)!
    var messagesd: String! = String(cString: [116,105,108,108,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &messagesd) { pointer in
    
   }
      documentsG.append("\(documentsG.count | 3)")

        let v = UIView()
      messagesd = "\(messagesd.count + 2)"
        v.backgroundColor = .white
        v.layer.cornerRadius = 26
        v.clipsToBounds = true
        return v
    }()

    let textField: UITextField = {
       var recordsR: [Any]! = [92, 22, 91]
      recordsR.append(recordsR.count)

        let tf = UITextField()
        tf.placeholder = "Say something"
        tf.font = .systemFont(ofSize: 15)
        tf.textColor = UIColor(hex: "#4A3F35")
        tf.returnKeyType = .send
        tf.attributedPlaceholder = NSAttributedString(
            string: "Say something",
            attributes: [.foregroundColor: UIColor(hex: "#4A3F35").withAlphaComponent(0.35)]
        )
        return tf
    }()

    private lazy var sendButton: UIButton = {
       var rawm: [String: Any]! = [String(cString: [112,115,110,114,104,118,115,0], encoding: .utf8)!:30, String(cString: [115,99,104,117,110,99,107,0], encoding: .utf8)!:25]
    _ = rawm
    var content5: String! = String(cString: [100,108,115,121,109,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
       var listr: [Any]! = [76, 77, 44]
       _ = listr
       var destt: String! = String(cString: [100,105,114,101,99,116,105,111,110,97,108,0], encoding: .utf8)!
      while (3 <= (destt.count * 1) && 1 <= (listr.count * destt.count)) {
         listr = [listr.count]
         break
      }
      repeat {
         listr.append(3)
         if 1711992 == listr.count {
            break
         }
      } while (!destt.contains("\(listr.count)")) && (1711992 == listr.count)
      content5.append("\(rawm.values.count)")
   }

        let photoBtn = UIButton(type: .custom)
   while ((3 * rawm.values.count) < 3) {
      rawm[content5] = content5.count
      break
   }
        photoBtn.setImage("post_send".toImage, for: .normal)
        photoBtn.addTarget(self, action: #selector(sendTapped), for: .touchUpInside)
        return photoBtn
    }()

    override init(frame: CGRect) {
        super.init(frame: frame)
        setupUI()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    @objc private func sendTapped() {
       var collectsW: String! = String(cString: [98,105,116,115,104,105,102,116,0], encoding: .utf8)!
    var documents0: Double = 2.0
      documents0 += Double(1)

      documents0 *= Double(collectsW.count ^ 3)
        let group = textField.text?.trimmingCharacters(in: .whitespacesAndNewlines) ?? ""
        guard !group.isEmpty else { return }
        onSendTapped?(group)
        textField.text = nil
   for _ in 0 ..< 2 {
      collectsW.append("\(collectsW.count << (Swift.min(2, collectsW.count)))")
   }
    }


    private func setupUI() {
       var running3: Int = 4
   if running3 > 5 {
       var a_layer6: Double = 5.0
       var file8: Double = 1.0
       var peoplej: [String: Any]! = [String(cString: [97,99,100,101,99,0], encoding: .utf8)!:false]
       var optionsV: Double = 0.0
       _ = optionsV
         peoplej = ["\(file8)": (Int(optionsV > 75268890.0 || optionsV < -75268890.0 ? 25.0 : optionsV))]
          var registeredu: [Any]! = [15, 50, 44]
         peoplej = ["\(peoplej.values.count)": peoplej.count]
         registeredu = [(Int(file8 > 255536427.0 || file8 < -255536427.0 ? 22.0 : file8))]
          var didp: Float = 3.0
          var extraK: String! = String(cString: [109,117,108,97,100,100,0], encoding: .utf8)!
          var parametersh: [String: Any]! = [String(cString: [115,97,108,115,97,0], encoding: .utf8)!:String(cString: [98,117,102,102,101,114,101,118,101,110,116,0], encoding: .utf8)!, String(cString: [115,105,110,101,115,0], encoding: .utf8)!:String(cString: [115,117,98,112,97,116,104,0], encoding: .utf8)!]
         peoplej = ["\(a_layer6)": (Int(didp > 247337901.0 || didp < -247337901.0 ? 87.0 : didp))]
         extraK.append("\(peoplej.count)")
         parametersh = ["\(a_layer6)": 3]
         peoplej["\(optionsV)"] = (Int(optionsV > 265426214.0 || optionsV < -265426214.0 ? 75.0 : optionsV) ^ peoplej.count)
          var overridesY: String! = String(cString: [115,97,118,101,105,0], encoding: .utf8)!
          _ = overridesY
          var warningp: Double = 5.0
          var permission_: [String: Any]! = [String(cString: [112,97,114,115,101,117,116,105,108,115,0], encoding: .utf8)!:37, String(cString: [98,108,97,99,107,108,105,115,116,0], encoding: .utf8)!:69]
          _ = permission_
         peoplej[overridesY] = (1 << (Swift.min(3, labs(Int(a_layer6 > 149841123.0 || a_layer6 < -149841123.0 ? 85.0 : a_layer6)))))
         warningp -= (Double(Int(file8 > 78079735.0 || file8 < -78079735.0 ? 45.0 : file8) % (Swift.max(7, permission_.values.count))))
         permission_["\(warningp)"] = permission_.keys.count % 1
       var senderO: Double = 3.0
      repeat {
          var modelsq: Bool = false
          _ = modelsq
         file8 += (Double(1 | Int(senderO > 85273747.0 || senderO < -85273747.0 ? 51.0 : senderO)))
         modelsq = senderO == 95.80
         if 4792670.0 == file8 {
            break
         }
      } while (4792670.0 == file8) && ((peoplej.values.count * Int(file8 > 56268731.0 || file8 < -56268731.0 ? 43.0 : file8)) <= 1 && (Double(peoplej.values.count) * file8) <= 1.0)
         peoplej = ["\(senderO)": 3]
         senderO /= Swift.max(5, (Double(Int(optionsV > 367460576.0 || optionsV < -367460576.0 ? 31.0 : optionsV))))
         peoplej["\(a_layer6)"] = 2
      repeat {
          var afdL: [String: Any]! = [String(cString: [98,114,111,97,100,99,97,115,116,115,0], encoding: .utf8)!:String(cString: [116,114,117,116,104,0], encoding: .utf8)!, String(cString: [115,115,121,98,0], encoding: .utf8)!:String(cString: [112,114,101,115,115,101,115,0], encoding: .utf8)!]
          var previewT: String! = String(cString: [115,99,104,101,109,97,115,0], encoding: .utf8)!
         optionsV -= (Double(Int(a_layer6 > 49498593.0 || a_layer6 < -49498593.0 ? 57.0 : a_layer6)))
         afdL = ["\(peoplej.keys.count)": afdL.keys.count]
         previewT.append("\((previewT == (String(cString:[117,0], encoding: .utf8)!) ? previewT.count : Int(file8 > 225205051.0 || file8 < -225205051.0 ? 52.0 : file8)))")
         if 511001.0 == optionsV {
            break
         }
      } while (2.34 <= (optionsV - 4.11) || (4.11 + optionsV) <= 4.58) && (511001.0 == optionsV)
         optionsV += Double(peoplej.keys.count)
      running3 -= (Int(file8 > 10172759.0 || file8 < -10172759.0 ? 7.0 : file8) | 1)
   }

        backgroundColor = .clear

        addSubview(fieldContainer)
        fieldContainer.addSubview(textField)
        fieldContainer.addSubview(sendButton)

        fieldContainer.snp.makeConstraints { make in
            make.left.right.equalToSuperview().inset(16)
            make.top.equalToSuperview().offset(8)
            make.bottom.equalToSuperview().offset(-8)
            make.height.equalTo(52)
        }

        sendButton.snp.makeConstraints { make in
            make.right.equalToSuperview().offset(-4)
            make.centerY.equalToSuperview()
            make.width.equalTo(59)
            make.height.equalTo(40)
        }

        textField.snp.makeConstraints { make in
            make.left.equalToSuperview().offset(16)
            make.right.equalTo(sendButton.snp.left).offset(-8)
            make.centerY.equalToSuperview()
        }
    }
}
