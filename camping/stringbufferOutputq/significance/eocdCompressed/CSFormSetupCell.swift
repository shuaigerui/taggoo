
import Foundation

import UIKit

final class CSFormSetupCell: UICollectionViewCell {

    static let reuseID = "CSFormSetupCell"

    var onDeleteTapped: (() -> Void)?

    private let thumbView: UIImageView = {
       var start0: Double = 1.0
   if start0 >= 4.41 {
      start0 -= (Double(2 << (Swift.min(labs(Int(start0 > 33697534.0 || start0 < -33697534.0 ? 42.0 : start0)), 1))))
   }

        let v = UIImageView()
        v.contentMode = .scaleAspectFill
        v.clipsToBounds = true
        v.layer.cornerRadius = 12
        v.backgroundColor = UIColor(hex: "#D4C4A8")
        return v
    }()

    private lazy var deleteButton: UIButton = {
       var g_titled: String! = String(cString: [104,97,110,100,108,101,115,0], encoding: .utf8)!
   repeat {
       var collected2: Bool = false
       _ = collected2
       var profilex: Float = 5.0
      withUnsafeMutablePointer(to: &profilex) { pointer in
             _ = pointer.pointee
      }
       var buttonsM: [String: Any]! = [String(cString: [114,101,119,114,105,116,101,0], encoding: .utf8)!:97, String(cString: [114,111,117,110,100,110,101,115,115,0], encoding: .utf8)!:87]
       var rgbi: [Any]! = [34, 97, 95]
       _ = rgbi
       var launchz: [Any]! = [UILabel()]
      for _ in 0 ..< 1 {
         collected2 = profilex < Float(buttonsM.keys.count)
      }
       var applyk: Double = 1.0
       var randomM: Double = 1.0
       _ = randomM
      while (5.39 < profilex) {
          var sharedb: [Any]! = [17, 90]
         collected2 = rgbi.count < buttonsM.values.count
         sharedb = [(Int(applyk > 132413224.0 || applyk < -132413224.0 ? 37.0 : applyk) - 3)]
         break
      }
      if (launchz.count % 5) > 3 {
          var endY: String! = String(cString: [105,109,109,101,100,105,97,116,101,108,121,0], encoding: .utf8)!
          _ = endY
          var setting7: [String: Any]! = [String(cString: [110,97,103,108,101,0], encoding: .utf8)!:69, String(cString: [110,101,105,103,104,98,111,114,105,110,103,0], encoding: .utf8)!:74]
         withUnsafeMutablePointer(to: &setting7) { pointer in
    
         }
         launchz = [2]
         endY = "\((Int(randomM > 31284050.0 || randomM < -31284050.0 ? 68.0 : randomM)))"
         setting7 = ["\(launchz.count)": 1 - launchz.count]
      }
       var providerz: Double = 4.0
       _ = providerz
       var fallbackr: Double = 4.0
       _ = fallbackr
      while (3.87 <= (5.52 + fallbackr)) {
          var vcsA: String! = String(cString: [100,101,99,114,101,97,115,105,110,103,0], encoding: .utf8)!
          _ = vcsA
          var looperU: Double = 5.0
         fallbackr /= Swift.max(Double(buttonsM.values.count / 2), 2)
         vcsA = "\((Int(looperU > 241637503.0 || looperU < -241637503.0 ? 82.0 : looperU) - Int(fallbackr > 373554730.0 || fallbackr < -373554730.0 ? 100.0 : fallbackr)))"
         looperU -= Double(rgbi.count)
         break
      }
          var gradienth: Float = 0.0
          var fallbackD: String! = String(cString: [102,105,108,101,112,97,116,104,0], encoding: .utf8)!
         launchz.append(3)
         gradienth -= Float(1)
         fallbackD = "\(launchz.count >> (Swift.min(labs(1), 2)))"
      while (!launchz.contains { $0 as? Int == rgbi.count }) {
         rgbi = [(Int(applyk > 143227196.0 || applyk < -143227196.0 ? 93.0 : applyk))]
         break
      }
      repeat {
         randomM -= Double(3)
         if randomM == 3967581.0 {
            break
         }
      } while (randomM == 3967581.0) && (buttonsM["\(randomM)"] != nil)
      while ((launchz.count % 3) == 3 || 1.73 == (Double(launchz.count) + fallbackr)) {
          var rowF: String! = String(cString: [115,101,116,116,105,109,101,111,117,116,0], encoding: .utf8)!
          var u_imageM: Float = 0.0
         fallbackr += (Double(Int(profilex > 32634892.0 || profilex < -32634892.0 ? 60.0 : profilex)))
         rowF = "\(1)"
         u_imageM -= Float(3)
         break
      }
         providerz += Double(buttonsM.values.count << (Swift.min(labs(2), 5)))
         rgbi.append(launchz.count)
      repeat {
          var register_wN: String! = String(cString: [100,111,120,121,103,101,110,0], encoding: .utf8)!
          _ = register_wN
          var dirg: String! = String(cString: [114,101,118,105,115,105,111,110,0], encoding: .utf8)!
          var infoL: Bool = true
          _ = infoL
         launchz.append((Int(profilex > 385935183.0 || profilex < -385935183.0 ? 91.0 : profilex) % (Swift.max(8, (infoL ? 3 : 3)))))
         register_wN = "\((3 + Int(applyk > 338536795.0 || applyk < -338536795.0 ? 37.0 : applyk)))"
         dirg = "\(2)"
         if 3253213 == launchz.count {
            break
         }
      } while (3253213 == launchz.count) && (3 >= (rgbi.count - launchz.count) && 1 >= (3 - rgbi.count))
         buttonsM = ["\(collected2)": ((collected2 ? 4 : 5) + Int(providerz > 76774738.0 || providerz < -76774738.0 ? 41.0 : providerz))]
      if launchz.count > 1 {
          var networkq: Double = 2.0
         collected2 = providerz >= 36.51 && applyk >= 36.51
         networkq /= Swift.max(5, Double(1))
      }
      g_titled.append("\(rgbi.count)")
      if g_titled == (String(cString:[48,105,116,99,106,102,0], encoding: .utf8)!) {
         break
      }
   } while (g_titled.count == g_titled.count) && (g_titled == (String(cString:[48,105,116,99,106,102,0], encoding: .utf8)!))

        let photoBtn = UIButton(type: .custom)
        photoBtn.setImage("push_del".toImage, for: .normal)
        photoBtn.addTarget(self, action: #selector(deleteTapped), for: .touchUpInside)
        return photoBtn
    }()

    override init(frame: CGRect) {
        super.init(frame: frame)
        contentView.addSubview(thumbView)
        contentView.addSubview(deleteButton)

        thumbView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        deleteButton.snp.makeConstraints { make in
            make.top.right.equalToSuperview()
            make.width.height.equalTo(28)
        }
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    @objc private func deleteTapped() {
       var collects0: String! = String(cString: [108,111,97,116,0], encoding: .utf8)!
   repeat {
      collects0 = "\(2)"
      if collects0.count == 3852260 {
         break
      }
   } while (!collects0.contains("\(collects0.count)")) && (collects0.count == 3852260)

        onDeleteTapped?()
    }


    func configure(image: UIImage) {
       var icont: [String: Any]! = [String(cString: [109,97,115,107,0], encoding: .utf8)!:78, String(cString: [100,97,116,97,99,101,110,116,101,114,115,0], encoding: .utf8)!:50]
   withUnsafeMutablePointer(to: &icont) { pointer in
          _ = pointer.pointee
   }
   repeat {
      icont["\(icont.keys.count)"] = icont.count
      if 4576839 == icont.count {
         break
      }
   } while (4576839 == icont.count) && ((icont.values.count & icont.values.count) < 2 && (2 & icont.values.count) < 4)

        thumbView.image = image
    }
}
