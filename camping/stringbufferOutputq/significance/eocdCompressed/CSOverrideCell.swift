
import Foundation

import UIKit

final class CSOverrideCell: UICollectionViewCell {

    static let reuseID = "CSOverrideCell"

    var onTapped: (() -> Void)?

    private lazy var addButton: UIButton = {
       var authorq: [Any]! = [99, 47]
    _ = authorq
    var contentVa: Double = 3.0
   if contentVa == 5.2 {
       var sectionG: Double = 3.0
      withUnsafeMutablePointer(to: &sectionG) { pointer in
    
      }
       var cacheb: String! = String(cString: [109,111,118,101,115,0], encoding: .utf8)!
       var signatureu: Bool = true
       var privacyy: String! = String(cString: [111,99,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
      if privacyy.count < 1 {
         signatureu = cacheb.count == 69
      }
      while (privacyy.hasSuffix("\(signatureu)")) {
          var absoluteu: String! = String(cString: [102,101,119,0], encoding: .utf8)!
         signatureu = (92 == ((signatureu ? 92 : absoluteu.count) % (Swift.max(absoluteu.count, 10))))
         break
      }
          var relativeG: String! = String(cString: [115,105,103,105,100,0], encoding: .utf8)!
          _ = relativeG
          var postd: Float = 3.0
          var modelP: String! = String(cString: [115,105,110,113,102,0], encoding: .utf8)!
         signatureu = sectionG <= 37.4 || !signatureu
         relativeG = "\(((String(cString:[67,0], encoding: .utf8)!) == privacyy ? privacyy.count : (signatureu ? 4 : 4)))"
         postd /= Swift.max(3, (Float(cacheb.count | (signatureu ? 5 : 1))))
         modelP.append("\(cacheb.count)")
      while (signatureu && cacheb.count < 3) {
         cacheb.append("\(((signatureu ? 4 : 2)))")
         break
      }
         cacheb.append("\(cacheb.count)")
      for _ in 0 ..< 2 {
         signatureu = !signatureu
      }
         privacyy = "\((1 + Int(sectionG > 103280647.0 || sectionG < -103280647.0 ? 8.0 : sectionG)))"
          var appleG: Double = 2.0
         privacyy.append("\(((signatureu ? 4 : 4) % 3))")
         appleG += (Double((signatureu ? 3 : 4) - 3))
         cacheb.append("\(((signatureu ? 1 : 5) + Int(sectionG > 297029073.0 || sectionG < -297029073.0 ? 42.0 : sectionG)))")
         privacyy.append("\((2 >> (Swift.min(3, labs(Int(sectionG > 304040516.0 || sectionG < -304040516.0 ? 28.0 : sectionG))))))")
       var default_d7: Int = 0
       _ = default_d7
       var itemsk: Int = 0
          var registeredG: Double = 1.0
         withUnsafeMutablePointer(to: &registeredG) { pointer in
    
         }
          var reusen: Double = 1.0
          var greetingV: Float = 0.0
         default_d7 -= itemsk
         registeredG /= Swift.max(2, (Double(Int(sectionG > 307619121.0 || sectionG < -307619121.0 ? 92.0 : sectionG) % (Swift.max(1, (signatureu ? 4 : 1))))))
         reusen -= Double(default_d7)
         greetingV -= (Float(2 * Int(reusen > 81311320.0 || reusen < -81311320.0 ? 31.0 : reusen)))
      contentVa -= (Double((signatureu ? 5 : 2) / (Swift.max(Int(contentVa > 112961931.0 || contentVa < -112961931.0 ? 82.0 : contentVa), 9))))
   }

        let photoBtn = UIButton(type: .custom)
      authorq.append(2)
        photoBtn.setImage("push_add".toImage, for: .normal)
        photoBtn.contentHorizontalAlignment = .fill
        photoBtn.contentVerticalAlignment = .fill
        photoBtn.imageView?.contentMode = .scaleAspectFit
        photoBtn.backgroundColor = UIColor(hex: "#F3F7BB", alpha: 0.5)
        photoBtn.layer.cornerRadius = 12
        photoBtn.clipsToBounds = true
        photoBtn.addTarget(self, action: #selector(addTapped), for: .touchUpInside)
        return photoBtn
    }()

    override init(frame: CGRect) {
        super.init(frame: frame)
        contentView.addSubview(addButton)
        addButton.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    @objc private func addTapped() {
       var whitew: Double = 2.0
   repeat {
       var diamonds: String! = String(cString: [97,118,120,115,121,110,116,104,0], encoding: .utf8)!
       var layout9: [Any]! = [String(cString: [115,117,112,101,114,115,99,114,105,112,116,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &layout9) { pointer in
             _ = pointer.pointee
      }
       var datew: [String: Any]! = [String(cString: [116,105,109,101,115,116,97,109,112,101,100,0], encoding: .utf8)!:100, String(cString: [102,105,110,100,101,114,0], encoding: .utf8)!:86, String(cString: [117,112,112,101,114,0], encoding: .utf8)!:56]
       var folderC: Int = 5
       var labels: String! = String(cString: [110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &labels) { pointer in
    
      }
      if 4 <= (folderC / (Swift.max(labels.count, 7))) && 5 <= (folderC / (Swift.max(4, 9))) {
          var alertZ: String! = String(cString: [106,99,104,117,102,102,0], encoding: .utf8)!
          _ = alertZ
          var price7: Int = 2
          _ = price7
          var onlyH: String! = String(cString: [119,105,100,101,115,99,114,101,101,110,0], encoding: .utf8)!
          var f_widthx: Double = 2.0
          _ = f_widthx
         folderC |= alertZ.count
         price7 |= folderC % (Swift.max(alertZ.count, 4))
         onlyH = "\(layout9.count * price7)"
         f_widthx += Double(price7)
      }
      for _ in 0 ..< 2 {
         layout9 = [1]
      }
      if diamonds != String(cString:[68,0], encoding: .utf8)! {
         labels.append("\(1 ^ labels.count)")
      }
      while (folderC >= datew.count) {
          var collectionI: [String: Any]! = [String(cString: [98,99,100,117,105,110,116,0], encoding: .utf8)!:56, String(cString: [97,116,116,105,98,117,116,101,115,0], encoding: .utf8)!:60]
          var campingS: String! = String(cString: [102,116,118,109,111,0], encoding: .utf8)!
          var collectx: [Any]! = [String(cString: [102,102,116,112,97,99,107,0], encoding: .utf8)!]
         datew = ["\(layout9.count)": campingS.count]
         collectionI[diamonds] = campingS.count / (Swift.max(diamonds.count, 8))
         collectx.append(layout9.count >> (Swift.min(labs(2), 5)))
         break
      }
       var starti: String! = String(cString: [104,101,108,100,0], encoding: .utf8)!
       _ = starti
       var topC: String! = String(cString: [116,118,100,99,0], encoding: .utf8)!
       var nowi: String! = String(cString: [101,120,101,99,117,116,111,114,0], encoding: .utf8)!
         folderC &= (diamonds == (String(cString:[76,0], encoding: .utf8)!) ? layout9.count : diamonds.count)
      while ((2 | diamonds.count) <= 2 || (folderC | 2) <= 1) {
          var inset_: Float = 4.0
         withUnsafeMutablePointer(to: &inset_) { pointer in
    
         }
          var timerN: String! = String(cString: [117,114,108,99,111,110,116,101,120,116,0], encoding: .utf8)!
          var commentsL: Double = 2.0
          var liked: [Any]! = [String(cString: [121,117,118,114,103,98,97,0], encoding: .utf8)!, String(cString: [100,105,102,102,97,98,108,101,0], encoding: .utf8)!, String(cString: [99,111,110,116,114,97,105,110,116,115,0], encoding: .utf8)!]
          var privacyO: String! = String(cString: [100,97,109,112,105,110,103,0], encoding: .utf8)!
          _ = privacyO
         folderC *= 2
         inset_ += (Float(nowi == (String(cString:[105,0], encoding: .utf8)!) ? nowi.count : Int(inset_ > 373152198.0 || inset_ < -373152198.0 ? 52.0 : inset_)))
         timerN = "\(folderC)"
         commentsL /= Swift.max(3, Double(2 ^ folderC))
         liked.append(2)
         privacyO = "\(liked.count)"
         break
      }
         datew["\(starti)"] = datew.keys.count
      while (starti.contains("\(datew.values.count)")) {
         datew = [labels: 2]
         break
      }
         datew[nowi] = 3 - starti.count
       var namesF: [String: Any]! = [String(cString: [104,105,103,104,115,0], encoding: .utf8)!:77, String(cString: [119,105,116,104,105,110,0], encoding: .utf8)!:59]
      repeat {
         folderC += 1
         if 4861025 == folderC {
            break
         }
      } while (folderC > diamonds.count) && (4861025 == folderC)
          var actions2: [Any]! = [UILabel()]
          var timerc: Double = 4.0
         withUnsafeMutablePointer(to: &timerc) { pointer in
    
         }
         diamonds.append("\(namesF.count ^ nowi.count)")
         actions2.append(namesF.count / (Swift.max(10, folderC)))
         timerc += Double(actions2.count)
      for _ in 0 ..< 1 {
         topC.append("\(2)")
      }
      whitew += (Double(labels == (String(cString:[88,0], encoding: .utf8)!) ? Int(whitew > 322491790.0 || whitew < -322491790.0 ? 56.0 : whitew) : labels.count))
      if 1866348.0 == whitew {
         break
      }
   } while (1866348.0 == whitew) && ((whitew + 3.53) == 2.12)

        onTapped?()
    }
}
