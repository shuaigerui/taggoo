
import Foundation

import UIKit

final class CSInfoConversationCell: UITableViewCell {

    static let reuseID = "CSInfoConversationCell"

    var onFollowTapped: (() -> Void)?
    var onLikeTapped: (() -> Void)?
    var onCollectTapped: (() -> Void)?
    var onReportTapped: (() -> Void)?
    var onAvatarTapped: (() -> Void)?

    private let cardView: UIView = {
       var person2: [String: Any]! = [String(cString: [112,117,98,108,105,115,104,101,100,0], encoding: .utf8)!:82, String(cString: [109,105,103,114,97,116,101,100,0], encoding: .utf8)!:57, String(cString: [99,104,97,114,108,101,110,0], encoding: .utf8)!:33]
    var load5: [Any]! = [85, 98]
      person2 = ["\(person2.values.count)": 1 ^ load5.count]

        let v = UIView()
      load5 = [1 - person2.values.count]
        v.backgroundColor = UIColor(hex: "#E9DC8A")
        v.layer.cornerRadius = 24
        v.clipsToBounds = true
        return v
    }()

    private let avatarView: UIImageView = {
       var addh: Double = 2.0
    var result4: Float = 0.0
   if (addh + 2.22) <= 4.12 && 2.22 <= (Double(result4) + addh) {
      result4 += Float(1)
   }

        let v = UIImageView()
   repeat {
       var menuo: String! = String(cString: [98,108,117,101,0], encoding: .utf8)!
       var hint3: String! = String(cString: [99,100,108,109,115,0], encoding: .utf8)!
       var type_rte: String! = String(cString: [109,97,120,113,0], encoding: .utf8)!
       _ = type_rte
      repeat {
         menuo = "\(((String(cString:[110,0], encoding: .utf8)!) == menuo ? menuo.count : hint3.count))"
         if menuo.count == 2024501 {
            break
         }
      } while (5 == type_rte.count) && (menuo.count == 2024501)
      if type_rte != hint3 {
         hint3.append("\(2 | menuo.count)")
      }
         hint3.append("\((type_rte == (String(cString:[69,0], encoding: .utf8)!) ? hint3.count : type_rte.count))")
      repeat {
         type_rte = "\(((String(cString:[73,0], encoding: .utf8)!) == menuo ? menuo.count : type_rte.count))"
         if 2759900 == type_rte.count {
            break
         }
      } while (2759900 == type_rte.count) && (type_rte.count > 4)
      for _ in 0 ..< 2 {
          var configsg: String! = String(cString: [115,117,98,115,116,105,116,117,116,101,0], encoding: .utf8)!
          var homep: Float = 0.0
          var mcopy_1gO: Float = 2.0
         hint3.append("\(configsg.count)")
         homep /= Swift.max((Float(Int(mcopy_1gO > 261208215.0 || mcopy_1gO < -261208215.0 ? 69.0 : mcopy_1gO) / 1)), 5)
         mcopy_1gO /= Swift.max((Float(Int(mcopy_1gO > 142359964.0 || mcopy_1gO < -142359964.0 ? 74.0 : mcopy_1gO) % (Swift.max(hint3.count, 6)))), 3)
      }
         menuo.append("\(hint3.count)")
       var pickero: Bool = true
       var contactL: Bool = true
      withUnsafeMutablePointer(to: &contactL) { pointer in
             _ = pointer.pointee
      }
      while (4 == type_rte.count && !pickero) {
         type_rte = "\(type_rte.count)"
         break
      }
         contactL = !menuo.contains("\(contactL)")
      addh -= (Double(Int(addh > 374352654.0 || addh < -374352654.0 ? 44.0 : addh)))
      if addh == 363934.0 {
         break
      }
   } while (2.24 < (Double(result4) / (Swift.max(4, addh)))) && (addh == 363934.0)
        v.layer.cornerRadius = 24
        v.clipsToBounds = true
        v.contentMode = .scaleAspectFill
        v.backgroundColor = UIColor(hex: "#D4C4A8")
        v.isUserInteractionEnabled = true
        return v
    }()

    private let nameLabel: UILabel = {
       var list0: Double = 1.0
   while (2.28 < list0) {
       var contentN: Int = 3
       var extraC: String! = String(cString: [98,106,101,99,116,0], encoding: .utf8)!
       var changeO: String! = String(cString: [109,117,108,116,105,108,105,110,101,0], encoding: .utf8)!
       var author0: String! = String(cString: [118,111,105,99,101,109,97,105,108,0], encoding: .utf8)!
       _ = author0
       var dismissy: String! = String(cString: [108,97,115,116,109,98,117,102,0], encoding: .utf8)!
       _ = dismissy
      repeat {
          var displayT: String! = String(cString: [98,111,108,100,0], encoding: .utf8)!
          var popS: String! = String(cString: [119,119,119,0], encoding: .utf8)!
          var final_k7n: [Any]! = [92.0]
          var roomq: Double = 5.0
          var finishH: String! = String(cString: [114,101,102,101,114,101,110,99,101,100,0], encoding: .utf8)!
         author0 = "\(author0.count)"
         displayT.append("\(3)")
         popS.append("\(displayT.count)")
         final_k7n = [1]
         roomq /= Swift.max((Double(displayT == (String(cString:[89,0], encoding: .utf8)!) ? final_k7n.count : displayT.count)), 1)
         finishH = "\(popS.count)"
         if 4676800 == author0.count {
            break
         }
      } while ((5 * contentN) >= 4) && (4676800 == author0.count)
      for _ in 0 ..< 3 {
         contentN -= contentN
      }
         extraC.append("\(3)")
         extraC = "\(dismissy.count - extraC.count)"
      while (2 > dismissy.count) {
         changeO = "\(extraC.count)"
         break
      }
      while (extraC == changeO) {
         changeO = "\(1)"
         break
      }
      repeat {
         contentN += dismissy.count ^ 1
         if 1835065 == contentN {
            break
         }
      } while (1835065 == contentN) && (author0.hasPrefix("\(contentN)"))
          var wrapd: String! = String(cString: [105,99,99,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &wrapd) { pointer in
    
         }
         dismissy.append("\(((String(cString:[106,0], encoding: .utf8)!) == changeO ? changeO.count : contentN))")
         wrapd.append("\(changeO.count)")
      for _ in 0 ..< 3 {
          var screenf: [String: Any]! = [String(cString: [115,99,97,108,101,109,111,100,101,0], encoding: .utf8)!:40, String(cString: [108,111,99,107,0], encoding: .utf8)!:35, String(cString: [98,111,119,108,105,110,103,0], encoding: .utf8)!:86]
         withUnsafeMutablePointer(to: &screenf) { pointer in
                _ = pointer.pointee
         }
          var credential8: String! = String(cString: [115,111,100,101,97,108,108,111,99,0], encoding: .utf8)!
          var infoZ: Double = 5.0
         withUnsafeMutablePointer(to: &infoZ) { pointer in
                _ = pointer.pointee
         }
          var homej: [Any]! = [String(cString: [100,114,111,112,120,0], encoding: .utf8)!, String(cString: [105,97,100,115,116,120,0], encoding: .utf8)!, String(cString: [114,97,119,101,110,99,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &homej) { pointer in
    
         }
         author0.append("\(credential8.count)")
         screenf[dismissy] = contentN % (Swift.max(dismissy.count, 3))
         infoZ += (Double((String(cString:[80,0], encoding: .utf8)!) == credential8 ? credential8.count : screenf.values.count))
         homej = [1]
      }
         extraC.append("\(changeO.count)")
          var final_grU: Double = 5.0
         extraC = "\(dismissy.count)"
         final_grU -= Double(dismissy.count + extraC.count)
         extraC = "\((extraC == (String(cString:[119,0], encoding: .utf8)!) ? changeO.count : extraC.count))"
      repeat {
          var reasons4: Double = 1.0
          var finishc: String! = String(cString: [117,110,115,104,97,114,112,0], encoding: .utf8)!
         contentN %= Swift.max((3 & Int(reasons4 > 97624519.0 || reasons4 < -97624519.0 ? 52.0 : reasons4)), 3)
         finishc = "\((Int(reasons4 > 140947154.0 || reasons4 < -140947154.0 ? 57.0 : reasons4)))"
         if contentN == 374245 {
            break
         }
      } while (5 >= (contentN | changeO.count) || 5 >= (contentN | changeO.count)) && (contentN == 374245)
      for _ in 0 ..< 2 {
          var statev: Double = 5.0
         withUnsafeMutablePointer(to: &statev) { pointer in
                _ = pointer.pointee
         }
          var layoutQ: Int = 5
         dismissy = "\(contentN % (Swift.max(extraC.count, 10)))"
         statev += Double(2 ^ layoutQ)
         layoutQ /= Swift.max(3, (Int(statev > 244318551.0 || statev < -244318551.0 ? 57.0 : statev)))
      }
      while (1 < contentN) {
         contentN >>= Swift.min(4, extraC.count)
         break
      }
      list0 -= (Double(dismissy == (String(cString:[80,0], encoding: .utf8)!) ? dismissy.count : contentN))
      break
   }

        let v = UILabel()
        v.font = .systemFont(ofSize: 15)
        v.textColor = .black
        return v
    }()

    private let timeLabel: UILabel = {
       var detailF: Float = 3.0
      detailF -= Float(2)

        let v = UILabel()
        v.font = .systemFont(ofSize: 12)
        v.textColor = UIColor(hex: "#999999")
        return v
    }()

    private lazy var followButton: UIButton = {
       var shows1: Float = 5.0
   if 5.52 == shows1 {
      shows1 += (Float(1 % (Swift.max(Int(shows1 > 131518662.0 || shows1 < -131518662.0 ? 76.0 : shows1), 1))))
   }

        let photoBtn = UIButton(type: .custom)
        photoBtn.contentHorizontalAlignment = .fill
        photoBtn.contentVerticalAlignment = .fill
        photoBtn.imageView?.contentMode = .scaleAspectFit
        photoBtn.addTarget(self, action: #selector(followTapped), for: .touchUpInside)
        return photoBtn
    }()

    private lazy var reportButton: UIButton = {
       var http9: Float = 3.0
    var extu: String! = String(cString: [111,116,104,101,114,0], encoding: .utf8)!
      http9 /= Swift.max(4, Float(extu.count))

        let photoBtn = UIButton(type: .custom)
      extu.append("\(extu.count / 2)")
        photoBtn.setImage("home_report".toImage, for: .normal)
        photoBtn.addTarget(self, action: #selector(reportTapped), for: .touchUpInside)
        return photoBtn
    }()

    private let contentLabel: UILabel = {
       var znewsl: String! = String(cString: [114,97,100,105,111,0], encoding: .utf8)!
    var optionsC: Float = 3.0
   for _ in 0 ..< 2 {
      znewsl = "\((Int(optionsC > 53211823.0 || optionsC < -53211823.0 ? 60.0 : optionsC)))"
   }

        let v = UILabel()
   while (optionsC == optionsC) {
       var migratedN: String! = String(cString: [109,100,97,116,101,0], encoding: .utf8)!
          var networkD: [Any]! = [String(cString: [99,111,100,101,115,0], encoding: .utf8)!, String(cString: [112,97,105,114,119,105,115,101,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &networkD) { pointer in
    
         }
          var overridess: Float = 2.0
         withUnsafeMutablePointer(to: &overridess) { pointer in
                _ = pointer.pointee
         }
         migratedN = "\(3)"
         networkD.append(3)
         overridess -= Float(migratedN.count)
      while (3 <= migratedN.count) {
         migratedN.append("\(1 << (Swift.min(5, migratedN.count)))")
         break
      }
         migratedN = "\(migratedN.count / (Swift.max(migratedN.count, 3)))"
      optionsC /= Swift.max(2, (Float(2 + Int(optionsC > 330688288.0 || optionsC < -330688288.0 ? 98.0 : optionsC))))
      break
   }
        v.font = .systemFont(ofSize: 14)
        v.textColor = .black
        v.numberOfLines = 0
        return v
    }()

    private lazy var likeButton = makeIconButton(action: #selector(likeTapped))
    private lazy var commentButton: UIButton = {
       var ncopy_x5t: [String: Any]! = [String(cString: [109,101,116,97,98,111,100,121,0], encoding: .utf8)!:String(cString: [99,111,112,121,0], encoding: .utf8)!, String(cString: [102,97,105,108,97,98,108,101,0], encoding: .utf8)!:String(cString: [115,117,99,99,101,115,115,102,117,108,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &ncopy_x5t) { pointer in
          _ = pointer.pointee
   }
   while (2 < (5 / (Swift.max(8, ncopy_x5t.count))) && (ncopy_x5t.values.count / (Swift.max(8, ncopy_x5t.count))) < 5) {
      ncopy_x5t = ["\(ncopy_x5t.values.count)": 2 % (Swift.max(4, ncopy_x5t.values.count))]
      break
   }

        let photoBtn = makeIconButton(action: #selector(commentTapped))
        photoBtn.setImage("home_commit".toImage, for: .normal)
        return photoBtn
    }()
    private lazy var collectButton = makeIconButton(action: #selector(collectTapped))

    private let likeCountLabel = makeCountLabel()
    private let commentCountLabel = makeCountLabel()

    private let actionStack: UIStackView = {
       var fieldu: Float = 1.0
   withUnsafeMutablePointer(to: &fieldu) { pointer in
    
   }
       var configr: String! = String(cString: [97,99,99,101,110,116,0], encoding: .utf8)!
       var timer9: String! = String(cString: [105,110,118,102,0], encoding: .utf8)!
       _ = timer9
      while (5 == timer9.count) {
          var w_layerG: String! = String(cString: [99,111,110,110,0], encoding: .utf8)!
          var stringX: Float = 1.0
          var builtT: String! = String(cString: [115,116,105,99,107,121,0], encoding: .utf8)!
          _ = builtT
          var loginT: String! = String(cString: [118,105,115,105,98,105,108,105,116,121,0], encoding: .utf8)!
          _ = loginT
          var gemsM: Double = 4.0
         configr = "\(builtT.count | 1)"
         w_layerG = "\(loginT.count)"
         stringX += Float(builtT.count)
         loginT.append("\(w_layerG.count << (Swift.min(labs(1), 3)))")
         gemsM -= Double(loginT.count)
         break
      }
      repeat {
         configr = "\(configr.count | 1)"
         if configr.count == 2942198 {
            break
         }
      } while (configr.count == 2942198) && (timer9.count >= 2)
         timer9.append("\((timer9 == (String(cString:[115,0], encoding: .utf8)!) ? timer9.count : configr.count))")
      repeat {
         timer9.append("\(timer9.count * 1)")
         if 1018921 == timer9.count {
            break
         }
      } while (1018921 == timer9.count) && (!timer9.contains("\(configr.count)"))
      for _ in 0 ..< 2 {
         timer9.append("\(configr.count)")
      }
         timer9 = "\(((String(cString:[104,0], encoding: .utf8)!) == configr ? timer9.count : configr.count))"
      fieldu /= Swift.max(4, (Float(Int(fieldu > 291923180.0 || fieldu < -291923180.0 ? 2.0 : fieldu))))

        let stack = UIStackView()
        stack.axis = .horizontal
        stack.spacing = 20
        stack.alignment = .center
        return stack
    }()

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        setupUI()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }


    private func setupUI() {
       var register_cyd: Float = 3.0
   if register_cyd < 1.100 {
      register_cyd += (Float(Int(register_cyd > 124981105.0 || register_cyd < -124981105.0 ? 84.0 : register_cyd)))
   }

        selectionStyle = .none
        backgroundColor = .clear
        contentView.backgroundColor = .clear

        let network = UITapGestureRecognizer(target: self, action: #selector(avatarTapped))
        avatarView.addGestureRecognizer(network)

        contentView.addSubview(cardView)
        cardView.addSubview(avatarView)
        cardView.addSubview(nameLabel)
        cardView.addSubview(timeLabel)
        cardView.addSubview(followButton)
        cardView.addSubview(reportButton)
        cardView.addSubview(contentLabel)
        cardView.addSubview(actionStack)

        let capturer = makeActionWrap(button: likeButton, label: likeCountLabel)
        let backE = makeActionWrap(button: commentButton, label: commentCountLabel)
        let hnew_yu = makeActionWrap(button: collectButton, label: UILabel())
        actionStack.addArrangedSubview(capturer)
        actionStack.addArrangedSubview(backE)
        actionStack.addArrangedSubview(hnew_yu)
        actionStack.addArrangedSubview(UIView())

        cardView.snp.makeConstraints { make in
            make.left.right.equalToSuperview().inset(16)
            make.top.equalToSuperview().offset(12)
            make.bottom.equalToSuperview().offset(-20)
        }

        avatarView.snp.makeConstraints { make in
            make.top.left.equalToSuperview().offset(14)
            make.width.height.equalTo(48)
        }

        nameLabel.snp.makeConstraints { make in
            make.left.equalTo(avatarView.snp.right).offset(8)
            make.top.equalTo(avatarView).offset(5)
            make.height.equalTo(18)
            make.right.lessThanOrEqualTo(followButton.snp.left).offset(-8)
        }

        timeLabel.snp.makeConstraints { make in
            make.left.equalTo(nameLabel)
            make.top.equalTo(nameLabel.snp.bottom).offset(4)
            make.height.equalTo(15)
        }

        reportButton.snp.makeConstraints { make in
            make.centerY.equalTo(avatarView)
            make.right.equalToSuperview().offset(-12)
            make.width.height.equalTo(20)
        }

        followButton.snp.makeConstraints { make in
            make.centerY.equalTo(avatarView)
            make.right.equalTo(reportButton.snp.left).offset(-8)
            make.width.equalTo(70)
            make.height.equalTo(27)
        }

        contentLabel.snp.makeConstraints { make in
            make.top.equalTo(avatarView.snp.bottom).offset(12)
            make.left.right.equalToSuperview().inset(14)
        }

        actionStack.snp.makeConstraints { make in
            make.top.equalTo(contentLabel.snp.bottom).offset(12)
            make.left.equalToSuperview().offset(12)
            make.right.lessThanOrEqualToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
        }

        [likeButton, commentButton, collectButton].forEach { btn in
            btn.snp.makeConstraints { make in
                make.width.height.equalTo(24)
            }
        }
    }

    @objc private func followTapped() {
       var switch_1dC: [String: Any]! = [String(cString: [114,111,113,118,105,100,101,111,0], encoding: .utf8)!:51, String(cString: [97,99,111,109,112,114,101,115,115,111,114,0], encoding: .utf8)!:71]
    var description_lpv: String! = String(cString: [98,105,111,109,101,116,114,121,0], encoding: .utf8)!
      switch_1dC["\(description_lpv)"] = switch_1dC.keys.count
      description_lpv = "\(switch_1dC.count << (Swift.min(description_lpv.count, 3)))"
 onFollowTapped?() }


    private static func makeCountLabel() -> UILabel {
       var sidel: String! = String(cString: [116,119,101,97,107,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      sidel.append("\(sidel.count * 2)")
   }

        let main_nView = UILabel()
        main_nView.font = .systemFont(ofSize: 13, weight: .medium)
        main_nView.textColor = UIColor(hex: "#4A3F35")
        return main_nView
    }


    private func makeIconButton(action: Selector) -> UIButton {
       var buttonsy: Float = 4.0
       var v_imagej: Double = 3.0
       _ = v_imagej
       var baseT: [String: Any]! = [String(cString: [101,114,97,115,117,114,101,0], encoding: .utf8)!:74, String(cString: [121,117,118,97,0], encoding: .utf8)!:5]
      repeat {
         v_imagej -= Double(3)
         if 3546319.0 == v_imagej {
            break
         }
      } while (3546319.0 == v_imagej) && ((Double(baseT.keys.count) * v_imagej) > 2.31)
      repeat {
         baseT = ["\(baseT.count)": 1]
         if 4264420 == baseT.count {
            break
         }
      } while (4264420 == baseT.count) && (baseT.values.contains { $0 as? Double == v_imagej })
         v_imagej += (Double(2 | Int(v_imagej > 52391106.0 || v_imagej < -52391106.0 ? 85.0 : v_imagej)))
          var user2: Double = 2.0
          var type_t_x: String! = String(cString: [104,97,110,100,111,102,102,0], encoding: .utf8)!
          _ = type_t_x
          var parametersj: [String: Any]! = [String(cString: [117,99,109,112,0], encoding: .utf8)!:17, String(cString: [100,97,116,101,116,105,109,101,0], encoding: .utf8)!:24]
          _ = parametersj
         v_imagej -= (Double(Int(v_imagej > 176331031.0 || v_imagej < -176331031.0 ? 33.0 : v_imagej)))
         user2 -= (Double((String(cString:[68,0], encoding: .utf8)!) == type_t_x ? type_t_x.count : Int(v_imagej > 354652366.0 || v_imagej < -354652366.0 ? 58.0 : v_imagej)))
         parametersj = [type_t_x: type_t_x.count << (Swift.min(labs(1), 2))]
         v_imagej += Double(baseT.values.count)
         v_imagej += Double(3)
      buttonsy /= Swift.max(3, (Float(Int(v_imagej > 73716134.0 || v_imagej < -73716134.0 ? 64.0 : v_imagej))))

        let photoBtn = UIButton(type: .custom)
        photoBtn.addTarget(self, action: action, for: .touchUpInside)
        return photoBtn
    }


    private func updateCollectButton(isCollected: Bool) {
       var z_playerv: [Any]! = [String(cString: [112,108,97,121,101,114,0], encoding: .utf8)!, String(cString: [114,117,110,108,111,111,112,0], encoding: .utf8)!]
    var feedx: String! = String(cString: [98,114,111,119,110,0], encoding: .utf8)!
    _ = feedx
   for _ in 0 ..< 2 {
      feedx = "\(feedx.count)"
   }

      z_playerv = [feedx.count]
        let url = isCollected ? "home_collected" : "home_collect"
        collectButton.setImage(url.toImage, for: .normal)
    }


    @objc private func avatarTapped() {
       var mockJ: Int = 2
    var resolved0: Bool = true
    _ = resolved0
      resolved0 = 76 >= mockJ || !resolved0
      mockJ *= mockJ
 onAvatarTapped?() }

    @objc private func commentTapped() {
       var for_zE: Int = 3
   if (4 - for_zE) >= 1 {
      for_zE &= for_zE ^ for_zE
   }
}

    @objc private func likeTapped() {
       var movie4: String! = String(cString: [99,97,108,108,98,97,99,107,0], encoding: .utf8)!
    _ = movie4
       var storeO: [Any]! = [72, 64]
       var linkv: [Any]! = [51, 46, 66]
       _ = linkv
       var discoverz: Float = 1.0
      repeat {
          var documents_: Int = 0
         withUnsafeMutablePointer(to: &documents_) { pointer in
    
         }
          var iconB: String! = String(cString: [99,111,109,112,97,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &iconB) { pointer in
    
         }
          var optionsm: Float = 0.0
         discoverz /= Swift.max((Float(linkv.count << (Swift.min(3, labs(Int(optionsm > 21461687.0 || optionsm < -21461687.0 ? 24.0 : optionsm)))))), 4)
         documents_ /= Swift.max(3, documents_)
         iconB.append("\((Int(optionsm > 175643700.0 || optionsm < -175643700.0 ? 75.0 : optionsm)))")
         if discoverz == 4589743.0 {
            break
         }
      } while ((storeO.count % 5) > 2) && (discoverz == 4589743.0)
         storeO.append((Int(discoverz > 288887109.0 || discoverz < -288887109.0 ? 25.0 : discoverz) % (Swift.max(5, storeO.count))))
          var greetingd: Double = 4.0
         withUnsafeMutablePointer(to: &greetingd) { pointer in
    
         }
          var rawT: String! = String(cString: [109,98,99,110,116,0], encoding: .utf8)!
         linkv.append(linkv.count + 1)
         greetingd /= Swift.max(1, (Double(Int(discoverz > 260803619.0 || discoverz < -260803619.0 ? 91.0 : discoverz) << (Swift.min(storeO.count, 3)))))
         rawT = "\(linkv.count)"
          var pendingg: [Any]! = [25, 2]
          var permissiono: String! = String(cString: [115,117,98,116,114,101,101,115,0], encoding: .utf8)!
          var authorsL: [Any]! = [13, 76, 52]
         withUnsafeMutablePointer(to: &authorsL) { pointer in
    
         }
         linkv.append(permissiono.count)
         pendingg.append(pendingg.count >> (Swift.min(labs(3), 5)))
         authorsL = [pendingg.count | permissiono.count]
          var for_09: Double = 3.0
          _ = for_09
         storeO.append((Int(discoverz > 336492521.0 || discoverz < -336492521.0 ? 57.0 : discoverz) & 1))
         for_09 += (Double(Int(discoverz > 162455548.0 || discoverz < -162455548.0 ? 13.0 : discoverz)))
         storeO.append((Int(discoverz > 373564849.0 || discoverz < -373564849.0 ? 73.0 : discoverz) & 1))
         storeO = [linkv.count % 3]
         linkv.append(storeO.count)
          var looperd: String! = String(cString: [99,116,105,109,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &looperd) { pointer in
                _ = pointer.pointee
         }
          var networkd: Double = 1.0
         storeO.append(2)
         looperd.append("\(linkv.count - 2)")
         networkd -= (Double(Int(networkd > 70631546.0 || networkd < -70631546.0 ? 42.0 : networkd) + linkv.count))
      movie4.append("\(3 - linkv.count)")
 onLikeTapped?() }

    @objc private func reportTapped() {
       var selectedv: [Any]! = [38, 62, 19]
      selectedv.append(selectedv.count)
 onReportTapped?() }


    private func makeActionWrap(button: UIButton, label: UILabel) -> UIStackView {
       var configsc: Int = 2
      configsc |= configsc % (Swift.max(configsc, 10))

        let comment = UIStackView(arrangedSubviews: [button, label])
        comment.axis = .horizontal
        comment.spacing = 4
        comment.alignment = .center
        return comment
    }


    private func updateFollowButton(isFollowing: Bool) {
       var tab0: Float = 5.0
   repeat {
      tab0 -= (Float(Int(tab0 > 37944908.0 || tab0 < -37944908.0 ? 81.0 : tab0)))
      if tab0 == 968498.0 {
         break
      }
   } while (tab0 == 968498.0) && (1.30 <= (Double(Int(tab0) / 5)))

        let url = isFollowing ? "home_following" : "home_follow"
        followButton.setImage(url.toImage, for: .normal)
    }


    private func updateLikeButton(isLiked: Bool) {
       var colorM: Bool = true
    var skyR: String! = String(cString: [111,118,101,114,119,114,105,116,101,0], encoding: .utf8)!
       var capturerx: [String: Any]! = [String(cString: [105,112,118,105,100,101,111,0], encoding: .utf8)!:45]
       var httpj: String! = String(cString: [117,110,99,111,110,115,117,109,101,100,0], encoding: .utf8)!
       var collectionR: Float = 0.0
       var photoi: [Any]! = [[String(cString: [99,111,108,108,101,99,116,105,111,110,0], encoding: .utf8)!:86, String(cString: [114,101,99,111,109,109,101,110,100,0], encoding: .utf8)!:100, String(cString: [107,101,121,105,100,0], encoding: .utf8)!:27]]
      for _ in 0 ..< 2 {
         photoi = [capturerx.values.count]
      }
      while ((collectionR / (Swift.max(1.90, 6))) == 3.43) {
         collectionR += Float(1)
         break
      }
      if !photoi.contains { $0 as? Float == collectionR } {
          var tapN: Float = 5.0
          _ = tapN
          var mockI: String! = String(cString: [115,99,105,101,110,116,105,102,105,99,0], encoding: .utf8)!
          var loggedf: [Any]! = [String(cString: [112,114,101,99,111,109,112,117,116,101,0], encoding: .utf8)!, String(cString: [99,97,110,99,101,108,108,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [110,101,103,111,116,105,97,116,101,100,0], encoding: .utf8)!]
         photoi = [(Int(tapN > 9593491.0 || tapN < -9593491.0 ? 44.0 : tapN))]
         mockI.append("\((1 >> (Swift.min(2, labs(Int(collectionR > 56075282.0 || collectionR < -56075282.0 ? 80.0 : collectionR))))))")
         loggedf.append((Int(tapN > 98249314.0 || tapN < -98249314.0 ? 26.0 : tapN)))
      }
      if (photoi.count & 3) <= 3 && 1 <= (photoi.count & 3) {
         httpj.append("\(capturerx.keys.count)")
      }
         httpj.append("\(httpj.count)")
      while (5 == photoi.count) {
          var delayO: String! = String(cString: [99,108,97,110,103,0], encoding: .utf8)!
          var backv: Bool = true
          var skyk: String! = String(cString: [118,112,100,101,99,0], encoding: .utf8)!
          _ = skyk
          var pathse: Double = 1.0
         withUnsafeMutablePointer(to: &pathse) { pointer in
                _ = pointer.pointee
         }
          var appleT: String! = String(cString: [97,102,102,105,110,105,116,105,101,115,0], encoding: .utf8)!
         collectionR += (Float(skyk.count << (Swift.min(3, labs(Int(pathse > 243093301.0 || pathse < -243093301.0 ? 22.0 : pathse))))))
         delayO = "\(((backv ? 3 : 3) & Int(pathse > 281396250.0 || pathse < -281396250.0 ? 60.0 : pathse)))"
         backv = 15 < httpj.count
         appleT = "\(appleT.count << (Swift.min(3, photoi.count)))"
         break
      }
         collectionR /= Swift.max((Float((String(cString:[122,0], encoding: .utf8)!) == httpj ? httpj.count : photoi.count)), 4)
         httpj = "\(((String(cString:[88,0], encoding: .utf8)!) == httpj ? capturerx.values.count : httpj.count))"
      colorM = !colorM

   while (!skyR.hasPrefix("\(skyR.count)")) {
       var extension_1W: Bool = true
      withUnsafeMutablePointer(to: &extension_1W) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 3 {
         extension_1W = (!extension_1W ? !extension_1W : !extension_1W)
      }
      if extension_1W {
         extension_1W = !extension_1W
      }
       var allJ: String! = String(cString: [117,110,108,111,99,107,0], encoding: .utf8)!
       var chromeE: String! = String(cString: [114,101,102,101,114,114,101,114,0], encoding: .utf8)!
         allJ = "\(allJ.count & 2)"
         chromeE.append("\(((extension_1W ? 5 : 4) >> (Swift.min(labs(3), 1))))")
      skyR.append("\(((extension_1W ? 3 : 2) / 2))")
      break
   }
        let url = isLiked ? "home_liked" : "home_like"
        likeButton.setImage(url.toImage, for: .normal)
    }

    @objc private func collectTapped() {
       var visibleq: [String: Any]! = [String(cString: [100,105,118,0], encoding: .utf8)!:9, String(cString: [100,101,97,100,0], encoding: .utf8)!:98, String(cString: [105,97,116,0], encoding: .utf8)!:17]
    var z_imageD: Int = 3
       var form1: String! = String(cString: [108,105,102,101,99,121,99,108,101,0], encoding: .utf8)!
       var basee: [String: Any]! = [String(cString: [97,115,110,0], encoding: .utf8)!:44, String(cString: [102,114,97,109,101,113,117,101,117,101,0], encoding: .utf8)!:39, String(cString: [98,105,110,116,101,120,116,0], encoding: .utf8)!:95]
      for _ in 0 ..< 1 {
         form1.append("\((form1 == (String(cString:[105,0], encoding: .utf8)!) ? basee.values.count : form1.count))")
      }
         basee = ["\(basee.count)": basee.keys.count]
      for _ in 0 ..< 3 {
         basee = ["\(basee.values.count)": basee.values.count / (Swift.max(1, 10))]
      }
         basee[form1] = 3 % (Swift.max(5, basee.values.count))
          var buttonv: String! = String(cString: [99,108,111,115,101,0], encoding: .utf8)!
          var moviei: String! = String(cString: [99,108,111,99,107,119,105,115,101,0], encoding: .utf8)!
          var modez: Double = 5.0
         form1.append("\((buttonv == (String(cString:[84,0], encoding: .utf8)!) ? Int(modez > 387082465.0 || modez < -387082465.0 ? 79.0 : modez) : buttonv.count))")
         moviei.append("\(2 - moviei.count)")
      for _ in 0 ..< 1 {
          var pricek: String! = String(cString: [98,102,108,121,0], encoding: .utf8)!
         basee = ["\(basee.count)": 1]
         pricek = "\(basee.count / (Swift.max(form1.count, 5)))"
      }
      z_imageD += z_imageD
   for _ in 0 ..< 2 {
      visibleq["\(visibleq.keys.count)"] = 3 * visibleq.values.count
   }
 onCollectTapped?() }


    func configure(with post: CSColor) {
       var hangh: Int = 4
    var repliesv: Int = 3
   if (hangh >> (Swift.min(labs(2), 4))) == 2 || (repliesv >> (Swift.min(labs(hangh), 2))) == 2 {
      hangh |= repliesv & hangh
   }

       var discoverh: [Any]! = [64, 85]
         discoverh = [discoverh.count & discoverh.count]
      if (4 - discoverh.count) < 2 {
         discoverh.append(discoverh.count - discoverh.count)
      }
          var startj: String! = String(cString: [105,115,115,117,105,110,103,0], encoding: .utf8)!
          var d_centerC: String! = String(cString: [115,98,99,100,101,99,0], encoding: .utf8)!
          _ = d_centerC
         discoverh = [d_centerC.count]
         startj.append("\(d_centerC.count & startj.count)")
      repliesv ^= discoverh.count % 3
        nameLabel.text = post.userName
        timeLabel.text = post.time
        contentLabel.text = post.content
        likeCountLabel.text = "\(post.likeCount)"
        commentCountLabel.text = "\(post.commentCount)"
        updateFollowButton(isFollowing: post.isFollowing)
        updateLikeButton(isLiked: post.isLiked)
        updateCollectButton(isCollected: post.isCollected)

        if let avatarPath = post.avatarPath, !avatarPath.isEmpty {
            avatarView.image = avatarPath.resourceFileImage ?? avatarPath.toImage
            avatarView.backgroundColor = avatarView.image == nil
                ? UIColor(hex: "#D4C4A8") : .clear
        } else {
            avatarView.image = "info_avatar".toImage
            avatarView.backgroundColor = avatarView.image == nil
                ? UIColor(hex: "#D4C4A8") : .clear
        }
    }
}
