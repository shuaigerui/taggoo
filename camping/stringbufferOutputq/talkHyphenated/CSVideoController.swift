
import Foundation

import Toast_Swift
import UIKit

enum CS_SetupMode {
    case signIn
    case create
}

class CSVideoController: CSConversationController {

    private let mode: CS_SetupMode
    private var isPasswordVisible = false

    private lazy var backButton: UIButton = {
       var imgw: Double = 0.0
    var modes: [String: Any]! = [String(cString: [103,114,97,121,114,103,98,0], encoding: .utf8)!:82, String(cString: [115,101,113,117,101,110,99,101,114,0], encoding: .utf8)!:20]
       var sourcesK: Float = 5.0
      withUnsafeMutablePointer(to: &sourcesK) { pointer in
             _ = pointer.pointee
      }
       var trimmedH: [String: Any]! = [String(cString: [115,116,97,107,101,0], encoding: .utf8)!:69, String(cString: [115,116,97,116,101,0], encoding: .utf8)!:61, String(cString: [116,101,115,115,101,108,108,97,116,101,0], encoding: .utf8)!:36]
       _ = trimmedH
       var destl: String! = String(cString: [98,108,101,110,100,0], encoding: .utf8)!
       _ = destl
         trimmedH = ["\(trimmedH.count)": trimmedH.keys.count & destl.count]
      repeat {
         destl.append("\(3)")
         if destl == (String(cString:[107,99,114,97,122,102,111,105,118,0], encoding: .utf8)!) {
            break
         }
      } while (destl == (String(cString:[107,99,114,97,122,102,111,105,118,0], encoding: .utf8)!)) && (2 >= (2 / (Swift.max(6, trimmedH.count))) || (trimmedH.count / (Swift.max(7, destl.count))) >= 2)
         destl.append("\(trimmedH.keys.count)")
      while ((Float(destl.count) * sourcesK) == 2.1 && 2.1 == (Float(destl.count) * sourcesK)) {
          var window_38K: Double = 3.0
          var followerQ: Float = 3.0
         destl.append("\(3)")
         window_38K -= Double(destl.count)
         followerQ /= Swift.max(4, (Float(Int(window_38K > 366896265.0 || window_38K < -366896265.0 ? 88.0 : window_38K) ^ 3)))
         break
      }
      while ((trimmedH.count | 5) > 2) {
         trimmedH = ["\(trimmedH.keys.count)": trimmedH.keys.count << (Swift.min(labs(3), 5))]
         break
      }
      repeat {
         destl = "\(trimmedH.keys.count >> (Swift.min(labs(3), 3)))"
         if destl.count == 1938725 {
            break
         }
      } while (destl.contains("\(sourcesK)")) && (destl.count == 1938725)
      for _ in 0 ..< 1 {
         trimmedH[destl] = (destl == (String(cString:[102,0], encoding: .utf8)!) ? Int(sourcesK > 325837742.0 || sourcesK < -325837742.0 ? 64.0 : sourcesK) : destl.count)
      }
         trimmedH["\(sourcesK)"] = destl.count
          var submittedV: String! = String(cString: [108,103,111,114,97,110,100,0], encoding: .utf8)!
          var reportedy: Int = 0
          var vcsb: [Any]! = [29, 9]
          _ = vcsb
         trimmedH["\(sourcesK)"] = (destl.count >> (Swift.min(2, labs(Int(sourcesK > 212108440.0 || sourcesK < -212108440.0 ? 9.0 : sourcesK)))))
         submittedV.append("\(2)")
         reportedy -= trimmedH.values.count + destl.count
         vcsb = [3]
      modes["\(sourcesK)"] = trimmedH.count

        let photoBtn = UIButton(type: .custom)
   if (modes.count % (Swift.max(4, 1))) > 5 || (modes.count & 4) > 2 {
      imgw += (Double(modes.keys.count << (Swift.min(3, labs(Int(imgw > 302985937.0 || imgw < -302985937.0 ? 98.0 : imgw))))))
   }
        photoBtn.setImage(UIImage(named: "common_back"), for: .normal)
        photoBtn.addTarget(self, action: #selector(onBack), for: .touchUpInside)
        return photoBtn
    }()

    private let titleImageView: UIImageView = {
       var valueW: [Any]! = [24, 74]
    _ = valueW
    var constraintM: String! = String(cString: [99,111,102,97,99,116,111,114,0], encoding: .utf8)!
      constraintM = "\(((String(cString:[75,0], encoding: .utf8)!) == constraintM ? constraintM.count : valueW.count))"

        let v = UIImageView()
   for _ in 0 ..< 1 {
      valueW = [3]
   }
        v.image = "setup_title".toImage
        v.contentMode = .scaleAspectFit
        return v
    }()

    private let cardView: UIView = {
       var valueu: String! = String(cString: [112,117,110,99,104,0], encoding: .utf8)!
    var itemsx: String! = String(cString: [99,111,108,111,114,113,117,97,110,116,0], encoding: .utf8)!
       var modelJ: String! = String(cString: [111,103,103,100,101,99,0], encoding: .utf8)!
       var saved5: String! = String(cString: [100,101,99,111,100,101,114,116,104,114,101,97,100,105,110,103,0], encoding: .utf8)!
       var linkR: String! = String(cString: [99,97,109,101,108,108,105,97,0], encoding: .utf8)!
         saved5.append("\(((String(cString:[101,0], encoding: .utf8)!) == saved5 ? saved5.count : linkR.count))")
      for _ in 0 ..< 3 {
         modelJ.append("\(saved5.count)")
      }
      while (modelJ.count > 2) {
          var costN: [Any]! = [89, 94]
         withUnsafeMutablePointer(to: &costN) { pointer in
                _ = pointer.pointee
         }
         modelJ = "\(saved5.count)"
         costN.append(saved5.count)
         break
      }
          var formatterH: Float = 4.0
          var dismissi: Bool = false
          var collectsc: [Any]! = [84, 32]
         withUnsafeMutablePointer(to: &collectsc) { pointer in
                _ = pointer.pointee
         }
         saved5.append("\(modelJ.count % 1)")
         formatterH -= Float(collectsc.count)
         dismissi = saved5 == linkR
         collectsc.append(((dismissi ? 2 : 5) | collectsc.count))
          var launchl: Float = 1.0
         saved5 = "\(3)"
         launchl += Float(1 / (Swift.max(2, modelJ.count)))
      while (4 < saved5.count && linkR == String(cString:[79,0], encoding: .utf8)!) {
         saved5.append("\(3)")
         break
      }
      repeat {
          var changei: String! = String(cString: [115,101,108,101,99,116,97,98,108,101,0], encoding: .utf8)!
          var switch_qo: Double = 0.0
          _ = switch_qo
          var running9: String! = String(cString: [111,98,115,101,114,118,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &running9) { pointer in
    
         }
          var reporti: Double = 5.0
          var light6: String! = String(cString: [112,97,114,105,116,105,111,110,105,110,103,0], encoding: .utf8)!
         saved5.append("\(modelJ.count)")
         changei.append("\(light6.count)")
         switch_qo += Double(1 - modelJ.count)
         running9.append("\(light6.count)")
         reporti /= Swift.max(Double(running9.count | 3), 1)
         if 775961 == saved5.count {
            break
         }
      } while (saved5.count >= 2 && linkR != String(cString:[66,0], encoding: .utf8)!) && (775961 == saved5.count)
      while (linkR.count == saved5.count) {
         linkR.append("\((linkR == (String(cString:[88,0], encoding: .utf8)!) ? saved5.count : linkR.count))")
         break
      }
         linkR = "\(linkR.count)"
      valueu = "\(3 * itemsx.count)"

        let v = UIView()
   if itemsx.contains("\(valueu.count)") {
      valueu = "\(valueu.count | itemsx.count)"
   }
        v.backgroundColor = UIColor(hex: "#E9DC8A")
        v.layer.cornerRadius = 24
        return v
    }()

    private let emailIconView = CSVideoController.makeIconView(named: "setup_email")
    private let passwordIconView = CSVideoController.makeIconView(named: "setup_password")

    private let emailTitleLabel = CSVideoController.makeSectionTitle("Email")
    private let passwordTitleLabel = CSVideoController.makeSectionTitle("Password")

    private lazy var emailField = CSVideoController.makeTextField(placeholder: "Your Email", keyboard: .emailAddress)
    private lazy var passwordField = CSVideoController.makeTextField(placeholder: "Your Password", keyboard: .default, isSecure: true)

    private lazy var passwordToggleButton: UIButton = {
       var for_p2: Double = 3.0
      for_p2 += (Double(Int(for_p2 > 255009400.0 || for_p2 < -255009400.0 ? 39.0 : for_p2) + 2))

        let photoBtn = UIButton(type: .custom)
        photoBtn.setImage("setup_show".toImage, for: .normal)
        photoBtn.addTarget(self, action: #selector(togglePasswordVisibility), for: .touchUpInside)
        return photoBtn
    }()

    private lazy var switchTextView: UITextView = {
       var reportedC: Float = 5.0
    var testn: Bool = false
   while (!testn) {
      reportedC /= Swift.max((Float(Int(reportedC > 16469739.0 || reportedC < -16469739.0 ? 33.0 : reportedC) | 2)), 1)
      break
   }

        let displayView = UITextView()
   for _ in 0 ..< 1 {
      testn = !testn
   }
        displayView.backgroundColor = .clear
        displayView.isEditable = false
        displayView.isScrollEnabled = false
        displayView.isSelectable = true
        displayView.textContainerInset = .zero
        displayView.textContainer.lineFragmentPadding = 0
        displayView.delegate = self
        displayView.linkTextAttributes = [
            .foregroundColor: UIColor(hex: "#4A3F35"),
            .underlineStyle: NSUnderlineStyle.single.rawValue
        ]
        return displayView
    }()

    private lazy var actionButton: UIButton = {
       var starryZ: String! = String(cString: [112,114,111,109,105,110,101,110,116,0], encoding: .utf8)!
   repeat {
      starryZ.append("\(starryZ.count - starryZ.count)")
      if (String(cString:[52,52,116,97,108,117,119,118,97,0], encoding: .utf8)!) == starryZ {
         break
      }
   } while (starryZ.count == 5) && ((String(cString:[52,52,116,97,108,117,119,118,97,0], encoding: .utf8)!) == starryZ)

        let photoBtn = UIButton(type: .custom)
        let area = mode == .signIn ? "setup_sign" : "setup_next"
        photoBtn.setImage(area.toImage, for: .normal)
        photoBtn.contentHorizontalAlignment = .fill
        photoBtn.contentVerticalAlignment = .fill
        photoBtn.imageView?.contentMode = .scaleAspectFit
        photoBtn.addTarget(self, action: #selector(onAction), for: .touchUpInside)
        return photoBtn
    }()

    init(mode: CS_SetupMode) {
        self.mode = mode
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) {
        self.mode = .signIn
        super.init(coder: coder)
    }


    override func viewDidLoad() {
       var areaI: String! = String(cString: [116,116,116,0], encoding: .utf8)!
   while (areaI.count > areaI.count) {
      areaI.append("\(3)")
      break
   }

        super.viewDidLoad()
        switchTextView.attributedText = Self.makeSwitchText(mode: mode)
        setupUI()
    }


    private static func makeSectionTitle(_ text: String) -> UILabel {
       var menu4: [Any]! = [74.0]
      menu4.append(2)

        let popLabel = UILabel()
        popLabel.text = text
        popLabel.font = .systemFont(ofSize: 16, weight: .semibold)
        popLabel.textColor = UIColor(hex: "#4A3F35")
        return popLabel
    }


    @objc private func onAction() {
       var reasonsE: String! = String(cString: [112,114,105,110,116,118,97,108,0], encoding: .utf8)!
       var workw: [Any]! = [23, 76]
       _ = workw
       var photoS: Bool = false
      withUnsafeMutablePointer(to: &photoS) { pointer in
    
      }
       var liveW: String! = String(cString: [113,112,101,108,0], encoding: .utf8)!
         workw.append(liveW.count)
         liveW.append("\(workw.count >> (Swift.min(labs(3), 3)))")
          var s_badgeg: Int = 3
         workw = [workw.count]
         s_badgeg %= Swift.max(liveW.count, 3)
      for _ in 0 ..< 1 {
         workw = [(workw.count << (Swift.min(3, labs((photoS ? 2 : 5)))))]
      }
      while (liveW.hasPrefix("\(workw.count)")) {
         workw = [workw.count]
         break
      }
      if !photoS {
          var conversationg: Float = 4.0
          var listenerW: [Any]! = [76, 88, 38]
          _ = listenerW
          var costc: String! = String(cString: [102,105,108,101,110,97,109,101,115,0], encoding: .utf8)!
         photoS = conversationg < 34.63
         listenerW.append((costc == (String(cString:[105,0], encoding: .utf8)!) ? costc.count : listenerW.count))
      }
       var btn2: [String: Any]! = [String(cString: [98,101,116,116,101,114,0], encoding: .utf8)!:28, String(cString: [111,114,105,101,110,116,97,116,105,111,110,0], encoding: .utf8)!:96]
       _ = btn2
       var chromex: [String: Any]! = [String(cString: [119,114,105,116,101,0], encoding: .utf8)!:String(cString: [112,114,101,99,101,110,99,101,0], encoding: .utf8)!, String(cString: [116,101,109,112,108,97,116,101,0], encoding: .utf8)!:String(cString: [106,111,117,114,110,97,108,110,97,109,101,0], encoding: .utf8)!]
      while (chromex.values.count < 5) {
          var catalogC: [Any]! = [0, 42]
          var camera6: String! = String(cString: [112,114,101,118,105,101,119,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &camera6) { pointer in
                _ = pointer.pointee
         }
          var posts_: Double = 5.0
          var starr: Double = 4.0
         chromex["\(photoS)"] = ((photoS ? 5 : 5) - workw.count)
         catalogC.append(chromex.count)
         camera6.append("\((Int(starr > 260494658.0 || starr < -260494658.0 ? 23.0 : starr)))")
         posts_ -= (Double((photoS ? 3 : 3) + liveW.count))
         starr += Double(1 % (Swift.max(10, liveW.count)))
         break
      }
      repeat {
         photoS = 32 >= chromex.values.count || btn2.count >= 32
         if photoS ? !photoS : photoS {
            break
         }
      } while (liveW.count >= 2) && (photoS ? !photoS : photoS)
      reasonsE.append("\((liveW == (String(cString:[95,0], encoding: .utf8)!) ? liveW.count : (photoS ? 5 : 1)))")

        let folder = emailField.text?.trimmingCharacters(in: .whitespacesAndNewlines) ?? ""
        let posts = passwordField.text ?? ""

        switch mode {
        case .signIn:
            guard !folder.isEmpty, !posts.isEmpty else {
                view.makeToast("Please enter email and password")
                return
            }
            guard CSDeleteForm.shared.login(email: folder, password: posts) else {
                view.makeToast("Invalid email or password")
                return
            }
            CSDeleteForm.shared.switchRoot(on: view.window)
        case .create:
            guard !folder.isEmpty, !posts.isEmpty else {
                view.makeToast("Please enter email and password")
                return
            }
            navigationController?.pushViewController(
                CSManagerFormController(mode: .register(email: folder, password: posts)),
                animated: true
            )
        }
    }


    private func openCreateAccount() {
       var permissionP: String! = String(cString: [116,114,97,110,115,108,97,116,105,111,110,115,0], encoding: .utf8)!
   if !permissionP.contains(permissionP) {
      permissionP = "\(1)"
   }

        guard mode == .signIn else { return }
        navigationController?.pushViewController(CSVideoController(mode: .create), animated: true)
    }


    private static func makeIconView(named: String) -> UIImageView {
       var linez: String! = String(cString: [109,111,118,101,0], encoding: .utf8)!
    var packaged: Double = 1.0
   withUnsafeMutablePointer(to: &packaged) { pointer in
          _ = pointer.pointee
   }
   if packaged == Double(linez.count) {
      packaged /= Swift.max(3, (Double(Int(packaged > 298199756.0 || packaged < -298199756.0 ? 11.0 : packaged) / (Swift.max(1, linez.count)))))
   }

        let main_nView = UIImageView(image: named.toImage)
        main_nView.contentMode = .scaleAspectFill
        return main_nView
    }


    private static func makeSwitchText(mode: CS_SetupMode) -> NSAttributedString {
       var dest8: Double = 3.0
   withUnsafeMutablePointer(to: &dest8) { pointer in
          _ = pointer.pointee
   }
       var agreementO: Double = 2.0
       var records4: Bool = false
         records4 = !records4
      for _ in 0 ..< 3 {
         agreementO -= (Double((records4 ? 5 : 1) & Int(agreementO > 242872758.0 || agreementO < -242872758.0 ? 70.0 : agreementO)))
      }
          var local_nh9: Double = 3.0
          var fallbackS: String! = String(cString: [110,97,117,116,105,99,97,108,0], encoding: .utf8)!
          var roomx: String! = String(cString: [102,102,109,112,101,103,0], encoding: .utf8)!
         records4 = (61 < (roomx.count + (records4 ? 61 : roomx.count)))
         local_nh9 -= (Double(2 + Int(agreementO > 369300384.0 || agreementO < -369300384.0 ? 38.0 : agreementO)))
         fallbackS.append("\(2)")
      while (agreementO < 5.16) {
         agreementO += (Double(Int(agreementO > 20205292.0 || agreementO < -20205292.0 ? 62.0 : agreementO) | 2))
         break
      }
         agreementO /= Swift.max(1, (Double(Int(agreementO > 260323819.0 || agreementO < -260323819.0 ? 38.0 : agreementO) - (records4 ? 2 : 1))))
      while (5.71 > (3.23 * agreementO)) {
         agreementO /= Swift.max(3, (Double(Int(agreementO > 42307295.0 || agreementO < -42307295.0 ? 11.0 : agreementO) & 2)))
         break
      }
      dest8 -= (Double((records4 ? 2 : 2) >> (Swift.min(labs(Int(agreementO > 343104592.0 || agreementO < -343104592.0 ? 14.0 : agreementO)), 2))))

        let group: String
        let key: String
        let created: String
        switch mode {
        case .signIn:
            group = "Don't have an account yet ? Create Account"
            key = "Create Account"
            created = "cs://create-account"
        case .create:
            group = "Already have an account? Sign in"
            key = "Sign in"
            created = "cs://sign-in"
        }
        let field = NSMutableAttributedString(
            string: group,
            attributes: [
                .font: UIFont.systemFont(ofSize: 13),
                .foregroundColor: UIColor(hex: "#4A3F35")
            ]
        )
        let z_title = (group as NSString).range(of: key)
        field.addAttribute(.link, value: created, range: z_title)
        let modity = NSMutableParagraphStyle()
        modity.alignment = .center
        field.addAttribute(.paragraphStyle, value: modity, range: NSRange(location: 0, length: group.count))
        return field
    }


    private func setupUI() {
       var design0: Bool = false
   withUnsafeMutablePointer(to: &design0) { pointer in
    
   }
    var actionr: String! = String(cString: [105,110,118,115,98,111,120,0], encoding: .utf8)!
      design0 = actionr.count <= 77

   repeat {
      actionr = "\(3 * actionr.count)"
      if 4033607 == actionr.count {
         break
      }
   } while (4033607 == actionr.count) && (!design0)
        view.addSubview(backButton)
        view.addSubview(titleImageView)
        view.addSubview(cardView)
        view.addSubview(switchTextView)
        view.addSubview(actionButton)

        cardView.addSubview(emailIconView)
        cardView.addSubview(emailTitleLabel)
        cardView.addSubview(emailField)
        cardView.addSubview(passwordIconView)
        cardView.addSubview(passwordTitleLabel)
        cardView.addSubview(passwordField)
        cardView.addSubview(passwordToggleButton)

        backButton.snp.makeConstraints { make in
            make.top.equalTo(view.safeAreaLayoutGuide).offset(8)
            make.left.equalToSuperview().offset(20)
            make.width.height.equalTo(40)
        }

        titleImageView.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.top.equalTo(backButton.snp.bottom).offset(34)
        }

        cardView.snp.makeConstraints { make in
            make.left.right.equalToSuperview().inset(16)
            make.top.equalTo(titleImageView.snp.bottom).offset(28)
            make.height.equalTo(260)
        }

        emailIconView.snp.makeConstraints { make in
            make.top.equalToSuperview().offset(24)
            make.left.equalToSuperview().offset(12)
            make.width.height.equalTo(20)
        }

        emailTitleLabel.snp.makeConstraints { make in
            make.centerY.equalTo(emailIconView)
            make.left.equalTo(emailIconView.snp.right).offset(4)
        }

        emailField.snp.makeConstraints { make in
            make.top.equalTo(emailIconView.snp.bottom).offset(8)
            make.left.right.equalToSuperview().inset(12)
            make.height.equalTo(65)
        }

        passwordIconView.snp.makeConstraints { make in
            make.top.equalTo(emailField.snp.bottom).offset(24)
            make.left.equalToSuperview().offset(12)
            make.width.height.equalTo(20)
        }

        passwordTitleLabel.snp.makeConstraints { make in
            make.centerY.equalTo(passwordIconView)
            make.left.equalTo(passwordIconView.snp.right).offset(4)
        }

        passwordField.snp.makeConstraints { make in
            make.top.equalTo(passwordIconView.snp.bottom).offset(8)
            make.height.left.right.equalTo(emailField)
        }

        passwordToggleButton.snp.makeConstraints { make in
            make.centerY.equalTo(passwordField)
            make.right.equalTo(passwordField).offset(-16)
            make.width.height.equalTo(16)
        }

        actionButton.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.width.equalTo(358)
            make.height.equalTo(60)
            make.bottom.equalTo(view.safeAreaLayoutGuide).offset(-16)
        }

        switchTextView.snp.makeConstraints { make in
            make.left.right.equalToSuperview().inset(25)
            make.bottom.equalTo(actionButton.snp.top).offset(-16)
        }
    }


    @objc private func togglePasswordVisibility() {
       var fallbackM: Double = 5.0
    var conversationse: String! = String(cString: [112,114,101,115,101,110,116,97,98,108,101,0], encoding: .utf8)!
    _ = conversationse
      conversationse.append("\(conversationse.count)")

   for _ in 0 ..< 3 {
      conversationse.append("\(conversationse.count * 1)")
   }
        isPasswordVisible.toggle()
   if 2.56 <= (1.68 / (Swift.max(9, fallbackM))) {
      fallbackM -= (Double(Int(fallbackM > 47225030.0 || fallbackM < -47225030.0 ? 86.0 : fallbackM) & 3))
   }
        passwordField.isSecureTextEntry = !isPasswordVisible
        let area = isPasswordVisible ? "setup_hidden" : "setup_show"
        passwordToggleButton.setImage(area.toImage, for: .normal)
    }


    private static func makeTextField(
        placeholder: String,
        keyboard: UIKeyboardType,
        isSecure: Bool = false
    ) -> UITextField {
       var collects8: String! = String(cString: [115,104,111,119,0], encoding: .utf8)!
   if collects8.hasPrefix(collects8) {
      collects8 = "\(collects8.count)"
   }

        let unread = UITextField()
        unread.keyboardType = keyboard
        unread.autocapitalizationType = .none
        unread.autocorrectionType = .no
        unread.isSecureTextEntry = isSecure
        unread.font = .systemFont(ofSize: 15)
        unread.textColor = UIColor(hex: "#4A3F35")
        unread.backgroundColor = UIColor(hex: "#F3F7BB",alpha: 0.5)
        unread.layer.cornerRadius = 16
        unread.leftView = UIView(frame: CGRect(x: 0, y: 0, width: 14, height: 1))
        unread.leftViewMode = .always
        unread.rightView = UIView(frame: CGRect(x: 0, y: 0, width: 44, height: 1))
        unread.rightViewMode = .always
        unread.attributedPlaceholder = NSAttributedString(
            string: placeholder,
            attributes: [.foregroundColor: UIColor(hex: "#999999")]
        )
        return unread
    }


    private func openSignIn() {
       var micT: String! = String(cString: [97,110,111,110,121,109,111,117,115,0], encoding: .utf8)!
    var transitionn: String! = String(cString: [100,105,116,104,101,114,0], encoding: .utf8)!
      micT.append("\(transitionn.count)")
   if transitionn == String(cString:[112,0], encoding: .utf8)! && micT.count >= 5 {
      transitionn = "\(transitionn.count)"
   }

        navigationController?.popViewController(animated: true)
    }


    @objc private func onBack() {
       var resolved4: String! = String(cString: [112,117,116,99,0], encoding: .utf8)!
    _ = resolved4
    var doc7: [String: Any]! = [String(cString: [119,101,105,103,104,116,0], encoding: .utf8)!:String(cString: [116,114,97,110,115,102,111,114,109,101,114,115,0], encoding: .utf8)!, String(cString: [114,101,109,111,116,101,0], encoding: .utf8)!:String(cString: [115,99,97,109,0], encoding: .utf8)!, String(cString: [105,110,105,116,97,108,105,122,101,0], encoding: .utf8)!:String(cString: [111,110,116,111,0], encoding: .utf8)!]
   for _ in 0 ..< 2 {
      doc7 = ["\(doc7.values.count)": doc7.keys.count ^ 1]
   }
   while (2 > (resolved4.count * 2) && (doc7.count * 2) > 5) {
      resolved4.append("\((resolved4 == (String(cString:[109,0], encoding: .utf8)!) ? doc7.keys.count : resolved4.count))")
      break
   }

        navigationController?.popViewController(animated: true)
    }
}

extension CSVideoController: UITextViewDelegate {


    func textView(
        _ textView: UITextView,
        shouldInteractWith url: URL,
        in characterRange: NSRange,
        interaction: UITextItemInteraction
    ) -> Bool {
       var end3: String! = String(cString: [103,108,107,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
       var g_layerA: String! = String(cString: [109,111,100,101,114,110,0], encoding: .utf8)!
       var extrasy: Bool = true
       var pricesJ: String! = String(cString: [101,120,97,99,116,0], encoding: .utf8)!
       _ = pricesJ
          var input9: Bool = true
          var submitteda: Bool = false
          _ = submitteda
         pricesJ = "\((pricesJ.count + (input9 ? 2 : 2)))"
         submitteda = g_layerA.count == 42 || !submitteda
      repeat {
         g_layerA = "\(((extrasy ? 1 : 2) >> (Swift.min(labs(1), 2))))"
         if (String(cString:[104,117,48,107,122,54,117,104,48,117,0], encoding: .utf8)!) == g_layerA {
            break
         }
      } while ((String(cString:[104,117,48,107,122,54,117,104,48,117,0], encoding: .utf8)!) == g_layerA) && (g_layerA.count >= 3 || 3 >= pricesJ.count)
          var summariesd: Float = 0.0
         extrasy = summariesd > Float(pricesJ.count)
         extrasy = !extrasy && pricesJ.count > 5
      if g_layerA.hasPrefix("\(extrasy)") {
          var sender6: String! = String(cString: [115,101,116,0], encoding: .utf8)!
          var nav3: String! = String(cString: [98,111,114,100,101,114,115,0], encoding: .utf8)!
          var avatarf: String! = String(cString: [99,111,110,102,105,114,109,0], encoding: .utf8)!
         g_layerA.append("\(((extrasy ? 2 : 1) >> (Swift.min(pricesJ.count, 1))))")
         sender6 = "\(nav3.count)"
         nav3 = "\(((extrasy ? 3 : 1) << (Swift.min(labs(2), 5))))"
         avatarf.append("\(((extrasy ? 1 : 2) | nav3.count))")
      }
       var optionT: String! = String(cString: [103,101,111,99,111,100,101,100,0], encoding: .utf8)!
      if extrasy {
         optionT.append("\(pricesJ.count & 3)")
      }
      while (!g_layerA.contains("\(extrasy)")) {
          var likea: String! = String(cString: [109,111,118,101,100,0], encoding: .utf8)!
          var supportr: String! = String(cString: [100,101,110,121,0], encoding: .utf8)!
          var updatedU: Int = 4
          var bootstrapy: Double = 5.0
         withUnsafeMutablePointer(to: &bootstrapy) { pointer in
                _ = pointer.pointee
         }
         extrasy = (String(cString:[116,0], encoding: .utf8)!) == pricesJ
         likea.append("\(g_layerA.count + 3)")
         supportr = "\(likea.count | g_layerA.count)"
         updatedU ^= updatedU % (Swift.max(supportr.count, 8))
         bootstrapy /= Swift.max(Double(pricesJ.count), 4)
         break
      }
         g_layerA.append("\(((extrasy ? 5 : 4)))")
      end3.append("\(((extrasy ? 3 : 1)))")
   }

        switch url.absoluteString {
        case "cs://create-account":
            openCreateAccount()
        case "cs://sign-in":
            openSignIn()
        default:
            break
        }
        return false
    }
}
